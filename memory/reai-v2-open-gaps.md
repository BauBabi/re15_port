---
name: reai-v2-open-gaps
description: "Offene Lücken/TODOs in reAi_v2, die zum sauberen Loslegen noch fehlen"
metadata: 
  node_type: memory
  type: project
  originSessionId: 073aec74-f763-4c50-aa34-afe1225f60d0
---

Bekannte offene Punkte in reAi_v2 (Stand 2026-06-27), aus dem Readiness-Audit:

- ~~CLAUDE.md fehlt~~ **ERLEDIGT 2026-06-27**: aus dem alten reAi adaptiert (RE-Disziplin verbatim; Build/Run auf re15_port-CMake + Java-gradle-Hinweis aktualisiert; tote `DOCUMENTATION.md`/`scripts/`-Refs entfernt) und als `reAi_v2/CLAUDE.md` installiert.
- **ERLEDIGT 2026-06-27 (Referenz-Fixes)**: tote Pfade in `.kiro/skills/{ghidra-mapping,asset-pipeline}.md` korrigiert (deckungsgleich mit `.claude/skills`-Kopien); `RE15_KNOWLEDGE.md` Basis-Pfad reAi→reAi_v2 + Hinweis, dass `memory/`-/`scripts/`-Evidence historische Zitate sind; `UNTESTED_IMPLEMENTATIONS.md` nicht-existenter psx_dev-Mirror entfernt.
- **docs/ fehlt** — DOCUMENTATION.md (Format-Referenz) + docs/AI (enemy_ai) aus dem alten Projekt fehlen.
- **build.gradle/settings.gradle fehlen im Root** — der Java-Extraktor (`src/main/java/de/re15`) lässt sich mit dem mitkopierten gradlew nicht bauen.
- ~~STAGE5/6-RDTs fehlen im Engine-Baum~~ **ERLEDIGT 2026-06-27**: alle Assets nach `re15_port/shared_assets/PSX/` konsolidiert (alle 6 Stages, vollständiger CD-Baum); RE15_ASSETS_PATH defaultet dorthin, Intermediates nach `build/extracted/`. Siehe [[reai-v2-build-recipe]].
- **tools/overlay_mapper** nicht ins CMake eingehängt; **prop_case_resolve.c** (Property 2) nicht in tests/property/CMakeLists.txt.
- **Große ungetrackte Verzeichnisse**: vor erstem `git add .` entscheiden, ob `info/` (3.9GB), `voice/` (939MB), `.agent_refs` (758MB), `re15_port/shared_assets` ge-gitignored werden sollen. Repo wurde initialisiert, aber nichts committet.

**STRATEGIE-ENTSCHEIDUNG 2026-06-27/28 (User) — ENGINE → re15_port (umgesetzt, Phase A):** Die fertige `psx_dev`-Engine wurde nach `re15_port` **verschoben**; re15_port bleibt das Zuhause (Unified-CMake, Tests, Tools, Specs, kompletter Original-CD-Asset-Baum). Begründung (evidenzbasiert, Workflow): psx_dev = lauffähige Dual-Target-Engine (PC **und** PSX via PSn00bSDK), aber liest unvollständige extrahierte Assets (nur 2/240 RDT); re15_port = sauberes Gerüst + vollständige Assets + echter CD-Reader + 28 Tests + Tools. Frühere Notiz „re15_reborn_pc als Basis" ist damit überholt.

Transplantation (in `re15_port`):
- 29 `*_common.c` → `engine/src` (Lib `re15_engine`); 7 `*_pc.c` + `main.c` → `platform/pc`; 11 PSX-Backend-Dateien + `main.c` → `platform/psx` (gestaged, noch nicht gebaut); 33 Header + `shadow_blob_data.h` → `include` / `platform/pc/src`. Alte Minimal-Engine geparkt in `re15_port/_legacy_minimal/`.
- CMake-Fixes: `set(CMAKE_C_STANDARD 11)` (GCC 15 defaultet sonst C23 → bool-Konflikt); Engine-Lib bekommt `RE15_PLATFORM_PC=1` (psx_dev-Quellen schalten `<stdio.h>` per `#ifdef RE15_PLATFORM_PC` — in psx_dev flach kompiliert, hier separate Lib); MINGW-SDL2main-Guard (`if(NOT MINGW)`, da main.c nur `<SDL_timer.h>` inkludiert, kein SDL_main); `RE15_BUILD_TOOLS`/`RE15_BUILD_TESTS` vorerst OFF (alte API).
- Asset-Root nach Verschieben: `RE15_ASSET_ROOT` (env) + `RE15_ASSET_ROOT_DEFAULT` (Compile-String = `${CMAKE_SOURCE_DIR}/../psx_dev/assets_shared`) in `pc_read_shared` (main.c) UND `s_pc_bg_roots` (bg_pc.c — eigener Reader!). ACHTUNG: MSYS-Pfade (`/c/...`) per env scheitern an Windows-fopen; Windows-Pfad (`C:/...`) nutzen.
- **Bauen + Starten:** `cmake -S re15_port -B re15_port/build -G Ninja -DRE15_BUILD_PC=ON; cmake --build re15_port/build --target re15_pc`. Start: `re15_port/run_pc.ps1 [raumhex]` (setzt PATH→mingw für SDL2.dll, Asset-Root, RE15_START_ROOM). exe: `re15_port/build/platform/pc/re15_pc.exe`, Log: dortiges `debug.log` (App macht `freopen("debug.log",...)`).
- **VERIFIZIERT 2026-06-28:** ROOM1170 + ROOM1240 laden RDT+BSS, Engine simuliert (spawn, cuts, frames), 0 BG-Fehler. Alle 240 Original-CD-RDTs flach nach `assets_shared/RDT/` kopiert (byte-identische Kopien, MD5-geprüft) → 238-RDT-Lücke geschlossen.
- **Original-Font (TEX.TIM) gefixt:** Der PC-Subtitle-Renderer (`render_pc.c` `re15_msgfont_ensure`) lädt den Font aus `TEX.TIM` + die proportionalen Breiten aus `DEBUG.BIN` (@0x4416) — beide liegen NUR im Original-CD-Baum `shared_assets/PSX/{DATA,BIN}`, nicht in assets_shared. Pfad via Compile-Default `RE15_CD_ROOT_DEFAULT=${CMAKE_SOURCE_DIR}/shared_assets/PSX` (+ `RE15_CD_ROOT` env). Vorher: TEX.TIM nicht gefunden → Fallback auf synthetischen 6x8-Debug-Font (`s_font6x8`) = „Original-Font fehlt". Font-Dump (RE15_FONT_DUMP=1) bestätigt CLUT (Leon grün/Pilot orange/Elliot blau, FUN_80028868) + Glyphs.
- **ROOM1240-Intro → ROOM1170 Übergang gefixt (2026-06-28):** Das Intro-Cinematic (event 0x18 → thr10: 9 Cuts, 6 Message_on) endet mit `Set(2,7,1)+Aot_on(0)+Evt_end` — KEIN Plc_ret, die Cinematic-Flags (1,27)/(2,7) bleiben absichtlich gesetzt (Hand-off mit aktivem Cinematic). Die main00-Tür `Door_aot_set` hat ein **(0,0)-Null-Rechteck** (nie positions-/forward-reach-erreichbar) → Skript-/Auto-Advance-Tür zu STAGE1/ROOM1170 (dest_stage=0,dest_room=0x17 → dest_id=0x1170). Fix in `aot_common.c` re15_aot_scan: eine Null-Rechteck-DOOR-AOT mit echtem Cross-Room-Ziel (dest_id != current) feuert cinematic-immun + OHNE Action-Press, sobald `in_cinematic && scd_idle` (alle SCD-Threads idle = Intro fertig; schlafende Threads bleiben active). Byte-true-Begründung: Original-Door-Trigger FUN_8002bd44 @0x8002bf38 liest die Script-Cinematic-Flags NICHT (Port-Unterdrückung war die Abweichung). ZUSÄTZLICH gefixt: `room_pc.c` `re15_room_load` (PC-Cross-Room-RDT-Lader) hatte eigene relative `prefixes[]` ohne den verschobenen Asset-Root → Lade-Abbruch; `RE15_ASSET_ROOT_DEFAULT "/"` vorangestellt (wie bei bg_pc/pc_read_shared). VERIFIZIERT: `[room] PC loaded room1170.rdt`, DOOR FIRE=1.
- **VOLLE INTRO-KETTE (2026-06-28):** Echter Flow = **Pre-Intro ROOM1240 → Intro ROOM1170 (Narrator sub11 → Helipad-Cinematic Elliot/Pilot) → Kontrolle an Spieler**. ROOM1170s Intro lebt komplett in 1170: `main00` → `Evt_exec(0x180B)=sub11` (Narrator/Text), dann der `s_preintro`-Handoff in `platform/pc/main.c:747-764` (Narrator-Ende → `scd_room_reenter(scenario 0)` = Helipad-Self-Reentry → sub15→sub02 = Elliot/Pilot-Cinematic → letterbox close → gameplay @F1773). DIESER Handoff war **boot-only** → beim Tür-Eintritt aus 1240 lief 1170s Narrator, aber NICHT das Helipad-Cinematic. FIX: in `main.c` im `re15_room_apply_pending`-Erfolgsblock den s_preintro-Handoff **neu scharfmachen** (Event-Slots nach aktivem Narrator scannen → s_preintro=1, s_sub00_spawned=0, frame_count=0). Verifiziert: nach dem 1240→1170-Wechsel feuert „door-3 self-reentry", Elliot wird animiert (F950), letterbox close @F1773 — identisch zum Direkt-Boot 1170. OFFEN: 1240-Pre-Intro-Text soll auf SCHWARZ liegen (zeigt aktuell den Heli-Platz); Mechanismus im Decompilat nicht gefunden (ggf. DuckStation-Abgleich nötig).
- **SELBST-ENTHALTENE exe (2026-06-28):** `platform/pc/CMakeLists.txt` baut SDL2 jetzt STATISCH (`SDL_SHARED OFF`/`SDL_STATIC ON`, kein `FIND_PACKAGE_ARGS`, link `SDL2::SDL2-static`) + `-static` (gcc-Runtime) → exe (4,95 MB) hängt nur an Windows-System-DLLs, KEIN SDL2.dll/libgcc/PATH/Env nötig. Default-Startraum in `platform/pc/main.c` = `0x1240` (RE15_START_ROOM übersteuert). Doppelklickbar: `re15_port/build/platform/pc/re15_pc.exe` (schreibt `debug.log` daneben; App macht `freopen`). `run_pc.ps1` braucht `-ExecutionPolicy Bypass` (User-Policy blockt .ps1) — exe direkt ist sauberer.

Build-Recipe siehe [[reai-v2-build-recipe]]. Skills wurden nach `.claude/skills/` portiert (siehe [[reai-v2-skills-ported]]).
