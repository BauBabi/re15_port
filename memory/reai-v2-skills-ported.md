---
name: reai-v2-skills-ported
description: Die .kiro-Skills wurden nach .claude/skills/ portiert und sind als Claude-Code-Skills nutzbar
metadata: 
  node_type: memory
  type: project
  originSessionId: 073aec74-f763-4c50-aa34-afe1225f60d0
---

Die 5 Domänen-Skills lagen ursprünglich als reine Kiro-IDE-Markdown unter `.kiro/skills/` (kein YAML-Frontmatter → von Claude Code NICHT als /slash-Skill nutzbar). Am 2026-06-27 nach `.claude/skills/<name>/SKILL.md` mit Frontmatter portiert und damit als Claude-Code-Skills aufrufbar: `rdt-analysis`, `scd-disassembly`, `ghidra-mapping`, `build-workflow`, `asset-pipeline`.

Dabei 4 tote Referenzen korrigiert: ghidra-mapping lädt den Katalog jetzt aus Repo-Root `RE15_FUN_CATALOG.md` (vorher falsch `psx_dev/...`); asset-pipeline: `extractors/pld/PldExtractor.java`, `extractors/MD1Extractor.java`, EDH-Format → `RE15_KNOWLEDGE.md §1.9`, nicht-existentes `scripts/extend_vab.py` entfernt.

Die `.kiro/skills/`-Originale wurden am 2026-06-27 ebenfalls auf dieselben korrigierten Referenzen gebracht (deckungsgleich mit den `.claude/skills`-Kopien). Siehe [[reai-v2-build-recipe]] und [[reai-v2-open-gaps]].

**Seither dazugekommen (RE-Tooling-Skills, .claude/skills/):** `re15-room-capture` (DuckStation-Raum laden + provoke/menushot/quickload — vgamepad-getrieben), `re15-savestate-ghidra` (Savestate-RAM × Ghidra-Mechanismus; inkl. `re15_enemy_state.py` Enemy-AI-Decoder + **`re15_flag_sweep.py`** [2026-06-29 NEU — Dormanz-Check: ein Gate-Bit über ALLE Saves sweepen; bewies dass `DAT_800aca3c & 1` nie gesetzt = der Lunge-Arm dormant, Phase 8.7], §7 der Skill), `re15-psx-disasm` (2026-06-29 — MIPS-R3000-Direkt-Disasm „Weg C"; `scripts/re15_disasm.py` dis/table/scan/bytes/**read** [`read` 2026-06-30 NEU — typisiertes Daten-Tabellen-Lesen: `--w 1|2|4 --signed --stride N --rows R --rowstride S`; der byte-true Dumper für Damage-/Reach-/Reaktions-/Hitbox-Tabellen mit korrektem EXE-File-Mapping. Erzwungen in Phase 8.10: die Player-Waffen-Damage-Tabelle @0x8006e0d0=`u16[type*0x58+weapon*4]` — ein Workflow-Agent scheiterte am selbst-gerechneten Mapping → Garbage; `read` nutzt den `load()`-Helfer = immer korrekt. Skill-Sektion „Großes Subsystem RE'en (Workflow-Fanout)" ergänzt: Workflow-Agenten = schnelle Breite, ABER jeden Befund gegen Bytes+Savestate verifizieren]) und **`re15-room-probe`** (2026-06-29 — Port-seitige Dynamik OHNE DuckStation: echtes RDT laden + SCD/AI ticken als ctest; Vorlagen `test_room1140_spawn.c`/`test_room1140_combat.c`). **2026-06-30 — re15-room-capture LAUFFÄHIG + DAUERHAFT (Nutzer-Feedback: Capture-Wissen muss durable sein, getestet, niemals wieder fragen):** eingecheckte **Debug-Menü-Quickload-Base `stage_saves/mzd_debugmenu.sav`** — Quickload landet in **~16s direkt im Menü**, voller Raum-Load **~36s** (statt ~110s Boot). `re15_quickload.py --state` defaultet darauf (zero-config), `--triangle` ergänzt. **NAVIGATION VERIFIZIERT lauffähig:** Debug-Menü-Sequenz (Nutzer-bestätigt) = man landet OBEN → Down zu JUMP → Links/Rechts kurze Einzeldrücke = Raum ±1 → Square laden; Dreieck = Stage. ⚠️ **KRITISCHER FIX: nach `-statefile`-Quickload wird ein blankes Rechts IGNORIERT** — die Menü-Eingabe muss erst per Nav-Taste RE-AKTIVIERT werden; `re15_quickload.py` macht jetzt Up×2→Spitze, Down→JUMP, *dann* steppen (= die frische Nav des Boot-Treibers). Befunde: r0=124 OPENING(=ROOM1240/Boot/Base), 125 LOBBY, 126 SEWER PASSAGE, --left10=0x11A SEWER EXIT (HEX!), --right3=100 BATH-LOCKERS (Square-Load bestätigt, 96% non-black). **Cross-Stage bewiesen:** je ein Raum Stage 1/2/3 geladen (--triangle 1/2), Overlay-Diff @0x80100000 Stage1↔2=87.9%/↔3=85.5%/2↔3=88.2% (verschieden), Stage1↔Base=0.0% (Sanity); Cross-Stage braucht --postload≥16s. **Debug-JUMP-Nummern sind HEX. ROOM1140 = 0x114 BRIEFING = `--left 16`** (⚠️ KORREKTUR eines früheren Tagesbefunds, 2026-06-30: 0x114 IST ein sauberer JUMP-Raum, der die 5 Briefing-Zombies via Spawn-SCD lädt; der frühere „KEIN JUMP-Raum / 114=SEWER EXIT / 0 Zombies / nur geskriptet"-Befund war **FALSCH** — geladen war 0x11A SEWER EXIT = `--left 10`, ein 6-Schritte-Lesefehler 0x11A↔0x114). **Live-Engage-Capture (player alive, alle 5 wach):** `re15_quickload.py --left 16 --postload 10 --path "R0.5,U2,R0.3,U6"` → Artefakt `stage_saves/mzd_stage1_engage_live.sav`. `--path` L/R DREHEN (Tank-Control, kein Strafe); R = Bildschirm-links zu den Zombies (linke Gasse hinter dem Tisch); zwei-R robuster als R-dann-L. HURT-Capture braucht zusätzlich Feuern (R1+Square; `--provoke`/`--path` drücken kein Square). **Neu 2026-06-30:** `re15_runtime_table.py` (Cross-Check einer Pointer-Tabelle statisch↔Savestate-RAM) + `re15_enemy_state.py --ai` (dumpt +0x90/rot/motion/hurt_clip/stun + wertet das +0x5=0x13-forward-walk-Gate aus). ⚠️ KORREKTUR: die @0x8011f840/890-Dispatch-Tabellen sind NICHT runtime-gepatcht — sie stehen STATISCH in STAGE1.BIN (`re15_disasm.py table` liest sie korrekt, Overlay-Offset = `addr−0x80100000` OHNE 0x800-Header); 40/40 == RAM. Ein früherer „on-disc NULL/runtime-patched"-Befund war ein ad-hoc-Offset-Bug (`+0x800` für ein Overlay). Siehe [[reai-v2-re-pitfalls]]. Voraussetzungen vorhanden: DuckStation-EXE, MZD-Disc (`…/Downloads/ePSXe2018/Biohazard 1.5 (MZD Mod)…cue`), vgamepad+zstandard.
→ Vor manuellem RE: erst `ghidra-mapping`/Katalog, dann `re15-psx-disasm` (statisch) bzw. `re15-savestate-ghidra`/`re15-room-probe` (dynamisch). KEINE Scratchpad-Disassembler/-Reader mehr neu bauen — die Skills ersetzen die `/tmp/dis*.py`/`leafan.py`/ad-hoc-Sweeps der AI-Sessions. Combat-RE-Workflow: Live-Save dumpen (`re15_enemy_state.py`) + Gate-Bits sweepen (`re15_flag_sweep.py`) → bestätigten Pfad byte-true portieren → port-seitig ctest (`re15-room-probe`) → final gegen Savestate. Siehe [[disasm-verify-decompiles]].
