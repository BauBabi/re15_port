---
name: reai-v2-foundation-combat
description: "Foundation-Phase (Gegner-AI/Combat): RE'te Kern-Primitive + Code-Stand + Integrations-Plan für den mehrstufigen Build, der #13-Trigger/#14/#15/#19/#22 entsperrt"
metadata:
  type: project
  originSessionId: 2fc24f5a-47cf-487a-9546-6de01c8a2791
---

Die **Foundation-Phase** (Nutzer-gewählt 2026-06-29) = der deferred Block, an den #14/#15/#19/#22 + der #13-In-Game-Trigger koppeln. Großer mehrstufiger Build; hier der Stand + Plan. Für [[reai-v2-byte-true-audit]], [[reai-v2-aot-mechanism]].

**PHASE 8.15 (2026-06-30, C11 DYNAMISCHE VERIFIKATION — TEIL-erledigt).** Alles seit 8.11 war nur statisch RE't → Disziplin verlangt den Live-Check. Capture-Pipeline (DuckStation + MZD-Disc + vgamepad) läuft aus der Sandbox (re15-room-capture). **Bestätigt:** ENGAGE-Clip + Hitbox byte-true gegen `stage_saves/mzd_stage1_combat_death.sav` (slot 2 = `+0x4=1,+0x5=2 ENGAGE, hitbox 400/1440` = MATCHT die Port-Vorhersage engage→motion=+0x1d4 variant [8.13] + Hitbox 400/1440 [8.7a]); die übrigen Slots (+0x5=1 search/6 walk/12 dead-grab) = 8.10-Post-Death-Artefakte. **Durable gemacht (Nutzer-Feedback):** eingecheckte Debug-Menü-Quickload-Base `stage_saves/mzd_debugmenu.sav` (~41s statt 110s, re15_quickload --state-Default) + SKILL-Doku [[reai-v2-skills-ported]]. **C11-Capture: der BRIEFING-RAUM gefunden + player-alive gesichert (2026-06-30, Nutzer-korrigiert).** Der Port-ROOM1140 = **`0x114 BRIEFING ROOM`** im Debug-JUMP = **`--left 16`** von der Base — **die JUMP-Nummern sind HEX!** CLEAN JUMP lädt die **5 Briefing-Zombies** (0x10/0x11/0x16, Hitbox 400/1440, slot0=0x16 grid 0x88 lying) — matcht den Port exakt. Artefakt `stage_saves/mzd_stage1_briefing.sav`. **MEINE FEHLER (korrigiert):** Hex `0x11A` als „114" gelesen + 6 Schritte zu kurz → lud `0x11A SEWER EXIT` (1× 0x40), daraus fälschlich „Briefing nicht per JUMP/geskriptet". FALSCH — der Briefing IST ein JUMP-Raum (0x114). Auch live: Elliot 0x47 Hitbox 450/1530 (ROOM1170). **✅ LIVE-ENGAGE GESICHERT (2026-06-30):** Weg `re15_quickload --left 16 --postload 10 --path "R0.5,U2,R0.3,U6"` → `stage_saves/mzd_stage1_engage_live.sav`. Alle 5 Zombies geweckt (flags 0x0→0x10, dist <~3.5k), **Spieler lebt** (HP 100): slot1 **+0x5=2 ENGAGE**, slots 2/3 (nächste ~1.6k) **+0x5=0x13**, slot0 (lying) +0x5=0 (search). Tank-Control-Lehre: `--path`-L/R DREHEN (kein Strafe); `R`=Blick nach Bildschirm-LINKS zu den Zombies (linke Gasse hinter Tisch); zwei-R-Umweg statt R-L (Gegen-Dreh überkorrigiert). Skill [[reai-v2-skills-ported]] hat den Weg jetzt fest.
**⚠️ NEUER BYTE-TRUE BEFUND — +0x5=0x13 (FUN_8010561c, Dispatch @0x8011f840[0x13]; ⚠️ KORREKTUR: die Tabellen sind NICHT runtime-gepatcht — sie stehen STATISCH in STAGE1.BIN, 40/40 == RAM; der frühere „on-disc null"-Befund war ein Overlay-Offset-Bug `+0x800`, siehe [[reai-v2-re-pitfalls]]):** disasm-verifiziert = **engage (FUN_80102058) + vorangestellte `+0x90&3`+Winkel-Forward-Walk-Prüfung**. Der turn/grab/dead-grab/fallback-Teil ist BYTE-IDENTISCH zu engage (gleiche Thresholds 0x7d0/0x4b0/0x5dc, arc 0x2c8/0x200, State-Wörter 0x701/grab(facing+3)<<8|1/0xc01/0x1001). → frisch geweckte NAHE Zombies landen real in 0x13. **ABER der Forward-Walk-Zweig ist GEGATED durch `+0x90 & 3` — und im Live-Save ist `+0x90 == 0x00` für ALLE 5 Zombies (auch die zwei in 0x13).** Also wird der Forward-Walk NICHT genommen; 0x13 fällt durch zur engage-identischen turn/grab-Logik → **0x13 verhält sich für ROOM1140 byte-IDENTISCH zu engage(+0x5=2). Der Port (wake→engage 2) ist damit verhaltens-byte-true faithful — NICHTS zu portieren.** Das BESTÄTIGT §8.10 TEIL 1 („Forward-Walk = andere-Konfig, KEINE ROOM1140-Lücke"), nur mit präziserem Grund: nicht „0x13 nie erreicht" (0x13 IST live erreicht), sondern der Forward-Walk in 0x13 ist via +0x90 dormant (ROOM1140 setzt +0x90 nicht). Forward-Walk-Port nur nötig falls je ein Raum mit `+0x90&3`-Zombies drankommt (Reader re15_emd_get_keyframe_speed +6/+10 + m0-Walk-Handler +0x5=9/10 @decide 0x801031a4/0x801033c0 existieren). **C11 für ROOM1140 ist damit VOLL geschlossen: wake→engage→turn→grab→damage→death live-verifiziert byte-true.** Lehren: **Debug-JUMP-Nummern sind HEX; Overlay-File-Offset = `addr−0x80100000` (KEIN 0x800-Header — den Offset nie selbst rechnen, `re15_disasm.py` nutzen; ad-hoc `+0x800` las die Tabellen fälschlich als null = der „runtime-patched"-Irrtum); ein State zu ERREICHEN ≠ sein Verhalten ist aktiv (immer die Gate-Flags wie +0x90 im Live-Save prüfen, bevor man ein Verhalten portiert).** Siehe HANDOVER §8.15 + [[reai-v2-re-pitfalls]]. [[disasm-verify-decompiles]]

**PHASE 8.14 FERTIG (2026-06-30, PLAYER-AIM/FIRE-ANIMATION byte-true, Commit b3379f71, 32/32 ctest) → zwei-seitiger Combat BEIDSEITIG sichtbar.** RE'd via Workflow `player-aim-anim-re` (4 Finder + adversariale Verify, 16 Agenten). **KORREKTUR (Workflow-bewiesen, gegen meine Prämisse): die Aim/Fire-Anim nutzt NICHT PL00W01, sondern die gemeinsame PL00.EDD-Bank** (anim_set a1=DAT_800acbc0). action-8-Aim-Sub-FSM `@0x80035810` schreibt Player-motion entity+0x94=0x800acae8 (**Player-Block-Base 0x800aca54 bewiesen @0x8001d0a0** `sw DAT_800aca54,0(0x800ac784)`): phase2 RAISE=clip 0x11=17 (25f), phase4 AIM-READY + phase5 FIRE=clip 0x12=18 (20f @0x800359e0; Discharge re-spielt clip 18). **Port:** re15_player_tick — R1 halten → ROOT (keine Translation, Turn bleibt) + `p->motion=18` DIREKT (echter PL00-Clip, KEIN Sentinel → anim_select überspringt W01/idle-Remap → clip 18 HOLD-LAST = gehaltene Aim-Pose); game_step — Square-Fire resettet `pl->anim_frame=0` → clip 18 re-spielt = Recoil, selber Frame wie Damage. **Kein anim_select-Edit nötig** (motion=18 echter Clip; RBJ-loser Raum def_*==PL00). test Part(16). **DEFERRED/faithful-line (geflaggt):** exakte 3-Level-Command-FSM (**echter Raise = HELD pad 0x100 + Waffe DAT_800aca5d, NICHT Square** — C8 refuted; Port-R1/Square faithful-line); RAISE-Clip 17 (rate-aware Anim-Completion fehlt); Aim-Elevation-Pitch entity+0x66; Waffen-Inventory; Muzzle-Flash; C7 (FUN_80045024→FUN_80011f50 intern) UNVERIFIED non-blocking. **Workflow-Lehre: clip 18 muss HOLD-LAST (nicht loopen wie die Sentinel-Clips) → motion=Clip-Index DIREKT statt Sentinel (umgeht den loopenden clip_override).** [[disasm-verify-decompiles]]

**PHASE 8.13 FERTIG (2026-06-30, PER-STATE ANIMATIONS-CLIPS engage/turn/grab byte-true, Commit 475093f7, 32/32 ctest).** RE'd via Workflow `enemy-anim-clips-re` (5 Finder + adversariale Verify, 18 Agenten). Jeder Live-State spielt jetzt seinen byte-true `+0x94`-motion-Clip → der Zombie animiert durch den GANZEN Combat (feeding→wake→ENGAGE→TURN→GRAB→HURT/DEATH). **ENGAGE** (+0x5=2, animate @0x801021f8): `motion=+0x1d4` variant {2,3,4,5} (= derselbe Clip wie Hurt/Turn — die {2,3,4,5} sind die 4 per-Spawn-Idle-Varianten). **TURN** (+0x5=7, FUN_80102dc8): bei Entry (`+0x6==0`) `motion=+0x1d4`+`anim_frame=0` (@0x80102e00/08/18). **GRAB** (+0x5=3/4, FUN_80102548): per Sub-Step `+0x94=(+0x5−3)*3+{0,1}` → Face {0,1}, Behind {3,4} (@0x801025bc/0x80102714); RELEASE=Literal 17 (@0x80102a64). `re15_ai_set_state_word` resettet +0x6 aus den State-Word-High-Bytes (0x701/0x301/0x401/0x201→+0x6=0) → Turn/Grab-Entry-Latches sauber. **REFUTED/deferred (ehrlich):** Init schreibt `g_entity+0x94` NICHT (das einzige sh-to-148 trifft `*(entity+0x188)+0x94` = bbox-Sub-Record, NICHT die entity-motion — die „init=motion 0"-Annahme war FALSCH); Feeding-Liege = inferred clip-0 (struct-zero); Stand-up-Clips 27→28→29 (@0x80103c20/d7c/f44) RE'd aber Ramp deferred (Anim-Dauer-Timing); Forward-Walk (+0x5+4=9/10) + Search (0/1) RE'd aber m0 nicht live erreicht → optional. test Part(15). **Damit ist die sichtbare Gegner-Combat-Animation für ROOM1140 KOMPLETT.** [[disasm-verify-decompiles]]

**PHASE 8.12 FERTIG (2026-06-30, HURT-STAGGER-ANIM + HIT-STUN byte-true, Commit 2f3cec99, 32/32 ctest).** RE'd via Workflow `hurt-stagger-re` (5 parallele Finder, jeder disasm-verifiziert, adversariale Refutation jeder Behauptung, Spec-Synthese — 18 Agenten; JS-Klammer-Bug `(await parallel(...).map(...))` statt `((await parallel(...)).map(...))` abgefangen + per resumeFromRunId aus Cache fortgesetzt). **`FUN_80105a8c` = ROUTER:** testet `+0x9&0x80`; NORMAL dispatcht 2D-Tabelle `@0x8011fb90[+0x5*8++0x6]` (Live-Stagger `0x80105b7c` für +0x5∈{1,3,4}) → EXIT-GATE `@0x80105b18`. Stagger phase 0 kopiert `+0x1d4→+0x94`(motion); EXIT recovered zu ACTIVE wenn `s16 +0x1dc<0` (**NICHT** Clip-Ende — anders als Death!), setzt +0x5=0x11,+0x6=0. **Hit-Stun +0x1dc = eigenständiger Timer** (NICHT Clip-Länge — meine frühere 8.11-Annahme war FALSCH): Init-Seed `(rng&3)+4`=4..7 (@0x80100838), Dekrement/Frame `step[+0x5]` aus `@0x8011fe30={0,-2,-2,-3,-3,-3,-3,0,0,0,0,0}` (Pistole +0x5=2→−2, Melee-React +0x5=3→−3). **Hurt-Clip +0x1d4** = zufällig {2,3,4,5} (Init @0x8010079c, Tabelle @0x8011f7e4). **Port:** neue dedizierte Felder `hurt_clip`(+0x1d4)+`hit_stun` — **NICHT `ai_target_x` (+0x1dc) wiederverwenden** (Original time-shared +0x1dc mit ai_target_x; Workflow-Must-Fix). live_init seedet Clip; re15_enemy_ai_live_hurt (ersetzt Snap-Stub) = kanonischer Stagger (motion=hurt_clip, halten via Stun-Timer, Recovery). +0x9&0x80-Special = dormanter Stub. **DOKUMENTIERTE UNSICHERHEITEN (kein Raten):** (a) Port re-seedet Stun per Hit (Original 1×Init+time-share = faithful-line); (b) Gunshot +0x5=weapon_id(Pistole 2) → Original-Stagger-Row für +0x5=2 ist NULL (C4) + kein Savestate bezeugt HURT (C11) → Port spielt verifizierten kanonischen Stagger (Step[2]=−2 byte-true); exakte Original-Pistolen-Reaktion = Verifikations-Schuld (Capture). **Workflow-Lehre: Ultracode-RE via Workflow (Finder→adversarial-verify→synth) liefert byte-true Spec; aber Port-Struct-Aliasing (+0x1dc=ai_target_x) muss VOR dem Coding aufgelöst werden.** [[disasm-verify-decompiles]]

**PHASE 8.11 FERTIG (2026-06-30, ENEMY-MODELL-LADEN + DEATH-ANIM — die Anim-Präsentation ist ENTSPERRT, Commits 0e2203a8/91f7d20b, 32/32 ctest).** Schlüssel-Befund: „die Anim-Schicht" war NICHT der Blocker — der Port hat sie (anim_select_common.c + re15_compute_actor_kf + per-Actor anim_frame-Advance in player_common.c:317-329 + Hold-Last). Der echte Blocker: die GEGNERMODELLE wurden nie geladen — sie liegen NUR in `EMD/CDEMD0.EMS`/`CDEMD1.EMS` (~4,7 MB), KEINE per-Typ `EM<NN>.EMD`; `pc_enemy_load` las `EM%02X.EMD` → scheiterte für JEDEN Typ → kein Gegner mit Modell → keine Anim. **EMS-Loader (0e2203a8):** engine-seitig `re15_ems.c` = byte-true Port von `RE15MasterExtractor.parseEmsEntries`+`DEFAULT_EMS_ORDER` (EMS = Folge EMD-Blobs: `u32 dir_off` öffnet jeden, gültig ⟺ `>=36 && (&3)==0`, Länge `dir_off+36`, 2048-aligned, Zero-Gaps skip; Order em10=0x10@0/em11@1/em16@4/…/em47=Elliot@21). `pc_enemy_load` extrahiert das Typ-Blob aus gecachtem CDEMD0.EMS → privater Bank-Buffer → `re15_emd_parse_container`. ctest `test_ems` gegen echtes Asset: Offsets byte-true, em16-Zombie = **15 Meshes/15 Bones/43 Clips, Death-Clip 0x1f=73 Frames**. **Death-Anim (91f7d20b):** `re15_enemy_ai_live_death` = byte-true `FUN_80107cb0` (3-Phasen +0x7: motion=0x1f spielt aus → Corpse bei Clip-Ende, statt sofort-Corpse; SE@Frame7+Gore@Frame35 deferred; Headless-Fallback ohne Bank=sofort-Corpse) + `+0x7=0` in re15_player_weapon_fire (L157). **NÄCHSTER SCHRITT entsperrt: HURT-Stagger-Anim + Hit-Stun** — `FUN_80105a8c`: Stagger-FSM @0x8011fb90 (sparse, Row1→0x80105b7c setzt motion=`u8@+0x1d4`/Anim-Felder), zurück zu ACTIVE erst wenn `s16@+0x1dc<0` (=Anim-Frames-verbleibend=Stagger-Clip-Länge, in Anim-Wiedergabe geseedet) → wie die Death-Anim modellieren (Hurt-Clip +0x1d4-Quelle am Spawn ermitteln). In-game-Sicht: GUI-Run `RE15_START_ROOM=1140`. Siehe HANDOVER §8.11. [[disasm-verify-decompiles]]

**PHASE 8.10 TEIL 8 FERTIG (2026-06-30, Reach-Cone byte-true, Commit 7d3e305b, 31/31 ctest) — die INFRA-FREIEN Combat-Refinements sind damit AUSGESCHÖPFT.** Der per-Waffe Cone-Tester `PTR_LAB_8006e548[weapon]` (@0x80011fec) disasm'd: für ALLE Gun-Waffen einer von zwei **byte-IDENTISCHEN** reinen-2D-Testern — `FUN_800127fc`(Pistole w1/w2)+`FUN_800128a0`(Magnum/Shotgun): `dist=SquareRoot0(dx²+dz²)` vom Aim-Point, `R=(u16@hbdata+6=hit_radius_min)+(reach&0xffff)`, Treffer ⟺ `dist<R` strikt UND `dist<DAT_8008f5e0` (Min-Latch). Der Enemy-Hitbox-Radius (hbdata+6, Zombie=400) wird zur Reach ADDIERT. Der Port hatte den Radius (`hit_radius_min`), ließ ihn weg + nutzte `<=` → effektive Pistolen-Reichweite 999 statt 1399. **FIX** (re15_damage.c): `R=reach+(hit_radius_min&0xffff)`, strikt `dist<R`. test Part(12). **PROVEN-BLOCKER (byte-true belegt, für die nächste Session — NICHT denselben Tunnel graben):** (a) die Dist-ORIGIN bleibt faithful-line (Spieler statt des **Aim-Points** param_2 = 16-bit Welt-SVECTOR, den die Aim-FSM @0x80035810 nach vorn projiziert — Port hat die FSM nicht; `arc_test(0x400)` bleibt Richtungs-Stand-in für Aim-Point + Kamera-Line-of-fire @0x80012280–0x80012370 = DAT_800ac784 Kamera + DAT_800aca88/8c/90 Aim-Ray); (b) **Hit-Stun-Dauer**: HURT `FUN_80105a8c` kehrt nur zu ACTIVE wenn `s16@+0x1dc<0`, aber +0x1dc wird in der Stagger-Anim-FSM @0x8011fb90 (SPARSE: nur Row1[0,1]→0x80105b7c) tief im Anim-System geseedet (+0x188/+0x8c/+0x94/+0x9e) — Port hat kein Enemy-Anim-Playback → Anim-Detail, deferred; (c) Hit-Dir-Clip `+0x6=DAT_8006f410[heading>>0x1d]={7,0,1,7,2,0,0,0}` braucht DAT_800acaec+Aim-Z; (d) Waffen-Inventory DAT_800aca5d braucht Inventory-System. **Aim-Mode-Alt-Reach-Row @+0x58 ist byte-IDENTISCH zur Base → nichts zu portieren.** → Der Combat-KERN ist beidseitig byte-true KOMPLETT; was bleibt ist auf fehlende Infra geblockt (Aim-FSM/Kamera/Enemy-Anim) oder ein neues Subsystem. Siehe HANDOVER §8.10 TEIL 8 + [[disasm-verify-decompiles]].

**⚠️ GRAVIERENDE KORREKTUR (2026-06-29, savestate-PROVEN, Commit c3e577e7) — der portierte AI-ROOT ist die FALSCHE Typ-Tabelle.** Die Phase-2-7-AI (`re15_enemy_ai_*` = `FUN_8011d6d4`→`@0x801217a0[+0x4]`→`FUN_8011d9f4`/`da48`) ist die **Typ-0x47**-Tabelle. Die LIVE-STAGE1-Zombies (0x10/0x11/0x16) nutzen eine PARALLELE Tabelle: die Per-Frame-Loop `FUN_8001a50c` (@0x8001ce04, stride 0x1f4 über DAT_800acc2c) ruft `@0x80072bac[+0x8 type]` = für 0x10 **`FUN_80100424`** → Dispatch `@0x8011f7b4[+0x4]` → active root **`FUN_80101224`**. `FUN_80100424` ist KEIN Spawn-Konstruktor (das `STAGE1/FUN_80100424.c`-Decompilat ist FALSCH/fehlanalysiert — raw STAGE1.BIN + Live-RAM beweisen: dist-cache-Tick). Phase-4s „Live-Pfad=FUN_8011d9f4" = falsche Inferenz (savestate-widerlegt). **GERETTET:** die Lunge/Hitbox/Damage/Attack-Point-Schicht (Phase 6-7) IST geteilt+korrekt — `FUN_80101224` enthält den byte-true Lunge-BEGIN (`+0x1da==300→FUN_80019d50(8,3,0x16)` über LUT `@0x8011f7a4`); atk_pt-Mapping (8.1) + room1140-Spawn (8.2) bleiben gültig. **NÄCHSTER PFAD: die `@0x8011f7b4`-Familie (FUN_80100424/FUN_80101224/…) DIREKT aus STAGE1.BIN portieren** (Skill re15-psx-disasm; NICHT die .c trauen). Der 0x47-`@0x801217a0`-Code bleibt als echter Parallel-Typ erhalten, klar gelabelt. Details: RE15_FUN_CATALOG „HIGH-VALUE CORRECTIONS" + HANDOVER §8.3/8.4.
**PHASE 8.5b/c FERTIG (Commits a8880747/e6df8260, 30/30 grün):** 8.5b = `re15_enemy_ai_live_tick` (FUN_80100424: gates+dist@+0x1d0+Dispatch @0x8011f7b4[+0x4]) + `re15_enemy_ai_live_step` (Tick+geteilte Lunge-Slice = game_step-Entry) + Full-Chain-Test `test_live_step_chain` (live spawn→tick→arm→Lunge@300→Hitbox→HP 100→90). 8.5c = der un-armed Tail von FUN_80101224 (@0x80101560+) dispatcht `@0x8011f80c[+0x9&0xf]`; **SCHLÜSSEL: `@0x8011f840 == &@0x8011f80c[13]`** → Phase-3s Brain (FUN_80101b64/de4/2058) ist ein SUB-VIEW = der LIVE-Decision-Graph (via @0x8011f80c[0]=FUN_8010168c→@0x8011f840[+0x5]), NICHT „System B parallel" (Fehllabel korrigiert). Combat-Zombies (+0x9=0) routen dahin → `re15_enemy_ai_live_active` un-armed (sub 0) ruft jetzt `re15_ai_dispatch_decision`. **8.5d FERTIG (Commit b0b973f9): der ATTACK-ARM** = `FUN_8010ab2c` (disasm-VERIFIZIERT; sein `.c` ist KORREKT — das falsche FUN_80100424.c hatte dessen Pose-Body geborgt). Attack-Commit/Arm: Arc `+0x5fc=0x390` (live, ≠0x2c8); gated `DAT_800aca3c&1` → `+0x1d8|=0x100` + `(rng&1)<<9` + Seed `+0x1da = rng&0xff+rng&0xff+600` (600..1110→300=Lunge). `re15_enemy_ai_live_arm`+`re15_enemy_ai_set_combat_active`, eingehängt: Brain committet (+0x5=7)→arm→Windup→Lunge@300. **DIE VOLLE BYTE-TRUE LIVE-ATTACK-KETTE LÄUFT JETZT AUS EINER ENTSCHEIDUNG** (Tick→INIT→ACTIVE→Brain→arm→Windup→Lunge@300→geteilte Hitbox→HP fällt), korrekte @0x8011f7b4-Familie. test_live_decision_arm.
**PHASE 8.6 FERTIG (2026-06-29, game_step-WIRING — die Live-AI läuft jetzt IM Spiel, 31/31 ctest grün):** Neue geteilte `re15_enemy_ai_run_all(combat_active)` (enemy_ai_common.c) = die TYP-gegatete Slice des Original-Per-Frame-Entity-Loops **FUN_8001a50c** (@0x8001ce04: iteriert die Entity-Liste, dispatcht `@0x80072bac[type]`) — tickt NUR 0x10/0x11/0x16 durch `re15_enemy_ai_live_step` (Elliot 0x47/Krähen 0x21/Player/Props unberührt). Eingehängt am ENDE von `re15_game_step` (gegated `c->rdt_ok`; nach Player-Move/Collision+AOT = Player-Pos settled, Lunge-EXEC nach AOT = byte-true zu FUN_80019e20 @0x8001ce2c). **1170-sicher VERIFIZIERT:** ROOM1240-Boot+ROOM1170 spawnen keine Live-Zombies → run_all No-Op (Headless-Boot unverändert); ROOM1140-Headless tickt die 5 Zombies sauber (kein Crash).
- **combat_active byte-true (3 Agenten trianguliert + selbst-disasm-verifiziert):** `DAT_800aca3c & 1` (Arm-Gate `FUN_8010ab2c` @0x8010a4f0 `andi v0,v0,0x1`) = Flag-Bank 1 (`PTR_DAT_80074664[1]`), gesetzt via SCD `Set`(0x22) auf Bank 1/Bit 31 (runtime-Maske `0x80000000>>31` → KEIN literaler `ori …,0x1` existiert in EXE/Overlays), gehaltener Latch, beim Raum-Load gelöscht (`FUN_800396fc &= 0xffff0000`). Im Port = neues **`g_scd.combat_active`** (dediziertes Bool wie die Schwester-Bit 0x100 = cut_auto_enabled; g_scd-memset beim Raum-Reenter/Init = byte-true Raum-Load-Clear gratis). **OFFEN (nur per Savestate klärbar): ob der Prototyp das Bit je SETZT** — wenn nie, ist das Arm-Gate dormant + die Briefing-Zombies ein reines Feeding-Set-Piece. Default 0 ist byte-true in beiden Fällen.
- **Port-seitig verifiziert (Skill `re15-room-probe`):** neuer ctest `test_room1140_combat` — lädt echte ROOM1140, sub00 spawnt 5 Zombies, treibt run_all: (1) alle 5 ticken INIT→ACTIVE + dist gecacht, (2) greifen als gespawnt NICHT an (feeding/lying `grid_id & 0xf`=6/8 ≠ 0 → Combat-Brain nicht betreten, auch mit combat_active=1), (3) ein manuell armed Zombie feuert durch run_all die Lunge → Player −10 HP + hurt(2), (4) ein 0x47-Actor typ-gegated unberührt.
**PHASE 8.7 FERTIG (2026-06-29, Savestate-Ground-Truth + Attack-Modell KORRIGIERT + Wake-up portiert, 31/31 ctest):** Aus dem Live-Combat-Save `stage_saves/mzd_stage1_combat_death.sav` (Skill `re15-savestate-ghidra` `re15_enemy_state.py`) + 9-Save-Sweep + RE (Agent + Selbst-Disasm-Verify):
- **8.7a (Commit 0283bd2c):** Hitbox 0x10/0x11/0x16 = **400/1440** byte-true aus dem Live-RAM (`*(entity+0x78)`) in re15_enemy_apply_hitbox gewired (fielen vorher durch zu „keine Hitbox").
- **⚠️ GROSSER BEFUND — der LUNGE-ARM ist DORMANT, die Zombies greifen per GRAB an.** `DAT_800aca3c & 1` (Arm-Gate FUN_8010ab2c @0x8010a4f0 `andi v0,v0,0x1`) ist über ALLE 9 Savestates (inkl. Live-Combat mit 7 Zombies) **nie** gesetzt; ROOM1140-SCD macht nur `Set bank1 bit27` (Maske 0x10=bit4), **nie bit31** (= das Combat-Bit, 0x80000000>>31 = 0x1). Spieler stirbt state 7 = *grabbed*. → Echter In-Game-Kill = der **GRAB**: engage FUN_80102058 (+0x5=2, schon portiert als re15_ai_decide_engage) committet bei dist<0x4b0 && arc(0x200) && same-floor && facing den Grab (+0x5=3/4) → `FUN_80102548` (@0x8011f890[3/4]): Player-Register `0x800aca58=(+0x5−3)<<8|5` + player+0x93|=1 (grabbed) + **−5 HP/Biss** direkt auf player+0x9a (wenn Bite-Anim-Frame landet, anim_set a3=0x200), umgeht FUN_80012d60, KEIN DAT_800aca3c-Gate. Selbst-disasm-verifiziert (0x80102620/40/0x801027cc-e4). 8.5d-Lunge-Arm bleibt byte-true als Mechanismus, ist dormant.
- **8.7b Wake-up portiert:** feeding `@0x8011f80c[6]=0x801018f8` (Stage-A 0x80103980: `dist<0xfa0(4000)` && +0x6==0 → +0x6=1, +0x9c=rng&0xf; +0x6-Machine 0x80103a58: 1=Timer→2=Stand-up-Anim→3=COMMIT 0x80103b3c: +0x9=0, +0x4=0x201=state1/+0x5=2 engage, +0x93&=~1). Lying `[8]=0x80101974`=passiv (leeres jr ra). `re15_enemy_ai_live_feeding` + Sub-Mode-Dispatch in re15_enemy_ai_live_active. Faithful-line: Stand-up-Anim-Dauer deferred (2→3 direkt); Phase-0-Busy-Writes (+0x93/+0x1b8) deferred (Feld-Aliasing). test_room1140_combat Part(5): feeding-Zombie nah → wacht zu engage auf, ferner schläft.
**PHASE 8.8 FERTIG (2026-06-29, GRAB-Execution portiert = der eigentliche In-Game-Angriff landet, 31/31 ctest):** `FUN_80102548` (@0x8011f890[3/4]) vollständig byte-true RE't (Agent + Selbst-Disasm-Verify): 9-Sub-Step-Machine, dispatched auf entity+0x6 (Jump-Table @0x80100024; Grab-Commit-Word 0x301/0x401 resettet +0x6=0). f840[3/4]-LOGIC `0x80102540` = **No-Op** (`jr ra`) — alles in der f890-ANIMATE-Hälfte. Sub-Steps: [0] init (Player-Register 0x800aca58=cmd5 + player+0x93|=1 + Motion +0x94) · [1] pull-in · **[2] IMPACT `player.hp-=10`** (0x8010277c) · **[3] BITE `player.hp-=5`** (0x801027dc, LOOPT via +0x9c=0x6e-Fenster) · [4] hold · [5] struggle (Alt-Exit 0xb01 wenn Spieler sich freikämpft) · [6] release (player+0x93&=~1) · [7] recover · **[8] EXIT `+0x4=0x201`** (→ engage, +0x0&=~0x1000). Player-Seite: 0x800aca58 cmd 5 → LAB_80036834 (pinnt+animiert Spieler); HP<0 → Damage-cmd 3 (death) → äußerer state 7. **Portiert `re15_enemy_ai_live_grab`** (in Combat-Dispatch grid 0 als f890-Hälfte: decide dann animate; +0x5=3/4 → grab): byte-true Damage (−10 + −5) + Sub-Step-Struktur + Exit (+0x5=2) + HP<0→state3. Faithful-line: anim-gegatete Advances + Bite-Loop-Count = Stand-ins (Port = 1 Biss/Grab-Zyklus; engage re-committet → Damage wiederholt sich). DEFERRED: Player-grabbed-Pose/Lock-FSM (cmd5→LAB_80036834) + player+0x93-Flag (Feld-Aliasing) + Grab-Motion + Link-Globals. test_room1140_combat Part(6): Zombie im Grab-State → Player 100→85 (−15) → Exit zu engage.
**PHASE 8.9 FERTIG (2026-06-29, TURN-to-face portiert + +0x5=7-Modell-Korrektur, 31/31 ctest):** Agent + Selbst-Disasm-Verify der Bewegung. Dispatch: FUN_8010168c ruft je Frame DECIDE @0x8011f840[+0x5] DANN ANIMATE @0x8011f890[+0x5]; Bewegung nur in den Animate-Hälften (anim_set bewegt NICHT). **TURN [7]** (decide 0x80102d20 / animate 0x80102dc8): animate = `+0x6a += arc_test(playerX,0x80)` (±0x80 ≈7°/Frame zum Spieler, keine Translation); decide = Grab-Commit (`player+0x93==0 && dist<0x4b0 && arc_test(playerX,0x200)==0 && floor==` → +0x4=(facing+3)<<8|1). **FORWARD-WALK [5/6]** (0x80102bd8) = `FUN_8001ad68` Anim-Root-Motion (Displacement aus Walk-Anim-Stream) → DEFERRED (wie Player-Walk; konstanter Walker FUN_800245d8 nur für Back-Step). search/engage = stehen+tracken (FUN_8001aac4). **⚠️ KORREKTUR: +0x5=7 ist die TURN-State, NICHT der Lunge-Arm** (8.5d-„+0x5=7→arm" war Approximation) — der Arm (FUN_8010ab2c) ist separater dormanter Dispatch @0x80120208[+0x4=6]. Portiert: `re15_enemy_ai_live_turn` (animate [7]) + dispatch_decision case 7 (decide [7] grab-commit), in die Combat-Animate-Hälfte eingehängt (decide dann animate; +0x5=3/4→grab, +0x5=7→turn). Falsche Kopplung entfernt; test_live_decision_arm re-pointed aufs Arm-Primitiv. test_room1140_combat Part(7): Zombie +0x5=7 90° abgewandt nah → dreht sich (rot_y 1024→1664) → Grab-Commit → Player-HP fällt.
**PHASE 8.10 TEIL 2 FERTIG (2026-06-29, PLAYER-GRABBED-LOCK — der Grab pinnt den Spieler, Commit fb4a06e7, 31/31 ctest):** Der In-Game-Grab HÄLT jetzt den Spieler (er kann nicht mehr steuern/weglaufen während der −10/−5-Bisse). Byte-true selbst-disasm-verifiziert: der Grab latcht `0x800aca58 = ((+0x5−3)<<8)|5` = **cmd 5** (FUN_80102548 Sub-Step 0 @0x80102640); der Player-Command-FSM dispatcht `@0x80073f90[cmd]` → `[5]=LAB_80036834` (@0x80036834): pinnt den Spieler (Init DAT_800aca5a==0: grabbed-Y `DAT_800acc0e=−floor*1800`, `DAT_800acb04=0`), holt die greifende Entity (`DAT_800acbfc`, vom Walk/Grab-Setup), dispatcht per-Typ grabbed-Pose `@0x800ac758[type]` — **liest NIE das Pad**. Exit [8] @0x80102b90 clear-t cmd5 NICHT (Latch; der Player-grabbed-FSM resettet beim Anim-Ende/Struggle = deferred). Portiert: `re15_player_is_grabbed()` (enemy_ai_common.c) — `s_player_grabbed` in `re15_enemy_ai_live_grab` je Frame gesetzt, in `re15_enemy_ai_run_all` je Frame genullt = faithful-line Release (grabbed ⟺ Live-Zombie grabt diesen Frame → kein Soft-Lock; engage stoppt re-commit wenn Spieler aus dem ±0x4b0-Kegel → frei). game_step: neuer Branch `else if (rdt_ok && re15_player_is_grabbed())` skippt `re15_player_tick`+Collision+Door-Scan (RVD-Cam läuft) = exakt der Stair-„engine-driven"-Pattern. **1170-sicher** (Branch nur erreichbar wenn Live-Zombie grabt). Savestate-verankert (re15_flag_sweep.py 0x800aca58 width1 über 9 Saves): cmd 1/4/**7** live (cmd 7 = death im combat_death-Save = Grab→Death-Pfad; cmd 5/6 = transiente Mid-Grab/Walk). test_room1140_combat Part(8): free→grabbed→free. DEFERRED: per-Typ grabbed-Pose/Anim, exakter XZ/Y-Pin, Struggle-Escape (Sub-Step5 @0x80102968 anim-gegatet+bit-0x2), player+0x93|=1 (Feld-Aliasing).
**PHASE 8.10 TEIL 7 FERTIG (2026-06-30, FIRE-INPUT gewired — ZWEI-SEITIGER Combat SPIELBAR in-game, Commit 93c9f233, 31/31 ctest):** game_step-Normal-Branch: faithful-line Classic-RE Aim+Fire — **R1 halten = zielen, Square = feuern** → `re15_player_weapon_fire(2)` (Pistole); beim Zielen feuert Square + setzt `g_aot_action_pressed=0` (keine Tür/Treppe beim Zielen, wie im Original). **Beide Combat-Schleifen laufen jetzt in-game end-to-end:** Zombie→Spieler (spawn→wake→engage→turn→grab→pin→drain→Tod) UND Spieler→Zombie (R1+Square→Auto-Aim→byte-true-Damage→hurt/death→corpse). **1170-sicher** (feuert nur auf R1+Square = kein Input im Boot; trifft nur Live-Zombies). DEFERRED (zitiert, Präsentation): die exakte Original-Aim/Fire-Command-FSM (@0x80035810, 3-Level @0x80073f90[4]→action 8→sub-FSM @0x80010b68, FIRE anim-getriggert bei DAT_800acae9==1 in Aim-Sub-5 @0x80035a00), die Waffen-Inventory (DAT_800aca5d), die Aim/Raise/Muzzle-Anim, die exakte Line-vs-Box-Cone. Port hat keine Player-Command-FSM (Player=re15_player_tick+SCD) → faithful aufs Port-Pad-Modell gemappt (R1=0x0800/Square=0x8000, eigenes Bit-Layout ≠ PSX).

**PHASE 8.10 TEIL 6 FERTIG (2026-06-30, PLAYER-SCHUSS-DAMAGE-CORE — die zwei-seitige Damage-Schleife ist GESCHLOSSEN, Commit e058ad25, 31/31 ctest):** Der Spieler kann jetzt die Zombies schießen. **WICHTIG: der Player-Schuss ist FUN_80011f50, ein SEPARATER Resolver von FUN_80012d60** (das der Port als re15_resolve_attack hat) — andere Damage-Tabelle, anderes Targeting. `re15_player_weapon_fire(weapon_id)` (re15_damage.c) byte-true RE't (Workflow `player-fire-re` + Selbst-Disasm + PSX.EXE-Table-Dumps): Auto-Aim den nächsten LIVE-Zombie VOR dem Spieler in per-Waffe-Reach (`s_player_wpn_reach`, @0x8006e5a0, Pistole=1000) → per-Waffe/per-Typ Damage (`u16 @ 0x8006e0d0 + type(+0x8)*0x58 + weapon*4`, @0x800124b0-ec; **Zombie-Row 0x10/0x11/0x16 identisch = {0,6,24,5,5,15,15,200,40,100,200,100,10,100,10,100,200,100,400,20,0,100}**, Pistole w2=24) → enemy `+0x5=weapon_id` (Reaktions-Clip @0x800124bc), `HP-=dmg`, `+0x93|=1`, **Crit** (w7 || (w8 && dist<3000) → +0x93|=0x40 → HP=-1 für type<0x20, @0x800124fc-1c), `+0x4=2(HURT)/3(DEATH)` @0x80012520. Der getroffene Zombie läuft dann die TEIL-5 hurt/death-States. **FAITHFUL-LINE:** Auto-Aim = nächster Frontal-Zombie (`re15_ai_arc_test(player,0x400)`=Front-Hemisphäre) in Reach (min-dist wie DAT_8008f5e0 seed 0x7fffffff); die EXAKTE per-Waffe Line-vs-Box-Cone (Tester @0x8006e548: FUN_80012574 default/800127fc/800128a0 + FUN_8004f008-Projektion) + Hit-Dir-Clip (+0x6, DAT_8006f410={7,0,1,7,2,0,0,0}) + die Per-Typ-Rows für NICHT-Zombies = deferred. **1170-sicher** (standalone, nicht in game_step). test Part(11): Pistole → Front-Zombie −24 → HURT(2) (näherer Zombie HINTER übersprungen); tödlich → DEATH→CORPSE. **NÄCHSTER CHUNK: der Fire-INPUT** (Aim/Fire-FSM, Workflow-RE't): 3-Level-Command-FSM cmd `DAT_800aca58`(+0x4)→@0x80073f90[4]=0x80030660 → action `DAT_800aca59`(+0x5)→@0x80073e30/fb0/ff0[**action 8**=aim/fire] → sub `DAT_800aca5a`(+0x6); Post-Handler 0x80035810 = 8-Phasen-FSM @0x80010b68; **FIRE@Aim-Sub-5 0x80035a00**→FUN_80045024(spawn)→intern FUN_80011f50. Input: idle (action0 0x80031f38) liest Pad held DAT_800ac768/pressed DAT_800ac76c; **Square**(0x80)+Waffe-equipped DAT_800aca5d!=0→cmd 0x701 (Waffe heben); Aim Up/Down=0x1000/0x4000. ⚠️ Square ist im Port schon AOT-Action → Input-Mapping mit Sorgfalt (R1-aim+Square-fire?). Port hat KEINE Player-Command-FSM → Aim/Fire faithful aufs Port-Modell mappen; Aim/Fire-Anim deferred.

**PHASE 8.10 TEIL 5 FERTIG (2026-06-30, ZWEI-SEITIGER Combat begonnen — Zombie-hurt/death-States, Commit 062992d7, 31/31 ctest):** Nutzer-gewählte Richtung: der Spieler schießt die Zombies. Bottom-up gebaut — zuerst die EMPFANGENDE Seite. Die @0x8011f7b4-AI-Dispatch ist jetzt KOMPLETT: [0]init/[1]active/**[2]hurt** (`re15_enemy_ai_live_hurt`, FUN_80105a8c: 2D-Stagger-Anim-FSM @0x8011fb90[+0x5][+0x6] → zurück zu ACTIVE `+0x4=1` @0x80105b48; faithful-line: HURT→ACTIVE, Stagger-Anim+Hit-Stun-Dauer[=Reaktions-Clip-Framecount] deferred)/**[3]death** (`re15_enemy_ai_live_death`, FUN_80106ba4→FUN_80107cb0: Death-Anim+SE+Gore → state-word **7** `+0x4=7` @0x80107ec8 = CORPSE, out-of-range→kein Dispatch=inert, kann nicht mehr engagen/grabben; Anim+SE+Gore deferred)/[4]idle-deferred/**7=CORPSE**. `re15_enemy_take_damage` (existiert, FUN_80012d60-Enemy-Branch) setzt state 2 (Treffer)/3 (HP<0); der Tick dispatcht jetzt [2]/[3]. Neu: RE15_AI_STATE_CORPSE=7. **1170-sicher** (nur dispatcht wenn ein Hit state 2/3 setzt = der kommende Player-Schuss). test Part(10): Zombie-Schuss→HURT(2)→ACTIVE; tödlich→DEATH(3)→CORPSE(7) inert (kein Player-Damage). **NÄCHSTER CHUNK: der Player-Fire-Trigger** (Aim/Fire-Input→Hitbox→`re15_resolve_attack`[existiert]→diese hurt/death-States); Background-Workflow `player-fire-re` REt die Pipeline (Aim/Fire-FSM @0x80073f90 [Player-Command-States, R1/Square?] + Fire→Damage-Call-Site von FUN_80012d60 + Port-Input-Seite). Port hat KEINE Player-Waffen/Aim-Grundlage → das wird das neue Subsystem.

**PHASE 8.10 TEIL 4 FERTIG (2026-06-30, PLAYER-DEATH-FSM-Kern — der Grab TÖTET den Spieler, Commit 05af1a76, 31/31 ctest):** Der Combat-Loop ist jetzt end-to-end geschlossen (HP<0 → Tod statt HP-Drain für immer). Byte-true: HP<0=Tod (FUN_80012d60 @0x80012ee8 `bgez hp`); der Grab erreicht dasselbe HP<0. Das Player-Command-Register `0x800aca58` (=player block +0x4) dispatcht `@0x80073f90[state]`: [3]=generic death (@0x800366bc), **[7]=grabbed-death** (@0x8003694c — combat_death.sav zeigt 0x800aca58=7). Death-Sequenz @0x8003694c: INIT (sub-state 0) seeded Fade-Timer `DAT_800acaf2=0x78`(120) + Color-Fade (DAT_800acb18/acb40 mask 0xff000000|0x00ffff38), per-Frame (sub-state 1) Death-Kamera + Countdown → sub-state 2=game-over; liest NIE das Pad (Spieler eingefroren). Portiert (re15_damage.c): `re15_player_is_dead()`(=HP<0) + `re15_player_death_tick()` (byte-true Timer 0x78→0) + `re15_player_death_reset()` (in re15_actor_init); der Grab setzt player.state=**7** beim Kill. game_step: Branch `else if (rdt_ok && re15_player_is_dead())` VOR grabbed — friert Spieler ein (skip player_tick+Collision) + tickt Timer; RVD-Cam läuft. **1170-sicher** (nur erreichbar wenn ein Hit HP<0 treibt). Death hat Vorrang vor Grab (Zombie dead-grabt dann die Leiche). test Part(9): Grab-Kill→HP-2, state 7, Timer 0x78→0x77. **DEFERRED (Port hat keine Fade/Game-Over-Infra):** Color-Fade+Death-Kamera (@0x8003694c), „gefressen"-Anim-FSM (@0x8010a28c verschachtelt), Game-Over-Screen (sub-state 2).

**8.10 TEIL 1 (FORWARD-WALK) EXHAUSTIV RE'd (Workflow `forward-walk-re` + Savestate-Arbiter), NICHT portiert — 2 Blocker, raten verboten:**
- **TRANSLATION geklärt, Reader EXISTIERT im Port:** animate f890[5/6] @0x80102bd8 (decide @0x80102bd0=`jr ra`): Setup (motion +0x94=`+0x5+4`=Walk-Clip 9/10, `0x800aca58=cmd6`, SE) → per-Frame `FUN_8001ad68`. `FUN_8001ad68`/`FUN_8001ae38` **dreifach verifiziert** (Selbst-Disasm + Workflow + Format-Ref): lesen die **Root-SPEED aus dem EMR-Keyframe** (RE15_KNOWLEDGE §5.2 L461 „Per keyframe: 12B position+speed"): `dx=s16@keyframe+6`, `dz=s16@keyframe+10` (beide ae38-Pfade konsistent +6/+10); dann RotMatrix(rot_y +0x6a)·ApplyMatrix·`x(+0x34)=base(+0xa0)+disp.x`/`z(+0x3c)=base(+0xa2)+disp.z`. **Der Port liest +6/+8/+10 schon: `re15_emd_get_keyframe_speed` (emd_common.c)** (für ROOM1170-Cinematic gebaut, ungenutzt). → **Port-L3-Doku „kein Root-Motion in Keyframes / 6..11 UNREAD" ist FALSCH** (eigener Port-Code + §5.2 + Disasm widerlegen sie); actor_locomotion.c-Kommentare korrigiert. Player-Walk = Scalar-Speed (FUN_800245d8, +0x8C, korrekt); FUN_8001ad68 hat NULL EXE-Caller (Overlay/Enemy-only). Quelle = Enemy-Modell-File (EMS/EMD); Loader FUN_80022300 löst Sections auf (`entity[0x170]=B+dir[4]`=EMR, `[0x174]=B+dir[3]`=EDD).
- **✅ TRIGGER AUFGELÖST — der Forward-Walk ist KEINE Live-ROOM1140-Lücke; der Briefing-Combat ist KOMPLETT.** Variant-Schicht: `entity[+0x9]&0xf` → 13 Dispatcher @0x8011f80c (m0=FUN_8010168c/DECIDE 0x8011f840 [Port-Wurzel], m1=FUN_80101708/DECIDE 0x8011f8e0); einziger immediate `ori 0x601`-Write im ganzen Overlay = @0x801036c0 = m1 DECIDE[0]. **(1)** Feeding-WAKE committet `sb zero,9`→grid_id **0=m0** (@0x80103b3c) → Briefing-Zombies sind m0, **nie m1**. **(2)** das m0-LIVE-Brain (engage-decide @0x80102058 + search-decides — ALLE als `re15_ai_decide_*` portiert) setzt +0x5 ∈ {7,3/4,9/10,12,16} — **NIE 6** (exhaustiv getract) → ein LIVE m0-Briefing-Zombie forward-walkt NIE, macht wake→engage→turn→grab. **(3)** das +0x5=6 in combat_death.sav ist ein **DEAD-PLAYER-Artefakt** (player hp=-1, cmd 7): Distanz-Progression slot2(4492) engage → slot1(1476) dead-grab(+0x5=12, engage setzt das NUR bei hp<0) → slot0(357) walk(+0x5=6, motion=10) = walk-to-corpse. → **Forward-Walk (+0x5=6) = m1-Varianten-Verhalten (andere Räume) + Dead-Player-Edge, KEINE Live-ROOM1140-Lücke. Port-Combat dort byte-true KOMPLETT.** Disziplin-Win: drei Turns das +0x5=6 eines **post-death** Saves verfolgt — Lehre: post-death-Save ist atypisch; für „was tut LIVE-Combat" MUSS der Referenz-Save den Spieler am Leben haben [[disasm-verify-decompiles]].
- **Translation-Reader + Enemy-Anim sind DA (Blocker B war keiner):** Enemy-Bank (re15_enemy.h `re15_enemy_bank_t`) trägt `skel`(EMR+Keyframes)+`anim`(EDD), beim Spawn geladen; `re15_emd_get_keyframe_speed` liest +6/+10. → falls m1-Walk je gebraucht (anderer Raum): ~10 Zeilen (motion=walk-Clip → keyframe_speed → RotMatrix(rot_y)·speed+pos). Für ROOM1140 NICHTS zu tun.
- **NÄCHSTER SCHRITT (Combat ist komplett für ROOM1140):** echte Optionen = (a) Player-Death-FSM (cmd7→@0x80073f90[7]=0x8003694c, Death-Anim/Game-Over) wenn der Grab tötet; (b) Sub-States [2]/[3]/[4] hurt/death/idle (braucht Player→Enemy-Attack/Schießen); (c) optionale mid-LIVE-Combat-Capture (--provoke kurz) zur Bestätigung (braucht ROOM1140-JUMP-Index). Forward-Walk-Translation NUR falls ein m1-Gegner-Raum drankommt.

**PHASE 8.5a FERTIG (Commit bcada19a, 30/30 grün):** Re-root begonnen. `re15_enemy_ai_live_init` (FUN_80100688 @[0]: state→1, ai_timer+0x9c=0x14) + `re15_enemy_ai_live_active` (FUN_80101224 @[1], Attack-Windup-Hälfte: wenn ai_flags&0x100 → ai_attack_timer+0x1da-- ; ==0x12c(300)→re15_enemy_lunge_begin [geteilt]; ==0→Recovery 0x1503=state3/+0x5=0x15, motion 0xb/0x1f). Neues Feld ai_attack_timer (+0x1da). Der byte-true Lunge-Begin kommt jetzt aus dem RICHTIGEN Handler. test_live_active_lunge. **OFFEN (8.5b):** der Live-Tick+Dispatch FUN_80100424 (gates+dist+@0x8011f7b4[+0x4]) als Port-Dispatcher; die Decision/Arm-Hälfte (FUN_80101224 @0x80101560+, wo +0x1d8|0x100 + +0x1da geseedet werden); Sub-States [2]/[3]/[4]; dann game_step-Wiring (typ-gegated 0x10/0x11/0x16) + dyn. Verifikation.

**RE'TE KERN-PRIMITIVE (beide byte-true belegt):**
- **Damage-Resolver FUN_80012d60** (ghidra1:77607-77814): unified hit/damage. Testet eine Hitbox gegen Spieler-Block (DAT_800aca54) UND jeden Gegner (DAT_800acc2c, Stride 0x1f4) via FUN_8002b5d0, wendet `DAT_8006f418[type]` an. Player-Branch @80012e18-f04, Enemy-Branch @80012f08-80013034.
- **Hitbox-Test FUN_8002b5d0** (ghidra1:118005-118130): attack-hitbox vs actor. Target-Hitbox-Daten target+0x78 (radius-range +0x6/+0xa, height +0x8), ctx target+0x7c. Angular-Sektor (wenn +0x6!=+0xa: ratan2+rsin/rcos interp Radius über Facing) sonst circular; X/Z-AABB-Broadphase; Euklid-Dist SquareRoot0 < Radius; Y/Height-Range. attack-desc a1(+0 X/+4 Y/+8 Z), a3=Radius.
- **Caller FUN_80017fa4** (#13, ghidra1:84648-84717): Gegner-Lunge (Dispatch-Tab @0x80071da4) baut Hitbox aus Angreifer-Pos +0x28/+0x2a/+0x2c via FUN_8001c6e8 → `FUN_80012d60(0x1f4, hitbox, 0)` = attack_type 0 = 10 dmg.

**CODE-STAND (re15_port/engine/src/re15_damage.c, additiv, mock-getestet, KEIN game_step-Eingriff → kein 1170-Risiko):**
- ✅ `re15_player_take_damage` (#13, Player-Branch): HP-=dmg, hit-once +0x93 bit0, hurt/death state, bleed-RNG, front/back-anim.
- ✅ `re15_enemy_take_damage` (Enemy-Branch): HP-=dmg, hit-once, hurt/death, Reaktions-Clip aus re15_react_table (kein bleed).
- ✅ `re15_hitbox_overlap` (FUN_8002b5d0 **circular**, byte-true @8002b6fc-end: R=radius+atk_r, X/Z-AABB unsigned-idiom, Euklid-dist<R strikt via dmg_isqrt, Y-Range -h<dy<h) + `re15_hitbox_test` (Actor-Wrapper: center=pos+hit_offset, liest hit_radius_min/max/hit_height). Sektor-Pfad DEFERRED (braucht BIOS-ratan2). **2026-06-29.**
- ✅ `re15_resolve_attack` (FUN_80012d60 **LOOP**, byte-true: Gegner-Pass slots 1+, Player-Block slot0 immer, Reverse-Apply; GATE A self-exclusion @80012f40=slot-eq; GATE B @80012f54 +0x90&0x3000000=death/despawn DEFERRED/inert da Writer-FSM fehlt; per-frame hit_react&=1 + front-confirm bit0x80; take_damage). **2026-06-29.**
- ✅ `re15_enemy_attack` (FUN_80017fa4 **Attack-Trigger** Damage-Pfad, byte-true: Box aus atk_pt_* (+0x28/+0x2a/+0x2c) radius **500**/type **0**=10dmg → re15_resolve_attack, attacker self-excluded). Actor-Felder atk_pt_x/y/z. DEFERRED: FUN_8001c6e8 Raum-Koll-Sekundär, Lunge-Advance +0x4/+0x6+=0x1e, SE, state-reset +0x6e=0xd. test_enemy_attack. **2026-06-29.**
- Actor-Felder hit_radius_min/max, hit_height, hit_offset_x/y/z (re15_actor.h) — Port-Repräsentation der +0x78/+0x7c-Daten, Default 0.
- Tabellen re15_damage_table[11] / re15_react_table[11] byte-true. test_damage.c: test_hitbox_overlap_circular + test_resolve_attack. **9/9 PASS, 28/28 ctest grün.**

**HITBOX-DATEN-QUELLE + WERTE (RE'd + extrahiert 2026-06-29): NICHT im EMD.** Enemy +0x78/+0x7c = per-Typ-Tabelle (FUN_80019700: +0x78=DAT_800b2248[type<<2]+8, type=byte@+0x8; Tabelle overlay-PATCHED zur Laufzeit → on-disc-Slots null). Player +0x78 = feste EXE-Struct @**0x80073e94** (PSX.EXE file-offset 0x64694 = `00 00 06 fa 00 00 c2 01 fa 05 c2 01`). Struct: radius_min@+6, height@+8, radius_max@+10. **WERTE byte-true (Savestate-RAM, HP-sanity-gated + EXE-verifiziert):** Player + STAGE1-Zombie(Typ 0x47) = **450/1530 circular**, offset (0,-1530,0); STAGE2 Typ 0x16=400/1440; Typ 0x29=1100/1080. **ALLE bekannten Instanzen circular** → Sektor-Pfad ungenutzt. Verdrahtet: `re15_player_apply_hitbox` (init), `re15_enemy_apply_hitbox` (op_sce_em_set spawn). Werte-Quelle: stage_saves/HASH-*_1 (STAGE1), mzd_stage2_room, HASH-*_5; mzd_stage1_room.sav hat KEINE aktiven Gegner.

**INTEGRATIONS-PLAN — Stand 2026-06-29:**
- ✅ **2. resolve_attack-LOOP** portiert (FUN_80012d60) — siehe CODE-STAND.
- ✅ **3. FUN_8002b5d0 circular** portiert — siehe CODE-STAND. Sektor deferred.
- ✅ **1. Hitbox-Daten wiren** — Werte byte-true extrahiert (Savestate + EXE) + verdrahtet (re15_player/enemy_apply_hitbox bei init/spawn). Siehe HITBOX-DATEN-QUELLE oben. Offen nur: room1140-Zombie wird NICHT via Sce_em_set gespawnt (Overlay-Entity-Liste) → der Spawn-Hitbox-Call greift erst, wenn der Overlay-Spawn portiert ist (Teil von Punkt 4).
- 🟡 **4. Gegner-Attack-Trigger** — Damage-Pfad PORTIERT (`re15_enemy_attack`, FUN_80017fa4). Die **AI** (Typ-0x47-Zombie) ist teil-RE't 2026-06-29 (Handler **FUN_8011d6d4**, STAGE1_full): zweistufige Dispatch — AI-STATE `PTR_FUN_801217a0[ent+0x4]` + ACTION `@0x80071da4[..]` (idx12=FUN_80017fa4). VERIFIZIERT byte-true: Distanz @ent+0x1d0 = SquareRoot0(DX²+DZ², 16-bit-Deltas); FUN_80101b64-Transitions; FUN_8001a9cc Arc-Test. **PORTIERT** (Primitive, getestet): `re15_enemy_player_dist`, `re15_ai_arc_test`.
  ⚠️ **KORREKTUR eines Agenten-Mislabels:** „state 0x701 == attack" ist FALSCH/unbelegt — FUN_80101b64 setzt 0x701 bei `dist<2000 && arc!=0`, d.h. wenn der Spieler nah UND **außerhalb** des ±0x2c8-Frontkegels ist → liest sich als **turn-to-face/reorient**, nicht Lunge. Welcher State/Action die echte Lunge ist, ist NICHT gemappt → KEINE geratene Attack-Decision portiert.
  ⬜ NOCH OFFEN: (a) welcher ent+0x4-State + Action die Lunge feuert (PTR_FUN_801217a0/PTR_FUN_80120208 Tabellen + state-0x01/0x701-Handler decompilen); (b) der **Attack-Point FUN_80104178** = GTE-Matrizen + Model-Pool (ent+0x188+idx*0xac) + Kollision → DEFERRED; (c) Bewegungs/Targeting-AI. **Nächster Weg: room1140-Savestate eines ANGREIFENDEN Zombies** (re15-room-capture) → live ent+0x4-State + atk_pt_* lesen, dann den bestätigten State + Attack-Point portieren.

**AI-ARCHITEKTUR (RE't 2026-06-29, Disasm-verifiziert) — warum der Rest ein Multi-Session-Subsystem ist:** Die Lunge ist ZWEISTUFIG + struct-layout-gebunden:
- **Overlay-AI-State** (FUN_8011d6d4/FUN_8010a8c8): dispatch `PTR_FUN_801217a0[ent+0x4]` (Logik) + `PTR_FUN_80120208[ent+0x4]` (Anim); FUN_8010a8c8 ruft FUN_80104178 (Attack-Point) + Raum-Koll.
- **EXE-Action-Driver** (Tabelle @0x80071d98, indexiert über attacker `DAT_800b52c4 +0x0`): LAB_80017eb0 Setup (+0xe=0x20 Active-Frames) → LAB_80017ed8 Advance → LAB_80017f50 per-Frame-Tick (ruft FUN_80017fa4 solange +0xe>0) → FUN_800174e4 Ende. = Multi-Frame-Lunge (~32 Frames) über die DAT_800b52c4-Work-Struct.
- **Attack-Point** (FUN_80104178): GTE + Model-Pool (ent+0x188+idx*0xac) — der Port abstrahiert das via eigenes Skeleton → NICHT byte-true portierbar ohne PSX-Pool-Modellierung.
**FAZIT:** Der byte-true AI-Antrieb braucht das Modellieren mehrerer PSX-Work-Structs (Actor-Volllayout, DAT_800b52c4, Model-Pool), die der Port bewusst als benannte Subsets abstrahiert → großer Multi-Session-Port, kein sauberer einzelner Code-Brick mehr. Damage-Engine + AI-Primitive stehen byte-true; der AI-Antrieb ist ein eigenes Sub-Projekt.

**Nutzer-Entscheidung 2026-06-29: VOLLER AI-Port (Multi-Session).** Treue Linie festgelegt: AI-LOGIK byte-true portieren, Daten-Repräsentation (Model-Instanz/Attack-Point) aufs vorhandene Port-Skeleton mappen (Attack-Point = Angriffs-Bone-Weltpos via re15_skel_compute_pose), NICHT die PSX-GTE/Model-Pool-Pipeline nachbauen (die der Port schon anders macht).

**PHASE 1 FERTIG (FSM-Karte, RAM-extrahiert HASH_1.sav, byte-true):**
- Per-Typ-Tick-Entry: @0x80072bac[0x47] = FUN_8011d6d4 → Distanz@+0x1d0 + dispatch BEIDER State-Tabellen je Tick.
- **FSM ist 3-STUFIG verschachtelt** (frühere „flache 22-Einträge"-Lesart KORRIGIERT): (1) Main-State ent+0x4 → PTR_FUN_801217a0[+0x4], nur ~5 Einträge: 0=FUN_8011d84c (INIT: speichert Player-Ptr @ent+0x1a8=&DAT_800aca54, schreibt AI-Params in Model-Pool +0x5fc=0x2c8 Arc/+0x5f8=0x40/+0x5fa=0x30/+0x5fe=0x138), 1=FUN_8011d9f4, 2=0x8011db40, 3=0x8011db88, 4=0x80050be8(EXE). (2) FUN_8011d9f4 (State 1) sub-dispatcht **PTR_FUN_801217b4[(ent+0x24)&0xf]** (=0x801217a0+0x14; die „Einträge 5-21" = diese Sub-Behavior-Tabelle, geteilte EXE 0x8004f1xx-0x80051xxx). (3) Anim-Tabelle PTR_FUN_80120208[+0x4] separat. entity-ptr ist word* (ent[1]=byte+0x4, ent[0x62]=+0x188 Model-Pool, ent[0x6a]=+0x1a8 Player-Ptr).
- **Anim-Tabelle PTR_FUN_80120208** (heterogen: idx0-7, 8/9=embedded `02 03 04 05`-Daten, 10-23 mit Dup-Paaren; FUN_80101b64=anim idx23, also ANIM nicht Logik — frühere Lesart korrigiert).
- **Action-Driver FUN_80019e20** (EXE): iteriert Model-Instanz-Pool DAT_800a73b8 (Stride 0x84), GTE-Weltpos [0x14/0x15/0x16]=Attack-Point, dispatch Action-Tabelle @0x80071d40 (Base; FUN_80017fa4=idx0x19, Lunge-Setup/Advance/Tick=0x16/0x17/0x18, finish=idx7).
- Lebender room1140-Zombie (stationär) = **State 4**, action idx 1.
- ✅ **Attack-DECISION byte-true BESTÄTIGT** (Weg-A-Trace, identisch in 3 Handlern FUN_80101b64/de4/2058): `dist<2000 (0x7d0) && FUN_8001a9cc(player,0x2c8)!=0 → ent+0x4 word = 0x701` (Attack-Commit-State). Approach (FUN_80101c7c): `arc==0 && ent+0x1d8&0x10 && dist<4000 → 0x201`. **PORTIERT**: re15_enemy_should_attack (re15_damage.c, getestet). [Mein früherer „0x701 wohl reorient"-Zweifel war zu vorsichtig — die Transition ist real + 3-fach belegt.]
- ✅ **DISPATCH-TYPING byte-true aufgelöst (Weg C, Direkt-Decode aus STAGE1.BIN — KEIN Ghidra-Setup nötig).** STAGE1.BIN lädt **RAW @0x80100000, KEIN 0x800-Header** (CLAUDE.md irrt für die Overlays; file-offset = addr−0x80100000; verifiziert: FUN_8011d6d4 @0x1d6d4 = `addiu sp,-0x18; lui s0,0x800b; addiu s0,..0xca40`=_DAT_800aca40). Dispatch-Indizes sind RAW Bytes: Main **+0x4**, Sub **+0x9** (FUN_8011d9f4 `lbu v0,0x9(v0); andi 0xf`), Sub-Sub **+0x5** (FUN_8011da48). PTR_FUN_801217b4 = &PTR_FUN_801217a0[5]. Sub-Tabelle[0..15] dekodiert: FUN_8011da48,8004f100,8004f3a4,8004f5e8,8004f310,8004f4e0,8004f6f0,80050cb8,80050ddc,80050f00,80051024,80051148,80051484,800517f0,80051908,80051b00. → **direktes xxd+MIPS-Decode aus STAGE1.BIN raw@0x80100000 ist der schnelle Overlay-RE-Weg** (für alle künftigen Overlay-Tabellen/Disasm).
- ⚠️ OFFEN (Rest des Lunge-Pfads): die `0x701 → FUN_80017fa4`-Kette selbst (welcher Sub-State/Action; Agent-inferiert, nicht selbst getract) + der Attack-Point (FUN_80104178 via Skeleton) + die +1024-Arc-Konvention (Mid-Lunge-Savestate zum Pinnen). FSM ist 4-stufig (siehe AI-ARCHITEKTUR).

**ATTACK-ROUTING DYNAMISCH BESTÄTIGT (2026-06-29, eigene Live-Combat-Capture).** Capture-Pipeline (scratchpad re15_provoke_capture.py + re15_quickload.py): boot→New Game→Debug-Menü (Select→Down=JUMP); JUMP-Liste hat ALLE Räume (DPad-Right MUSS kurz getappt werden, hold≤0.05s, sonst Auto-Repeat → überspringt ans Listenende!). 124=OPENING, 107=LOBBY OFFICE. **Quick-Load via DuckStation `-statefile <menu.sav>`** lädt das Debug-Menü direkt (kein 64s-Boot) — Menü-Shots ✓; der echte Square-Load greift nach -statefile aber NICHT (nur boot-basiert laden). Der Briefing-Menü-Base-State ist zeit-sensitiv (Briefing spielt während der Load-Wartezeit aus → einmal landete der Spieler tot). **Diese Death-Capture (qf_r1.sav) = echtes STAGE1-Combat:** 7 aktive Gegner Typ **0x10/0x11**, ALLE +0x4=1 (approach), ALLE **+0x9=0** (→ sub0=FUN_8011da48), +0x5∈{1,2,6,c}; Spieler getötet → DAT_800aca58=**7** (death/grabbed). → BESTÄTIGT: aktive STAGE1-Gegner routen via +0x9=0/sub0, also Attack-Commit +0x5=7 → FUN_8011da48 → DAT_801217b8[7]=**LAB_80050ddc** (Anim-Advance, FUN_8001f314) + DAT_801217c4[7]=0x80051148. **+0x5 ist eine ANIM-PHASE, nicht die Hitbox** — die Hitbox FUN_80017fa4 feuert separat über den Model-Instanz-Action-Driver FUN_80019e20 (action 0x16-0x19), wenn die Lunge-Anim die aktiven Frames erreicht. Zwei gekoppelte Systeme. (Typ-0x47 nicht live gefangen, aber die FSM-Architektur ist typ-geteilt + das Routing über +0x9=0/sub0 ist für aktive STAGE1-Gegner belegt.)
**PHASE 2 FERTIG (2026-06-29, byte-true Dispatch-Foundation + Decision-Brain, additiv + 9/9 getestet, 29/29 ctest grün — NICHT in game_step verdrahtet, kein 1170-Risiko):** Neues Modul `re15_port/engine/src/enemy_ai_common.c` + `include/re15_enemy_ai.h`. ALLE Tabellen direkt aus STAGE1.BIN decodiert (raw@0x80100000, Weg C, file 0x217a0):
- **Main-Tabelle PTR_FUN_801217a0[0..4]** byte-true: 0=FUN_8011d84c(INIT), 1=FUN_8011d9f4(ACTIVE), 2=8011db40(HURT), 3=8011db88(DEATH), 4=80050be8(IDLE/EXE).
- **Sub-Tabelle PTR_FUN_801217b4[0..15]** (=&[5]) byte-true bestätigt (=Memory-Liste). **FUN_8011da48 (+0x9=0) Layout GELÖST:** EINE zusammenhängende Tabelle; logic=SUB[1+(+0x5)], anim=SUB[4+(+0x5)] (verifiziert +0x5=7→logic SUB[8]=0x80050ddc, anim SUB[11]=0x80051148 == Memory-Spotwert).
- **Decision-Brain in Per-Mode-vtables** (0x8011f840/f960/120264/120324, je 8 Einträge): [0]=FUN_80101b64(search+timer)/FUN_80101c7c(approach-only), [1]=80101de4(search), [2]=80102058(rich: grab/flee), [3..7]=80102540/80102bd0/80102d20 (movement/anim).
- **Portiert byte-true + getestet:** `re15_ai_set_state_word` (0x701→state1/sub7 etc.), `re15_enemy_ai_init` (FUN_8011d84c: state→1, Guards +0x93/+0x8f/+0x95=0, ai_arc=0x2c8 + params, Hitbox-Wire, stationär +0x9&0x40→state4/+0x5=6), `re15_enemy_ai_tick` (FUN_8011d6d4: Gate DAT_800aca40&0x20000000 + grid_id&0x20, dist@+0x1d0-Cache, Main-Dispatch), `re15_enemy_ai_active` (FUN_8011d9f4: grid_id&0xf), Decision `re15_ai_decide_search_timer/approach/search` (FUN_80101b64/c7c/de4 — 0x701 attack / 0x201 approach / 0x101 timer / 0x801 wander[RNG&7|&3] / 0x1001). Neue Actor-Felder ai_dist/ai_timer/ai_flags/ai_contact/ai_arc/ai_p5f8/5fa/5fe (re15_actor.h, offset-zitiert). Gemeinsame Engine-RNG `re15_engine_rand8` aus re15_damage exportiert (eine RNG für bleed+AI, wie Original).
- **BONUS byte-true:** PTR_DAT_8012179c→0x80121790 = Hitbox-Struct des Typ-0x47-Zombie = `00 00 06 fa 00 00 c2 01 fa 05 c2 01` = **450/1530** — bestätigt re15_enemy_apply_hitbox(0x47) jetzt AUCH aus Overlay-Daten (nicht nur Savestate).
- **DEFERRED (zitiert, nicht geraten):** die generischen Humanoid-EXE-Leaves (SUB[1..15]=0x8004f/0x80050/0x80051, Bewegung/Anim-Exec) + die +0x5-Logic/Anim-Leaves in FUN_8011da48; FUN_80102058 (braucht FUN_8001a780-Oktant + Globals DAT_800acad6/ae7); HURT/DEATH/IDLE-Bodies; game_step-Tick + room1140-Spawn + Verifikation.
**PHASE 3 FERTIG (2026-06-29, Decision-Brain komplett + Dispatch-Architektur byte-true gemappt, 12/12 AI-Tests, 29/29 ctest grün):**
- **ARCHITEKTUR-Durchbruch (der fehlende Link Active→Decision):** Die Decision-Handler liegen NICHT in PTR_FUN_801217b4, sondern werden über **+0x5** (sub_state_1, die Anim-Phase) dispatcht: **FUN_8010168c** macht `(*PTR_FUN_8011f840[+0x5])()` (DECIDE) + `(*PTR_FUN_8011f890[+0x5])()` (ANIMATE). FUN_8010168c/FUN_80101784 stehen in der AI-Mode-Tabelle @**0x8011f800** (16 Einträge, je ein decide+anim-Dispatcher-Paar; FUN_8010168c@+0xc nutzt f840, FUN_80101784@+0x14 nutzt f960). **f840-vtable (decide on +0x5)** byte-true: [0]=80101b64(search+timer) [1]=80101de4(search) [2]=80102058(engage) [3,4]=80102540 [5,6]=80102bd0 [7]=80102d20 [8]=80102f1c [9]=801031a4 [10]=801033c0 [11]=80103854. **f890=&f888[2] (animate on +0x5):** [0]=80101d08 [1]=80101ef0 [2]=801021f8 [3]=80102548… f960-Mode = gleiche Tabelle mit [0]=80101c7c (approach-only). → +0x5 IST der Active-AI-Sub-Mode; die Decisions setzen ihn (0x701→7, 0x201→2, 0x101→1, 0x801→8…), nächster Tick dispatcht den neuen Mode.
- **PORTIERT byte-true + getestet (re15_enemy_ai):** `re15_ai_facing_aligned` (FUN_8001a780: ((other.rot_y−e.rot_y+0x400)&0xfff)<0x800), `re15_ai_decide_engage` (FUN_80102058 — **alle Globals auf den Player-Actor gemappt:** DAT_800acae7=player+0x93 hit_react, DAT_800acad6=player+0x82 floor, DAT_800acaee=player+0x9a hp; Arme: attack 0x701 / directional-grab 0x301|0x401 [floor-gated, front/back via facing] / dead-grab 0xc01 / contact-reaction 0x901|0xa01 [ai_contact&3 + Front-Kontakt] / 0x1001), `re15_ai_dispatch_decision` (FUN_8010168c-Decide-Hälfte: +0x5 → search_timer(0)/search(1)/engage(2), [3+]=movement/attack-exec DEFERRED). **Damit ist der gesamte bestätigte Decision-Brain (alle 4 Handler b64/c7c/de4/2058) portiert + per +0x5 dispatchbar.**
- **VERIFIKATION der Helfer:** FUN_8001a6d4 = re15_atan2_q12 (catan-atan2, bestätigt). FUN_8001af20 = RNG (stateful via DAT_800ac774: next=(x+(x>>7))&0xff) → re15_engine_rand8-Stance (non-reproduzierbar-by-construction, distributions-treu) bleibt korrekt.
- **NICHT auto-verdrahtet:** re15_ai_dispatch_decision wird NICHT vom Tick aufgerufen — der Live-Pfad erreicht die Decision-Schicht durch die generischen Humanoid-EXE-Leaves (PTR_FUN_801217b4[1..15]=0x8004f.., FUN_8011da48 +0x5-logic=SUB[1+k]/anim=SUB[4+k]), die roh-Byte im EXE-Dump liegen + den Model-Pool brauchen → DEFERRED. FUN_8004f100 (decodiert): lädt entity, dist@+0x1d0, cmp <0x5dd, schreibt +0x5 (generische Bewegung).
**PHASE 4 (2026-06-29, EMPIRISCHE Live-Pfad-Auflösung + erstes Live-Leaf, 13/13 AI-Tests, 29/29 ctest grün):**
- **WICHTIGE RECORD-KORREKTUR (dynamisch, aus stage_saves/mzd_stage1_combat_death.sav):** Die Dispatch-Tabellen PTR_FUN_801217a0/b4 sind zur Laufzeit **NICHT gepatcht** (Live-RAM == on-disc STAGE1.BIN, 0/22 Einträge differ). Die aktiven type-0x10-Zombies sind +0x4=1, +0x9=0, +0x5∈{1,2,6,12}. → Der **dynamisch bestätigte LIVE-Pfad** = FUN_8011d9f4 → FUN_8011da48 → logic=SUB[1+(+0x5)] = die **EXE-Generic-Humanoid-Leaves** (0x8004f100-Familie, aus PSX.EXE @load 0x80010000), **NICHT** der Overlay-Decision-Brain (FUN_80101b64-Familie). **Die Phase-3-Overlay-Handler sind ein PARALLELES AI-Mode-System (B)** (dispatcht via FUN_8010168c/80101784/8010b6d4/8010b800 — in der Mode-Tabelle @0x8011f800 — in die f840/f960/120264/120324-vtables auf +0x5), das eine ANDERE Mode/Type fährt und NICHT auf dem Live-+0x9=0-Pfad liegt. Beide sind byte-true RE't + bleiben (echte Funktionen), jetzt klar als System (A) live / (B) parallel gelabelt. [Korrigiert die frühere Konflation „FUN_80101b64 = Live-Attack-Routing" — der Live-Pfad ist (A).]
- **PORTIERT byte-true + getestet:** `re15_ai_exe_assess` (FUN_8004f100, das +0x5=0 Assess-Leaf, PSX.EXE-disasm: dist≥0x5dd→+0x5=1; Spieler außerhalb ±0x4b0-Arc→+0x5=2; (global_flag&1 && type==0x4b)→+0x5=6; player.hit_react→+0x5=6). Reine Logik (kein Model-Pool). Globals aufgelöst: 0x800aca88/90=playerX/Z, 0x800acae7=player+0x93 hit_react, 0x800aca52&1=STAGE1-Flag (`re15_enemy_ai_set_global_flag`). **FUN_8001ab9c ≡ FUN_8001a9cc ≡ re15_ai_arc_test** (Cone direkt übergeben; identisch inkl. 0x800-Grenze) — kein neuer Code nötig.
- **MIPS-Disasm-Tooling → jetzt Skill `re15-psx-disasm`** (`scripts/re15_disasm.py` dis/table/scan/bytes; PSX.EXE PS-X-EXE t_addr 0x80010000 text@file 0x800, Overlays raw@0x80100000) — der schnelle Weg für die EXE-Leaves (Weg C). NICHT mehr als Scratchpad neu bauen.
**PHASE 5 (2026-06-29, Live-Decision-Graph komplett + an den Tick gekoppelt, 16/16 AI-Tests, 29/29 ctest grün):**
- **Live-Sub-Mode-Graph (System A) byte-true decodiert** (alle Leaves per Skill `re15-psx-disasm` scan/dis aus PSX.EXE): die DECISION-Leaves sind +0x5=0/1/2 (assess/search/turn), +0x5≥3 sind Movement/Anim-Exec (anim_set FUN_8001f314 / walker FUN_800245d8 / FUN_8002d7d8 / Model-Pool). Graph: **0 assess** (far→1, player außerhalb ±0x4b0→2, hit→6); **1 search** (dist<0x1f4→3, ai_target(+0x1dc/1de) außerhalb ±0x400→3, dist≥0xbb9→5, hit→6); **2 turn** (player in ±0x40→0, dist≥0x7d1→1, hit→6).
- **KRITISCHER BEFUND — kein Live-Leaf schreibt +0x5=7.** Die +0x5-Writes über ALLE Leaves: {0,1,2,3,4,5,6,18}. → **Im Live-Pfad ist die Lunge-ATTACK KEINE +0x5-Decision**; sie emergiert aus der Lunge-Animation + dem Model-Instanz-Action-Driver (FUN_80019e20 action 0x16-0x19 → FUN_80017fa4 = re15_enemy_attack = die Hitbox). Bestätigt die „zwei gekoppelten Systeme / +0x5 = Anim-Phase"-Einsicht; +0x5=7 (0x701) war NUR System B (Overlay), nicht der Live-Pfad.
- **PORTIERT byte-true + getestet:** `re15_ai_exe_search` (FUN_8004f3a4, +0x5=1), `re15_ai_exe_turn` (FUN_8004f5e8, +0x5=2), `re15_ai_exe_dispatch` (FUN_8011da48-Logic-Hälfte: +0x5→assess/search/turn für 0/1/2; ≥3 deferred). Neue Actor-Felder ai_target_x/z (+0x1dc/+0x1de). **An den Tick GEKOPPELT:** re15_enemy_ai_active ruft jetzt für sub(+0x9)=0 re15_ai_exe_dispatch → **der Tick führt die Live-Decision real aus** (FULL CHAIN getestet: tick→ACTIVE→exe_dispatch→assess→+0x5-Transition). Reine +0x5/+0x6-Mutation, keine ext. Seiteneffekte, kein 1170-Risiko (nicht in game_step).
**PHASE 6 (2026-06-29, Lunge-Attack-Trigger byte-true portiert, 17/17 damage-Tests, 29/29 ctest grün):**
- **Execution-Layer authoritativ decodiert** (RE_15_Quellcode_V2-Decompiles): **FUN_80017fa4** = `FUN_80012d60(500,&point,0)` → **Radius 500 bestätigt** (mein re15_enemy_attack korrekt), Attack-Point = Work-Struct **[0x14/0x15/0x16]** (GTE-Weltpos, vom Action-Driver berechnet — NICHT +0x28; der frühere +0x28-Hinweis korrigiert); on (resolve!=0 || Raum-Wand) → Reset Action (+0x6e=0xd) + SE (FUN_800199d4/45024, deferred). **Lunge-Maschinerie** (LAB_80017eb0 Setup: +0xe=0x20=32 Active-Frames, action=0x17; LAB_80017f50 Tick: +0xe-- bzw. FUN_800174e4-finish bei 0, dann FUN_80017fa4 JEDEN Frame). **FUN_80019e20** (Action-Driver): iteriert Model-Pool DAT_800a73b8 (stride 0x84) bis UNK_800aa538, RotMatrix/ApplyMatrix → Weltpos [0x14/0x15/0x16], PTR_LAB_80071d40[instance+0]/[+1]-Dispatch, Anim-Frame-Stepping (+0x37/+0x3c). = der GTE/Model-Pool-Teil → Skeleton-Mapping-Grenze (faithful-line, deferred).
- **PORTIERT byte-true + getestet (re15_damage.c):** `re15_enemy_lunge_begin` (LAB_80017eb0: lunge_frames=0x20) + `re15_enemy_lunge_tick` (LAB_80017f50: Fenster--, feuert re15_enemy_attack jeden aktiven Frame, beißt 1×/Lunge via Connect-Reset). Neues Actor-Feld lunge_frames (+0xe). re15_enemy_attack-Kommentar auf den authoritativen Decompile aktualisiert (Radius 500, GTE-[0x14..]-Attack-Point=Skeleton). test_enemy_lunge: in-range trifft 1× + Lunge endet; out-of-range zählt 32 Frames runter ohne Treffer.
- **Byte-true Kern hier** = das 32-Frame-Active-Window + per-Frame-Fire + bite-once-Reset. Die Lunge-BEWEGUNG (Work-Struct-Velocity/Pos + GTE-Attack-Point) + das End-of-Window-Keyframe (FUN_800174e4) = Port-Skeleton-Job (deferred).
**PHASE 7 (2026-06-29, byte-true RE KOMPLETT + Integrations-Entry + Full-Chain-Test, 17/17 AI-Tests, 29/29 ctest grün):**
- **Lunge-Action-Sequenz byte-true bestätigt** (action-table @0x80071d40 dekodiert): [0x16]=LAB_80017eb0 (setup), [0x17]=LAB_80017ed8 (advance/movement), [0x18]=LAB_80017f50 (per-frame fire). FUN_80019e20 dispatcht instance+0x0 (movement 0x16→0x17) + instance+0x2 (fire 0x18) je Frame. **Lunge-BEGIN (instance+0x0:=0x16) = Anim-Keyframe-Event** (FUN_80019e20 +0x37/+0x3c-Stepping liest Action-Change-Flags aus den Keyframes) → Teil des deferred Model-Pool/Anim-Layers; im Port ruft die Anim-Schicht re15_enemy_lunge_begin am äquivalenten Punkt. → **Die byte-true RE der gesamten Zombie-AI ist damit end-to-end abgeschlossen** (Decision-FSM + Attack-Sequenz + Hitbox + Damage, alle Konstanten zitiert).
- **PORTIERT + getestet:** `re15_enemy_ai_step(slot)` (enemy_ai_common.c) = der per-Gegner Integrations-Entry (FSM-Tick FUN_8011d6d4 + Lunge-Slice FUN_80019e20→re15_enemy_lunge_tick) — die EINE Funktion, die game_step pro aktivem Gegner rufen wird. **Full-Chain-Test (test_ai_step_chain):** Gegner spawnen → ai_step (INIT→ACTIVE, dist gecacht) → ai_step (Decision) → Lunge auslösen + atk_pt=Spieler (Skeleton-Stand-in) → ai_step → Hitbox feuert → **Spieler-HP 100→90**. Beweist: alle byte-true Teile komponieren in-engine. KEIN game_step-Eingriff (Disziplin gewahrt).
**PHASE 8.1 FERTIG (2026-06-29, atk_pt-Skeleton-Mapping, additiv, 29/29 ctest grün, kein game_step-Eingriff):** Der faithful-line Attack-Point. `re15_skel_bone_to_world` (skeleton_common.c) = die **byte-true** Modell→Welt-Bone-Transformation (Ryaw(rot_y)·trans+actor.pos, = NPC-Render-Loop + Stair-Fuß-Probe). `re15_enemy_update_attack_point` (re15_damage.c) = posed das Skelett (QUERY) → Attack-Bone-Weltpos → atk_pt_* (int16). **Mechanismus exakt; Bone = Parameter** (keine byte-true Konstante). Tests: test_attack_point_mapping + test_ai_step_chain treibt atk_pt jetzt durch die reale Abbildung.
  **BYTE-TRUE LUNGE-KETTE (2 Agenten + Decompile, trianguliert 2026-06-29 — KORRIGIERT Phase-6/7-Annahmen):** (a) Der **Damage-Punkt** = live GTE-Weltpos der **8 posed Body-Part-Frames**, jeden Frame von **FUN_80019e20** (EXE-Action-Driver) in work +0x28/+0x2a/+0x2c geschrieben = ApplyMatrix(M1,vecA)+(+0x60)+ApplyMatrix(M_pose,vecB), M_pose aus +0x74=model_base(+0x188)+bodypart*0xac+0x40. Body-Part-LUT **DAT_8011f7a4={00,07,08,0e,02,04,09,0d}** (0x0e/0x08=Kopf/Oberkörper dominieren). **FUN_80104178 = Lunge-MOTION+Gore (Entity-Block), NICHT der Damage-Punkt** (Katalog 73/77 + meine Phase-6-Notiz hatten das konflatiert). (b) **Lunge-BEGIN-Trigger** (= das „Anim-Keyframe-Event", ersetzt im Port re15_enemy_lunge_begin): Timer entity+0x1da→**300** → FUN_8010b274 feuert 8× FUN_80019d50(8,3,0x16,body_part_ptr) → schreibt action 0x16 (sh a2,0(v1)@0x80019d98) → Selbstlauf 0x16(+0xe=0x20)→0x17→0x18(jal FUN_80017fa4)→0x19(Biss). (c) **OFFEN:** Model-Pool-Index→EMD-Bone (welcher em10/16/47-Bone=Kopf) braucht Model-Pool-Build-Order ODER mid-lunge room1140-Savestate (combat_death.sav war post-bite, Pool leer).

**PHASE 8.2 FERTIG (2026-06-29, room1140-Spawn byte-true verifiziert + Fix, 30/30 ctest grün, kein game_step):** **PRÄMISSE KORRIGIERT** — die Briefing-Zombies kommen DOCH via `Sce_em_set` (RDT-SCD sub00 des Raums, `sub_scd[0]`), NICHT aus einer Overlay-Entity-Liste (RE'd byte-true, eigener Agent: ROOM1140.RDT sub00 + EXE-Handler `LAB_800420a0` ghidra1:152496-152869). sub00 = 10 Sce_em_set (5 IF / 5 ELSE) gegated `Ck zone3 flag 0xd2`; Flag clear (scd_vm_init nullt Flags) = Erstbesuch = **5 Zombies 0x16/0x10/0x10/0x11/0x11** an festen Pos, Posen 0x13 liegend/0x27 fressend. Da der Port `op_sce_em_set` hat + sub00 läuft → **spawnt sie schon**.
- **Verifiziert:** `tests/unit/test_room1140_spawn.c` lädt die ECHTE ROOM1140.RDT, tickt main00+sub00, prüft das Roster byte-true (Typ/Pos/rot_y/Pose). Asset-Pfad CMake-injected.
- **BYTE-TRUE FIX `op_sce_em_set`:** schrieb `behavior` (pc[3]) nach **state** (+0x4) statt **grid_id** (+0x9) — Pre-AI-RE-Mismap. `LAB_800420a0`: pc[3]→+0x9 (AI-Sub-Dispatch-Index + Pose-Sel + Flags 0x40 stationary/0x20 skip), +0x4=0 (INIT). Korrigiert (state→0, grid_id→behavior); Spawn-Pose kommt weiter aus dem `behavior`-Arg → kein Elliot/Krähen-Regress (nur AI[unverdrahtet]/Member lesen die Felder).
- **OFFEN (cited):** Hitbox-Dims 0x10/0x11 fehlen in re15_enemy_apply_hitbox (nur 0x16/0x47; on-disc-Tabelle overlay-gepatcht → room1140-Savestate nötig). pc[18]!=0→state=4-Variante deferred (Roster pc[18]=0).

**PHASE 8.3 FERTIG (2026-06-29, Zombie-Spawn-Konstruktor FUN_80100424, 30/30 ctest grün):** **DISPATCH-BEFUND:** Mein portierter AI-Tick (FUN_8011d6d4/d84c/d9f4/da48) ist der Typ-**0x47**-Handler. Die Briefing-/Combat-Zombies (0x10/0x11/0x16) mappen via `@0x80072bac` auf **FUN_80100424** (decompiliert, RE_15_Quellcode_Overlays/STAGE1) — den Typ-Konstruktor, den der Sce_em_set-Handler LAB_800420a0 SOFORT ruft. Portiert als `re15_enemy_spawn_init` (scd_vm.c, typ-gegated 0x10-Familie, in op_sce_em_set): setzt die finale Spawn-State aus +0x9 (sel6 fressend→+0x4=0x20c01=state1/+0x5=0xc/+0x6=2, +0x9=0; sel0xd→0x201; sel0xe→0x1201; sel1/3→+0x5=5; sel8 liegend→kein +0x4, +0x9 bleibt=state0/grid0x88). Pose-Hälfte war schon re15_enemy_spawn_action (byte-identisch). Elliot/Krähen/Irons unberührt (kein 1170-Regress). test_room1140_spawn prüft den vollen Post-Konstruktor-State. **op_sce_em_set ist jetzt byte-true-vollständig** (Field-Write + Konstruktor).
- **OFFEN (Voraussetzung game_step-Wiring):** der Per-Frame-Handler der 0x10-Familie ist nicht gepinnt. Aktive State-Maschinerie (PTR_FUN_801217a0[1]=FUN_8011d9f4) ist laut Phase-4-Savestate GETEILT zw. 0x47/0x10, aber der Per-Frame-Typ-Eintrag @0x80072bac[0x10] unterscheidet sich. VOR Wiring klären: ist mein Tick auch 0x10s Per-Frame-Handler? → room1140-Savestate (live update-fn-ptr) ODER Disasm der Entity-Update-Schleife.

**PHASE 8.4+ (offen) — Rest INTEGRATION (faithful-line + dynamisch):** (0) Per-Frame-Dispatch 0x10-Familie auflösen [Voraussetzung]. (1) Movement/Anim-Exec-Leaves (+0x5≥3, anim_set/walker) auf Walker/Skeleton mappen + der byte-true Lunge-BEGIN (entity+0x1da==300) statt Platzhalter. (2) **room1140-Overlay-Spawn** (Gegner-Entity-Liste, fehlender Spawn-Pfad). (3) **game_step-Wiring**: re15_enemy_ai_step pro Gegner (1170-Risiko, vorsichtig additiv) — hier wird atk_pt-Live-Wiring + EMD-Bone-Wahl savestate-verifizierbar. (4) **Dyn. Verifikation** (re15-room-capture/savestate: Spieler in Range → HP fällt, vs. Port).
- ⬜ **5. Hit-Once-Clear-Trigger** (re15_player_clear_hit_guard / enemy-Pendant): wann +0x93 bit0 gecleart wird (Attacke endet → nächster Treffer kann landen).
- ⬜ **6. Hurt/Death-Clips** (state2 → clip 0xa via FUN_80035af0 Sub-FSM #22; Gegner-Reaktions-Clips re15_react_table → sub_state_1).
- ⬜ **7. Verifikation:** STAGE1 room1140 (Briefing-Zombies) → Spieler in Range → HP fällt. Headless ohne Input → skript-gesteuerter Input-Test ODER DuckStation-Savestate-Vergleich.

**WICHTIG:** Die **Resolver-Kette** (Hitbox-Primitive + Loop + beide take_damage-Branches) ist jetzt in-engine lauffähig + getestet (kein game_step-Eingriff, kein 1170-Risiko). Der verbleibende Block (1-Werte + 4 AI-Trigger + 5/6/7) ist der gekoppelte „beobachtbares Ziel"-Teil — koppelt an Model-Load/AI/Clips. Nächster Schritt = **1 (echte Hitbox-Werte via Savestate/Overlay-Map) + 4 (AI-Attack-Trigger)** zusammen, dann 5/6/7. Erst dann macht room1140-Verifikation Sinn.
