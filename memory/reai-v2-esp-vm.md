---
name: reai-v2-esp-vm
description: ESP-„VM" war ein Duplikat von scd_vm.c — entfernt; Cross-Check fand 6 echte Bugs; +0x1c8 statisch aufgelöst
metadata:
  type: project
  node_type: memory
  originSessionId: f8885edf-151b-4733-b0d4-58398ed373e5
---

**KORREKTUR (2026-06-30):** Was die Handover-Notizen „ESP-C = eine 3-Schichten-Effekt-Script-VM" nannten — `FUN_8003f0a0` + die Opcode-Tabelle `@0x800744a8` (95 Ops 0x00–0x5e) + der 0x170-Instanz-Pool `DAT_800b2b4c` — **IST die SCD-VM**, kein separates Effekt-Subsystem. Beweis: der 0x170-Pool wird aus `RDT+0x40/+0x44` (main/sub SCD) geseedet (`FUN_8003ef6c`→`FUN_8003ee3c`); das SCD-Format `body = start + u16[start]` = das `FUN_8003edec`-pc-Seed (`rdt_common.c:179`); `@0x800744a8[0x4c]` = `op_sce_espr`. **Der Port implementiert diese VM BEREITS: `re15_port/engine/src/scd_vm.c`** (87 Opcode-Mappings, im Spiel verdrahtet — spawnt Zombies via op_sce_em_set, fährt Combat).

**Was passiert ist:** Ich folgte dem falschen Handover-Framing und baute eine PARALLELE byte-true VM `re15_esp_vm.c` (45 von 95 Ops portiert: voller Control-Flow + Work-Struct + ESP-B-Spawn). Das war Duplikat-Arbeit — ich hätte ZUERST nach einer SCD-VM greppen müssen. Auf Nutzer-Entscheidung KONSOLIDIERT: `re15_esp_vm.{h,c}` + `test_esp_vm.c` ENTFERNT, `scd_vm.c` bleibt die eine SCD-VM (33/33 ctest).

**Was bleibt (der Wert):**
- **`RE15_SCD_OPCODES_REFERENCE.md`** (Repo-Root) = byte-true Katalog aller 95 SCD-Opcodes (`@0x800744a8`-Adressen, Operanden, pc-Advances, Semantik), per 77-Agenten-Workflow klassifiziert + selbst-disasm-verifiziert. Nützlich, um scd_vm.c Op-für-Op zu validieren.
- **Cross-Check-Bugs in scd_vm.c GEFIXT (6):** (6) **`op_cut_replace` (0x4b)** ließ den byte-true Live-Cut-Switch weg (`LAB_80040414` @0x800404ac: wenn aktiver Cut==a → sofort auf b via cam-change-Pfad) — die ROOM1130-Nutzer-beobachtete fehlende Kamera-Änderung; gefixt (nutzt `g_re15_active_cut` + `cam_change_pending`) + Test. 0x53 untersucht, aber NICHT gefixt (raten-verboten: mode-1-Objekt unbekannt, modes 3/5 nicht darstellbar). Vorherige 5: (5) **`op_plc_rot` (0x58)** war RE2-fehlattribuiert — byte-true ist ein Flag-Bank-Bit-Check-Prädikat (Bank=pc[1], Word=`work_vars[desc&0xff]>>5`, Bit=`desc&0x1f` MSB-first, Invert wenn `(desc>>8)==0`); reimplementiert als `op_flag_ck2` (nutzt `re15_game_flag_get`) + 3 Unit-Tests. Vorherige 4: (1) `s_opcode_sizes[0x3D]` 4→3 (LAB_80041238; unmapped → op_unknown desynct). (2) `op_member_set2` (0x35) las aus `t->locals[]` statt `g_scd.work_vars[pc[2]]` (s16, DAT_800b0fd0). (3) `op_sce_key_ck` (0x51) No-Key-Return invertiert (`param` statt byte-true `param^1`). (4) `op_sce_espr_control` (0x52) war `return 1`-Stub statt Flag-AND-Prädikat (`param^1`). 2 Cross-Check-Pässe (16+35 Ops via Workflow, jede DIVERGE selbst verifiziert). **GROSSER BEFUND: scd_vm.c hat RE2-FEHLATTRIBUIERTE Handler** (laufen das Falsche für RE1.5, brauchen Reimpl+externe Tabellen): 0x47 (Object-Dispatch via DAT_800ac9b0 vs aot_on), 0x50 (Ptr-Latch vs item_aot_set), 0x53 (Work_set-Bind vs fade_set), 0x58 (Flag-Bank-Prädikat @0x80074664 vs plc_rot) + fehlende Seiteneffekte (0x2d/0x38/0x4a/0x4b). 0x0d = dokumentierte defensive count==0-Abweichung (kein Fix). 0x15 Fehlalarm. Volle Liste: RE15_SCD_OPCODES_REFERENCE.md.
- **ESP-A/B (`re15_esp.c`) bleiben** — der RDT-EFF/TIM-Parser (RDT+0x4C) + Effekt-Sprite-Pool (`DAT_800b2360/2368`, AABB-Cull `FUN_8004d5f0`). Daten-/Pool-Schicht, KEIN VM-Duplikat (`test_esp_parse` grün).
- **Cross-Check ABGESCHLOSSEN (2026-06-30):** das letzte deferred Item **0x4a statisch aufgelöst → kein Fix nötig**. `entity+0x1c8/+0x1ca` = **Animations-Pose-Felder**: alle 21 EXE-Zugriffe sind `sh`-Writes in der SCD-VM-Region 0x80041bbc–0x80042084 (Clears in op3f/4a/53; mode-indizierter Setter @0x80041cac liest u16-Pose-Code aus per-Mode-Tabellen @0x80073ea5 via `entity[8]`); Overlays clearen `_DAT_800ac784+0x1c8` aus AI-Handlern (STAGE1 FUN_8011da2c). **KEIN literal-offset Reader** (0× `lh/lhu/lw …,0x1c8(` in EXE, 0× Overlay-Read) → konsumiert vom Pose/Anim-Renderer via computed access. Port modelliert Anim anders (bank.skel/anim), kein Äquivalent-Feld → **die Clears sind faithful No-Ops; 0x4a state-4-Writes = arbeitende Port-Vereinfachung (`g_scd.plc_gun_state`), erzwingen würde Player-FSM kollidieren → declined wie 0x53.** 0x3f/0x5d teilen 0x1c8/0x1ca → gleiches Rationale. **Bilanz: 6 Bugs gefixt (0x3d/0x35/0x51/0x52/0x58/0x4b), 0x4a+0x53 resolved-not-a-bug (Beleg), 0x2d/0x38/0x47/0x50 = Subsystem-Arbeit (RE2-Fehlattribution / collision-AABB).**

**C3-RENDER ARCHITEKTUR (2026-06-30, → `C3_RENDER_DESIGN.md`):** byte-true gemappt (3-Agenten-Breitensuche + eigene Disasm-Verifikation + Savestate-Pool-Dumps). **DREI getrennte Sprite-Subsysteme; der Port modelliert in `re15_esp.c` das FALSCHE:** (1) `DAT_800b2360/2368` + Cull `FUN_8004d5f0` = **Map/Menü-Screen-Sprite-Layer** (kein In-World-Draw) — GENAU das modelliert ESP-B fälschlich (`re15_esp_run`=`FUN_8004d5f0`, `re15_esp_spawn(type,x,y,w,h,duration)`). (2) `DAT_800a73b8` „model_inst_pool" (96×0x84, busy `+0x6c`, alive=3, full→0xff — selbst-verifiziert) = 3D-Model-FX, Spawn op 0x3a→`FUN_80019700`, Tick `FUN_80019e20` (RotMatrix). (3) **`FUN_800392d4`/`FUN_80039590` = sprite.pri FOREGROUND-MASKS, NICHT der Muzzle-Rasterizer** (FEHLATTRIBUIERT von Agent B + mir, dann verifiziert: `FUN_800392d4` liest `room_struct+0x24` = **`cameraStart`** RDT-Offset 0x24, per-Cut → Sprite-Prioritäts-Liste). **Der Port hat das BEREITS byte-true portiert** (main.c:1034-1075, `re15_pri_parse_section`, bg_pc.c Foreground-Atlas aus BSS-SLD, pri_common.c — explizit „byte-true to PSX FUN_800392d4/FUN_80039590"). Producer-Spec (Record stride 0x20, SPRT@+0xc, rgb=0x80, clut=0x7800@+0x1a) gilt — aber es ist die Foreground-Mask-Schicht, erledigt. **Der echte Effekt-Sprite-Gap (Muzzle/Blut) = Subsystem 2** (`DAT_800a73b8` model_inst_pool, op 0x3a→`FUN_80019700`; Pool shared mit Charakter-Render `FUN_8002c444`/`FUN_80045630` 12+ Spawns → darum Saves „0 busy" = per-Frame transient, NICHT „keine Effekte"). Hürde: Pool ist shared (Port macht Charakter-Render anders) + KEIN Live-Save mit aktivem Effekt. **C3-PRÄMISSE TEILS WIDERLEGT: die dominante sichtbare Sprite-Schicht (Foreground-Masks) ist schon da; verbleibend = op-0x3a Muzzle/Blut, narrow + dynamisch + braucht Live-Capture.** Plan-Retarget in `C3_RENDER_DESIGN.md` §4. Lehre erneut: Mechanismus VERIFIZIEREN (hier: `room+0x24`=cameraStart + main.c-Kommentar) bevor man portiert — Agenten-Label war falsch.

**C3 IMPLEMENTIERT (2026-06-30, 6 feat-Commits c4b00878→ec360af6, 33/33 grün):** op-0x3a-Effekt-System end-to-end gebaut — ESP-C Clip-Parser (`re15_esp_anim`/`re15_esp_coord`, gegen echte ROOM1140-Bytes) → Partikel-Pool `re15_esp_fx_*` + byte-true Anim-Timing (`FUN_80019e20` Frame-Timer: param-low=Dauer, 0xff=Loop→desc-low, 0/0=Ende; eff05 terminiert@8, eff07 loopt@5) → `op_sce_espr_on` verdrahtet (pc[2]=Effekt-ID/Bank, pc[3]=Sub-Index, pc[8/10/12]=lokaler Offset; cat→bank GELÖST: FUN_80019700 cat=a0>>24=pc[2]=Effekt-ID) → Room-Load-Parse+Bank-Bindung+per-Frame-Tick (`pc_load_room_esp`/`re15_esp_fx_tick` in main.c) → Effekt-TIM-Upload (Slot 19) + `pc_draw_effects` (Projektion wie Spieler → Billboard-Quad via `re15_render_textured_tri`). **BYTE-TRUE: Position (Owner+Offset projiziert), TIM, Cell-(u,v)-Origin. FAITHFUL-LINE geflaggt: Cell-pro-Frame-Mapping (Index=frame%count_b, 24px-UV, ~world-600) = der datengetriebene Rest (C3_RENDER_DESIGN.md §2c) → braucht Live-Capture.** Empirisch: ROOM1140 op 0x3a sitzt in den EXTRA-SCDs (@0xd99/dcd/df9) = event-getriggert (Briefing-Cutscene), nicht im 176-B-Haupt-Script. **Verifikations-Limit: re15_pc.exe (Console-Subsystem aber SDL) gibt in der nicht-interaktiven Agent-Umgebung kein erfassbares stdio → visueller Lauf liegt beim Nutzer.** **FRONTIER (Stand Session-Ende 2026-06-30, Commits c4b00878→3d25fb85):** der echte ROOM1140-Effekt-Trigger ist NICHT op 0x3a (ROOM1140 hat 0× op 0x3a, korrekt disasm) — die Effekte (effect-id 5 by VALUE) kommen aus den **STAGE1-Overlay-Zombie-Verhaltens-Handlern** (`FUN_80106edc`/`80106a44`/`801066dc`/… rufen `func_0x80019700(0x5002800,…)`). **GELIEFERT byte-true+getestet (2 Bricks):** (1) `re15_enemy_gore_tick` (FUN_80106a44 +0x93&2-Hit-Spawn, effect-id 0 — Commit e4fca663). (2) `re15_enemy_gore_setup` (FUN_80106edc, der **effect-5**-Gore-Burst — Commit a405f446); beide in `re15_enemy_ai_run_all` verdrahtet. **DISPATCH GELÖST:** der Effekt-Aspekt ist `(*(code*)(&PTR_LAB_8011fe30)[entity+0x5])()` — indiziert per **`entity+0x5` = sub_state_1 = die ANIM-PHASE**; `0x8011fe30 + 0x58*4 = 0x8011ff90 = FUN_80106edc` → **Trigger = Anim-Phase 0x58 (88)** (die Effekt-Handler sitzen an hohen Phasen 79-96 = Ende einer langen Zombie-Anim = Gore-Höhepunkt). `re15_enemy_gore_setup` gated `sub_state_1==0x58 && sub_state_3==0 && rand&1` → 2× effect-0 + 1× effect-5 am Zombie. **LETZTER BRICK zur SICHTBAREN Gore (präzisiert):** `sub_state_1`=Behavior-State 0x00-0x7b; volle PTR_LAB_8011fe30-Tabelle dekodiert → die **Gore-States 0x57-0x65 = die DEATH-Anim-Sequenz** (0x58→FUN_80106edc effect-5). Die Walking-Behavior `FUN_80105b7c` setzt entity+5 nur niedrig (0x02/0x07/0x08/0x13), nie 0x58 → der Zombie erreicht 0x58 NUR über den **DEATH-Handler** (`FUN_8011db88`, Zombie-Main-Dispatch @0x8011f7b4[3], im Port `enemy_ai_common.c:119` DEFERRED). **NÄCHSTE EINHEIT = die Death-Anim-FSM porten.** `FUN_8011db88` (Death-State) ist nur ein Sub-Dispatcher: `(**(code**)(&DAT_801217d0 + entity+6 *4))()` → die Death-Sub-Handler sind **EXE-Funktionen `FUN_80050cb8`/`80050ddc`/`80050f00`/`80051024`/`80051148`/`80051484`/`800517f0`/`80051908`/`80051b00`/`80051cf8` (entity+6==0..9), NICHT decompiliert** (in RE_15_Quellcode_V2 fehlend → mit re15_disasm.py disassemblieren). Diese generische Death-FSM advanced entity+5 in die Gore-Sequenz (0x57→0x58). **`FUN_80050cb8` DISASSEMBLIERT (2026-06-30):** = die GENERISCHE Death-Anim-FSM (operiert auf `0x800ac784`=cur entity; `entity+0x180/+0x184` Anim-Daten, `entity+6` Death-Sub-State, `entity+0x8f`=7/`+0x95`=0; ruft `func_0x8001f314`=anim_set). Setzt `entity+6` + spielt Death-Anim; `entity+5` wird in den späteren Sub-Handlern (`FUN_80051148`/`80051484` ff., `sb v0,0x5(v1)` @0x800513a4/0x80051428/0x800516f4 …) **DATENGETRIEBEN pro Anim-Frame** geschrieben — KEIN literales 0x58, der Wert kommt aus den Death-Anim-Daten (per-Frame Effekt-State-Track). **Fazit: die effect-5-Gore ist ein DATENGETRIEBENER Death-Animations-Effekt** — bei einem Death-Anim-Frame wird entity+5==0x58 → Overlay-Effekt-Aspekt `PTR_LAB_8011fe30[0x58]`=FUN_80106edc feuert. **KORREKTUR (2026-07-01, Workflow+eigene Verifikation) — der Pfad war falsch:** `entity+5` wird **ON DAMAGE** gesetzt (nicht in einer Anim-Sequenz), von den EINZIGEN zwei +5-Writern: `FUN_80012d60`:78 (Melee) `entity+5=DAT_8006f430[hit_zone]`, `FUN_80011f50`:165 (Schuss) `entity+5=weapon_id`; gleicher Block `entity+4=2/3`, `+6=hit-dir`, `+7=0`. **Der Port hat das schon** (`re15_react_table`==`DAT_8006f430`=`[03,03,09,0A,0B,0E,0F,10,11,12,14]` @re15_enemy_take_damage:270). Der Effekt-Dispatch ist eine **2D-MASTER-TABELLE @0x8011fb90 `[entity+5 row *0x20][entity+6 col *4]`**: hurt `FUN_80105a8c` (verifiziert), death `FUN_80106ba4` (base 0x8011feac), active flat `PTR_LAB_8011fe30`(=row 0x15). Zombie-`entity+5∈{3..0x14}`; **row 3=FUN_80105b7c (effect-0), rows 9..0x14=NULL; effect-0 NICHT in ROOM1140-Bank (05/07).** `FUN_80106edc`(effect-5)=master-row 0x20 = ANDERER/höherer Enemy. **→ meine Gore-Bricks (gore_tick effect-0, gore_setup effect-5/0x58) sind byte-true, treffen ROOM1140 aber NICHT.** **Ehrliche Lage: die sichtbare ROOM1140-Zombie-Gore hat KEINEN sauberen Pfad** (Zombie spawnt effect-0/NULL, nicht 05/07; 05/07 = wohl kinematisch/anderer Enemy). **EFFEKT-SYSTEM FUNKTIONAL 100% KOMPLETT (2026-07-01, Commits d100ac48/e704e7d8):** der globale effect-0-Bank IST `CORE00.ESP` (`shared_assets/PSX/DATA/CORE00.ESP`, 7092B, ids {3,8,0,2,4} inkl effect-0; geladen bei game-init via Installer `FUN_8001923c` file-id 9 → `FUN_8001945c(param_4=0)` = globale Slots). Portiert: `re15_esp_parse_global` (idh_off=0, ptr_end=round_up4(size)-4, kein TIM) + `re15_esp_set_global_bank`; `re15_esp_fx_spawn` löst effect_id Room→Global auf (per-fx `bank`-Ptr); main.c lädt CORE00.ESP bei boot; **`re15_enemy_hurt_fx`** (= Master-HURT-Dispatch `FUN_80105a8c`→`FUN_80105b7c` Phase 0) spawnt effect-0 beim Zombie-Treffer (state==2+sub_state_3==0, in run_all verdrahtet) → erreichbar (Zombie anschießen) + animiert + zeichnet. 33/33 grün. **SAVESTATE-VERIFIZIERT (2026-07-01, der entscheidende Beweis):** am LIVE-ROOM1140-RAM (`re15_ss.py` auf mzd_stage1_briefing/engage_live) bestätigt: Install-id-Liste `DAT_800bbdf0`=`03 08 00 02 04 ff|05 07 ff` → Global-Gruppe {3,8,0,2,4}=**exakt CORE00.ESP** (effect-0 @0x801ed524 count_a=22/count_b=20 = exakt die Datei). **Mein Port ist byte-true KORREKT** (nicht nur Agent-Behauptung). Zombie-Hurt spawnt effect-0 byte-true (`FUN_80105b7c`:81 `func_0x80019700(0x2000,…)`, 0x2000>>0x18=0). `FUN_8001945c` indiziert Bank nach id-VALUE; globaler Install (param_4=0) ist NICHT im Decompile (Boot-Init, File-id; "CORE00"-String=0). **VERBLEIBT NUR die effect-0-Textur-PIXEL** (Mechanik 100% byte-true; Port spawnt/animiert/zeichnet, Platzhalter=Room-TIM-Slot19): effect-0 word1=0x001f7951 (Runtime==Datei) → tpage 0x001f=VRAM(960,256) 4-bit, CLUT 0x7951=VRAM(272,485). Pool `DAT_800a73b8` stride 0x108: +0x6c state/+0x30 tpage/+0x32 clut/+0x70 effid. **effect-0-BLUT-TEXTUR EXTRAHIERT — byte-true (2026-07-01, Commit 2e3649a2, Variante 3 GESCHAFFT):** `stage_saves/mzd_stage1_hit_effect.sav` (3 aktive effect-0, via `re15_fire_capture.py`: equip aus briefing_live, ~U5 laufen, R1+Square, LIVE-Save via SDL-0/LeftShoulder-Hotkey). **KRITISCHE LEHRE: `re15_ss.py`s VRAM-Decode ist FALSCH** (DuckStation-Savestate speichert VRAM nicht-linear → Framebuffer @x=448 statt x=0, CLUT-Zeilen lesen leer/verschoben; die RAM-Reads sind korrekt, nur VRAM nicht). Ich hatte fälschlich „Textur nicht resident" geschlossen — sie IST da (Nutzer bestätigte sichtbares Blut). **Zuverlässig: DuckStation `[Debug] ShowVRAM=true` + Screenshot-Hotkey an Pad-Button → 1024×512-PNG = VRAM 1:1 (RGB aus RGB555); `re15_vram_extract.py` rekonstruiert 15-bit (`v=(R>>3)|((G>>3)<<5)|((B>>3)<<10)`) + dekodiert tpage+clut.** effect-0 byte-true: tpage 0x001f→(960,256) 4-bit, clut 0x7951→(272,485)=Blut-Palette (0x084c=(96,16,16)…) → Blut-Sprite-Sheet (~20 Splatter-Frames + Pfützen + Streaks, 14% opak). Asset: `re15_port/shared_assets/extracted_fx/effect0_blood.png` (+clut.txt +vram_view_ground_truth.png = volle VRAM-Ref, hat auch id 2/3/4/5/7/8). **Effekt-Kette KOMPLETT** (Spawn+Mechanik+Textur). OFFEN nur: Port-Integration (Textur in GPU-Slot laden + pc_draw_effects daran binden statt Room-TIM-Slot 19). **Capture-Tooling (SKILL.md): DuckStation-Hotkey an SINGLE Pad-Button (`&`=CHORD!); Shoulder zuverlässig, L3/R3 nicht; R1 brauchte SDL-Binding.** Voller Stand: C3_RENDER_DESIGN.md §2g. **KORREKTUR-Lehre:** der ROOM1140-Zombie spawnt effect-0 (Hit) NICHT effect-5; effect-5/0x58 war ein anderer Enemy (meine Gore-Bricks byte-true aber mistargeted); RDT-Survey: 05/07 universell, effect-0 global. Volle Architektur: `C3_RENDER_DESIGN.md §2f`. Volle Korrektur: `C3_RENDER_DESIGN.md §2f`. — (Vorherige, jetzt überholte Spur:) `entity+5`==0x58 hat KEINEN literalen Setter — kein `reg=0x58;sb reg,5`, kein State-Word `entity+4` mit Byte1==0x58, kein Increment `lb+5/addiu/sb+5`, kein direkter `jal` zu FUN_80106edc/a44/etc. (nur via Dispatch-Tabelle). → **Der Wert 0x58 ist DATENGETRIEBEN: er kommt aus den Zombie-Anim-Daten** (als State-Word `entity+4` per `lw` aus einer Anim-Daten-Tabelle geladen, NICHT als Immediate). **NÄCHSTE EINHEIT = das datengetriebene Zombie-Anim-State-System porten** (die generische Anim-FSM `func_0x8001f314`/`FUN_80050cb8` ff. + die Zombie-Anim-Daten, die per-Frame State-Words mit entity+5∈{…,0x57,0x58,0x5c,0x60,…} liefern). Das ist ein GROSSES datengetriebenes Anim-Subsystem — kein Quick-Brick. Die Effekt-Seite (re15_enemy_gore_setup, gated entity+5==0x58) ist FERTIG verdrahtet + feuert automatisch, sobald dieses Anim-System entity+5 auf 0x58 bringt. **Ehrlicher Limit-Punkt:** der Gore-Effekt-Thread ist effekt-seitig byte-true komplett + die Trigger-Natur exhaustiv bewiesen; der sichtbare Trigger braucht das ganze Anim-State-System (eigene Einheit). Ganze Kette + Adressen: `C3_RENDER_DESIGN.md §2d/§2e`. **Alle Details: `C3_RENDER_DESIGN.md §2d/§2e`.** Mandat: [[reai-v2-full-port-mandate]] (100% Port, keine Shortcuts; faithful-line nur als geflaggter Zwischenschritt — hier die Bone-Position + die unmodellierten +0x8f/+0x94/+0x95-Felder). C3-Engine (Parser/Pool/Anim/Spawn/Room-Load/TIM/Draw) byte-true fertig.

**Why:** Die Vorsession schätzte ESP-C als „separate Effekt-VM" / „Sprite-Quad" fehl; ich übernahm das ungeprüft. Der Fehler kostete viel Arbeit.

**How to apply (Lehre — wichtig):** **VOR dem byte-true Nachbauen eines Subsystems IMMER greppen, ob der Port es schon hat** (`grep s_op_table`, `re15_<name>`, die CLAUDE.md-RE-Quellen). Hier hätte ein `grep scd_vm.c` + ein Blick auf `@0x800744a8` (das scd_vm.c selbst zitiert) die Duplikation verhindert. Nächste Schritte (Nutzer-Richtung): (a) Cross-Check vertiefen — scd_vm.c-Handler-Semantik vs. `RE15_SCD_OPCODES_REFERENCE.md` (weitere Bugs wie 0x3d); (b) C3-Render an scd_vm.c andocken (Draw-Ops fehlen scd_vm.c, nur length-stubs); (c) anderes Thema. Verwandt: [[disasm-verify-decompiles]], [[reai-v2-foundation-combat]].
