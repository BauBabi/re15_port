# Item-Box dormant-code hunt (wf_773c1d9f) - VERDICT + raw reports

## VERDICT

All four spot-checks confirm the reports' load-bearing claims byte-for-byte (tables @0x80074bdc-0x80074c08, run FSM @0x8004d904, four-array zero loop @0x8003e52c-554, sole selector writer @0x800431a8). Synthesis follows.

---

# VERDICT: (b) PARTIAL — dormant fragments exist, but NO dormant item-box CODE exists anywhere in RE1.5

The earlier "engine-side ABSENT" verdict survives adversarial re-examination — but it must be upgraded from a name-grep negative to this instruction-shape negative with two real dormant fragments attached, neither of which is box logic.

## What EXISTS (dormant, compiled-in, @0x-verified)

**Fragment 1 — dormant screen 2: an item-COMMAND-palette subscreen (not a box).**
Dispatch tables @0x80074be8 (init) / 0x80074bf4 (run) / 0x80074c00 (draw), entry [2] = {0x8004d7cc, 0x8004d904, 0x8004d96c} — spot-check-confirmed this session. It is a vestigial RE2-final-style verb-menu screen: status chrome + ARMS panel + the single 11-slot inventory grid @0x800b10ac at base (207,26) writes @0x8004d864/0x8004d870, plus one UNIQUE sprite template @0x800756f0 (element 0x16) pixel-decoded from DATA/TEX.TIM as CHECK / COMBN-USE-TAKE / EXIT at the status screen's button positions (x 94/126/158, y 166/182/198). Its run FSM (@0x8004d904, 26 instructions, re-disassembled this session) is gutted: draw, then (pad-held @0x800ac762 & 0x800) OR (pad-press @0x800ac76c & 0x8000) → phase byte 0x800b25bf++ = exit. No cursor, no verb dispatch, no second array, no deposit/withdraw. The TAKE label is TEX.TIM art only; its handler was removed before this build, not gated.
Reachability: selector DAT_800b25c0 is written =1 only @0x800431a8 (map-item use handler LAB_80043194, confirmed this session; entry 6 of use table @0x8007469c) and =0 @0x80046754. The value 2 is written NOWHERE in PSX.EXE, DEBUG.BIN, TITLE.BIN, or STAGE1-6.BIN (Reports A+B, independent scan methods, agreeing). The dispatch @0x800464b4-cc has no bounds check, so poking 0x800b25c0=2 executes it — same dormancy class as the save-card FSM FUN_80025c00.

**Fragment 2 — dormant save-persisted STORAGE of box-page geometry, with zero code.**
Four init-zeroed 8-slot x 4-byte arrays @0x800b1444/1484/14a4/14c4, cleared only by new-game init FUN_8003e4f4 (zero loop @0x8003e52c-554, confirmed this session; sole caller jal @0x8001d570). They sit inside the 0x1230-byte game-state block @0x800b0fbc that the dormant card FSM serializes wholesale (@0x800261c4-d4), so they WOULD persist to memory card. Geometry matches 4 pages x 8 visible slots (RE2 forces box view count to 8) in RE1.5's 4-byte slot format — but 0x20 bytes is also exactly one 256-bit flag zone (zones 3/8/9), and with ZERO readers/writers in any shipped binary and all-zero in all 10 savestates tested, their purpose is UNDECIDABLE. Also: the inventory arena reserves 5 unreferenced extra slots (0x800b10d8-0x800b10ec).

## What is MISSING (the method-backed negative)

No box CODE anywhere: (1) all five RE2 box instruction shapes absent from all 9 binaries (cmd-0x0e/0x0f compare, capacity-forced-8 write — capacity byte 0x800b0fbc has 19 xrefs, ALL reads, zero writers; two-array 3-byte transfer loop — full 18-instr-window scan, only intra-inventory swap @0x8004dc8c-0x8004de3c survived; free-slot-scanner FUN_8004df2c has exactly 3 callers, none box; no box array analog); (2) the inventory array's complete xref set (157 code sites, all in 0x8004641c-0x8004ed54) contains no transfer pairing — a functioning box MUST touch the inventory, so this enumeration closes the computed-pointer escape hatch; (3) no box strings (calibrated: RE2's shipped box screen is also textless, so this is only supporting), no box asset or CD-file-table entry, no unwired safe-room SCD sub (all 138 entries walked opcode-exact), no vestigial box sce-type game-wide; (4) DEBUG.BIN dev tool is read-only on items; (5) the dormant save system serializes no box block — asymmetry with the save system, which left a full 34-entry text bank @DEBUG.BIN:0x5f96 while the box left nothing.

## Cross-check of the four reports

- **A vs B on screen-2 identity**: B calls it "flat inventory-overview", A calls it "item command palette". Not a contradiction — B did not decode element 0x16's unique template @0x800756f0; A did (pixel-level). A's identification supersedes B's on the same underlying disasm, which agrees instruction-for-instruction (spot-checked). Both agree on the decisive point: reads only 0x800b10ac, no box semantics.
- **C vs A/B on DAT_800b25c0**: C flags it "NOT dormant, 101 refs" — consistent: the byte itself is live (screens 0/1 use it); only value 2 is unwritten. No conflict.
- **B's "capacity 10 vs campaign's 11-slot"**: reconciled by C — 10 pack slots @0x800b10ac + pre-slot cell DAT_800b10a8. Terminology only.
- **D's caveat** (string negative insufficient because RE2's box is textless) is answered by A+B's code-side closure of the screen-2 lead. All four reports are @0x-cited throughout; no UNVERIFIED claims are load-bearing.

## RECOMMENDATION for the campaign

**Do NOT implement an item box as "dormant RE1.5 revival" — there is nothing to revive.** Unlike the save system (real unused code: FUN_80025c00), the box has no code; any working box would be invention or an RE2 port. Wave-sized options, faithfulness-flagged:

1. **Wave 0 (done, keep): shipped message behavior** — the 16 safe-room "Itembox is not available in this preview" AOTs, already ctest-pinned. 100% byte-true RE1.5. This is the correct default for the byte-true mandate.
2. **Optional wave 1 (small, byte-true, dev-gated): revive dormant screen 2 as-is.** Wire an env/debug flag that sets the port's screen selector to 2; implement init/run/draw exactly per @0x8004d7cc/0x8004d904/0x8004d96c (elements 0x13-0x18, template @0x800756f0, geometry writes 0xcf/0x1a/0x7e/0xa6, 0x1d-step fade, exit masks 0x800/0x8000). Every byte is RE1.5-original; the only non-original bit is the trigger (flag as such, like the dormant save dev-overlay). Recommended pre-step: dynamic confirmation by poking 0x800b25c0=2 in a savestate (re15_ss_patch.py) and screenshotting — Report A's open question.
3. **Optional wave 2 (only if the user wants a FUNCTIONING box): explicit RE2-port, labeled as such** — port FUN_80068f9c/FUN_80069714/FUN_80069668 semantics (deposit 0x0e/withdraw 0x0f, count forced 8, compaction) exactly like the RE2 save port, storing pages in the dormant arrays @0x800b1444-14e4 IF byte-mapping the original save block. Flag: mechanics = RE2-ported; storage location = RE1.5-plausible but UNPROVEN (arrays' purpose undecidable); any wiring of the safe-room box object to open it = INVENTED (the shipped scripts only show the message). Do not merge behind anything but an explicit non-canonical toggle.
4. **Avoid entirely**: inventing verb handlers for the dead CHECK/COMBN/USE/TAKE/EXIT palette (its dispatch was stripped, not gated — no ground truth exists), or presenting any box as byte-true RE1.5.

Residual open leads (non-blocking): phase-table states 12-19 @0x80074c0c ff. not yet disassembled (dormant-code census completeness); initializer of capacity byte 0x800b0fbc; the un-zeroed 0x20 gap 0x800b1464-1484; sce=8 handler in FUN_80042bac (Report D's last suggestion — a compiled-in handler for an sce no RDT uses would be a dormancy signature, unchecked).

---
## Report: screen2

Screen-index-2 triplet {0x8004d7cc, 0x8004d904, 0x8004d96c} fully disassembled from raw MIPS (RE1.5 PSX.EXE). VERDICT: it is NOT an item-box screen and truly-empty-dead-code is also wrong — it is a DORMANT, statically unreachable "item command palette" subscreen: status-screen chrome + ARMS panel + the single 11-slot inventory grid @0x800b10ac drawn at base (207,26), plus one UNIQUE sprite panel (element 0x16, geometry @0x800756f0) pixel-decoded from TEX.TIM as a command cross CHECK / COMBN-USE-TAKE / EXIT at the status screen's button positions (x 94/126/158, y 166/182/198). Its run FSM is gutted to "draw + any button = exit" — no cursor, no sub-states, no second item array, no deposit/withdraw logic. It is a compiled-in remnant of an earlier RE2-final-style inventory-command UI, same dormancy class as the save-card FSM. The screen selector DAT_800b25c0 has exactly ONE non-zero writer in all shipped code (sb const 1 @0x800431a8 = the map-item use handler that opens dormant screen 1); no code in PSX.EXE, DEBUG.BIN, TITLE.BIN, or STAGE1-6.BIN ever writes 2 (verified by Ghidra xref enumeration + full-image opcode scans for direct-offset AND lui/addiu-constructed access). No dormant ITEM-BOX code exists in this subsystem: both dormant screens read only the single inventory array; nothing analogous to RE2's two-array box functions (FUN_80068f9c family) exists here.

### Screen dispatch mechanism [PSX.EXE (RE1.5, info/Re1.5/PSX.EXE)]
Status master task LAB_8004603c runs a linear phase table @0x80074bdc indexed by state byte 0x800b25bf (jalr [0x80074bdc+state*4] @0x80046084-0x8004608c, vsync 0x80029ac8(1), loop). State0=FUN_800460b8 common init, which dispatches the screen selector DAT_800b25c0 into the init table @0x80074be8 with NO bounds check (lbu 0x800b25c0 @0x800464ac; jalr [0x80074be8+sel*4] @0x800464c4-cc; then state++ @0x800464e8). State1=0x80046500 dispatches selector into run table @0x80074bf4 (@0x80046504-0x80046528). State2=0x80046540 is the fade-out phase: fade setup 0x800217b0(0x200,0x1800,7,0)+0x800216ec, then per-frame jalr [0x80074c00+sel*4] (draw) until 0x8002178c returns nonzero. Tables: init {0x80049524, 0x8004c54c, 0x8004d7cc}, run {0x8004974c, 0x8004cca4, 0x8004d904}, draw {0x80049a5c, 0x8004c830, 0x8004d96c}. Phase table continues with states 12-19 (0x8004b568, 0x8004b8a0, 0x8004b9d4, 0x8004bc9c, 0x8004bdd4, 0x8004bf70, 0x8004a9dc, 0x8004aa64) — other subscreen phases, not part of the 3-screen selector dispatch.

EVIDENCE: Raw disasm @0x8004603c-0x800460a0 (table base lui/addiu 0x80074bdc @0x80046044-48), @0x800464a8-0x800464e8, @0x80046500-0x8004653c, @0x80046540-0x800465ec; table words dumped from PSX.EXE file image @0x80074bdc-0x80074c28.

### Screen-2 init 0x8004d7cc (full decode) [PSX.EXE]
Init sequence: (1) builds RECT{x=0x1c0,y=0x100,w=0x40,h=0x100} on stack (@0x8004d7d8-0x8004d7f8) and calls 0x80068cec(rect, 0x801a0000) = VRAM(448,256,64x256) backup to RAM; (2) 0x80068a60(0); (3) loads UI elements 0x13..0x18 via loader FUN_800467a8 in a loop (s0=0x13, s1=0x19 @0x8004d80c-0x8004d828); (4) prim flushes 0x8004694c(0x80199800) and 0x80046a1c(0x80198000); (5) 0x80021634(2,0); (6) writes layout globals: slot-grid base DAT_800b25e0=0xcf(207) @0x8004d864, DAT_800b25e2=0x1a(26) @0x8004d870, panel base DAT_800b25e8=0x7e(126) @0x8004d87c, DAT_800b25ea=0xa6(166) @0x8004d898, zeros 0x800b25f8/0x800b25fa @0x8004d888/0x8004d890; (7) fade-in setup 0x800217b0(0x200,-1024,7,3) + 0x800216ec(0,0x2200,0x404040,0); (8) 29-iteration fade-in loop (s0=0x1d @0x8004d8bc) calling draw 0x8004d96c + vsync 0x80029ac8(1), plus one final draw @0x8004d8e4.

EVIDENCE: Raw MIPS @0x8004d7cc-0x8004d900 (jr ra @0x8004d8fc).

### Screen-2 run 0x8004d904 (full decode) — interaction logic is stripped [PSX.EXE]
The entire run FSM is 26 instructions: call draw 0x8004d96c; read pad-held halfword @0x800ac762, test bit 0x800 (@0x8004d918-0x8004d924); else read pad-press word @0x800ac76c, test bit 0x8000 (@0x8004d930-0x8004d93c); if either set, increment master state byte 0x800b25bf (@0x8004d94c-0x8004d958) which advances state 1->2 = fade-out+exit. NO cursor movement, NO sub-state byte usage, NO item read/write, NO second array, NO deposit/withdraw commands — despite the command-button labels it draws. This rules out item-box functionality: RE2's box (FUN_80068f9c, RE2-EXE) moves items between DAT_800d4a68 (box) and DAT_800d4a3c (inventory); screen 2 touches only the single RE1.5 inventory.

EVIDENCE: Raw MIPS @0x8004d904-0x8004d968 (jr ra @0x8004d964); RE2 reference RE2_Quellcode_V2/FUN_80068f9c.c (task brief).

### Screen-2 draw 0x8004d96c (full decode) [PSX.EXE]
Per frame: (1) redraw elements 0x13..0x18 via animator FUN_80047648 (loop @0x8004d984-0x8004d998); (2) loop slots 0..9: read item id from the SINGLE inventory array [0x800b10ac + slot*4] (@0x8004d9d4-0x8004d9dc), fetch icon byte from item property table [0x80074db0 + id*12] (@0x8004d9f4-0x8004d9fc), call icon/qty drawer FUN_80048f28(buf, slot, iconbyte, a3=0) — a3=0 skips the 2nd cell of 2-cell weapons (equip-byte==2 check @0x80048fa0-0x80048fa8); (3) prim flush 0x8006b538(0x800adca4+dbuf*0x1000, 0x800b2638+dbuf*12); (4) loop slots 0..9: FUN_80048704(slot) = slot frame SPRT at cell position table @0x80076274 (cells (4,32)..(44,152)) + base DAT_800b25e0/e2=(207,26), gated by inventory count @0x800b0fbc; (5) second flush with 0x800b2620. Draw buffers double-buffered by DAT_800aca34 (0x80199800/0x80199814 and 0x80198000/0x80198014).

EVIDENCE: Raw MIPS @0x8004d96c-0x8004dad8; FUN_80048704 @0x80048704-0x800487ac (cell table 0x80076274 @0x80048738, base 0x800b25e0/e2 @0x80048748/0x8004876c, count gate 0x800b0fbc @0x80048714-0x80048728); FUN_80048f28 @0x80048f28-0x80049054 (array 0x800b10ac @0x80048f74, qty byte 0x800b10ad with /100 and /10 magic-multiply digit extraction @0x80048fd0-0x80049054); cell table dumped from file: slots (4,32)(44,32)...(44,152) + slot10 (-66,88).

### Screen-2 identity: the unique element 0x16 = command palette CHECK/COMBN-USE-TAKE/EXIT (pixel-decoded) [PSX.EXE + DATA/TEX.TIM]
Element table @0x80074a8c (stride 12: {clut_idx, count, sprite-template ptr, prim-buf ptr}) rows 0x13-0x18: 0x13=chrome (template 0x80075108, 26 SPRTs — same as status-screen element 0), 0x14=ARMS panel (0x80075240, 15 — same as element 1), 0x15=element-5 clone (0x800754b0), 0x17/0x18=equip glyph clones (0x800754c8/0x800755e8 — same as elements 8/9), and 0x16=UNIQUE template @0x800756f0 (13 entries, stride 12: {i16 x, i16 y, u16 w, u16 h, u8 u, u8 v} per builder field mapping @0x80046830-0x8004689c): nine 32x16 tiles at x offsets -32/0/+32, y 0/16/32 from base (126,166) = screen x 94/126/158, y 166/182/198 — EXACTLY the status screen's command-button block positions — plus two 8x48 side rails at x=-40/+64 (uv 0xe8,0xb0). Rendering TEX.TIM (DATA/TEX.TIM, 4bpp 1280x256) at the tile UVs: (104,176)=CHECK, (40,208)=COMBN, (104,192)=USE, (72,176)=TAKE, (104,208)=EXIT, (40,192)=blank orange tile (used 4x at corners). Layout: CHECK on top, COMBN-USE-TAKE middle row, EXIT bottom. The same TEX strip also holds REMOVE/EXCNG/MANAGE/'Item list' labels used by other (retail-style) UI. Conclusion: screen 2 is a vestigial item-COMMAND subscreen (RE2-final-style verb menu over the inventory grid), not an item box.

EVIDENCE: Element table + templates dumped from PSX.EXE file image (@0x80074a8c rows, @0x800756f0 bytes); builder FUN_800467a8 field mapping @0x800467a8-0x800468a0 (SPRT code 0x64 @0x80046810, x->prim+8, y->prim+10, w/h->prim+16/18, u/v->prim+12/13, clut via [0x80074a8c+id*12]->DAT_800b2610[] @0x80046884-0x8004689c); base (126,166) from init stores @0x8004d87c/0x8004d898; button positions 94/126/158 x 166/182/198 match shots/inv_plan.md acceptance line 62; pixel decode: scratchpad tex_panel_clut2.png / panel_tiles.png rendered from re15_port/shared_assets/PSX/DATA/TEX.TIM.

### DAT_800b25c0 writer enumeration — selector can NEVER be 2 (exhaustive negative) [PSX.EXE + DEBUG.BIN + TITLE.BIN + STAGE1-6.BIN (all scanned)]
The screen selector byte 0x800b25c0 has exactly ONE non-zero writer in all shipped code: sb v1(=const 1) @0x800431a8, inside handler LAB_80043194 which also sets game-flag |=0x8000 in DAT_800aca3c (@0x800431ac-b0) and stores its item-param byte to 0x800b25cc (@0x800431c0) — this is the map-item 'use' handler that opens dormant screen 1. It is entry 6 of the 14-entry item-USE jump table @0x8007469c ({0x8004305c, 0x80043084, 0x800430bc, 0x800430f0, 0x80043120, 0x8004318c, 0x80043194, 0x800431cc, 0x8004330c, 0x80043328, 0x80043370, 0x800434b4, 0x80043500, 0x800435cc}), indexed from 0x80040824 and 0x80042f84/fac/fec/0x80043008. Only other writer: sb zero @0x80046754 (reset). All other Ghidra W-xrefs near the address (0x8004605c, 0x8004675c, 0x8004a400, 0x8004cdf0, 0x8004ce1c, 0x8004d488) target the sub-state bytes 0x800b25c1..c3, verified individually by raw disasm. SEARCH METHOD (why exhaustive): (a) grep of all '800b25c0' annotations in ghidra1_V2.txt (full EXE dump, catches every direct xref incl. the data-section XREF block @line 498932); (b) full-image 4-byte opcode scan of PSX.EXE + DEBUG.BIN (@0x800c0000) + TITLE.BIN + STAGE1-6.BIN (@0x80100000) for any load/store opcode with imm16 0x25c0/0x25bf/0x25cc -> only EXE hits plus ONE false positive in STAGE6.BIN @0x8011cbd8-area which is a data pointer 0x801025cc inside a 0x8010xxxx pointer table, not an instruction; (c) scan of all 9 binaries for lui 0x800b followed within 8 instructions by addiu/ori imm 0x25c0 (address construction for indexed access) -> 3 hits, all EXE READS (0x80046584 fade-out phase, 0x80046950 in FUN_8004694c, 0x80046b80 in FUN_80046a1c); (d) jal-scan of all 9 binaries for direct calls into {0x8004d7cc, 0x8004d904, 0x8004d96c, 0x8004c54c, 0x8004cca4, 0x8004c830, 0x80049524, 0x8004603c, 0x80043194} -> only the EXE's own internal calls (screen-2 init/run call their draw @0x8004d8c4/0x8004d8e4/0x8004d90c; screen-1 internal @0x8004c7d0/0x8004c800/0x8004cd44); (e) data-word scan for the addresses 0x80043194/0x8004603c as pointers -> 0x80043194 only @0x800746b4 (the use table), master task only via lui/addiu @0x8001cb3c (task spawner). Screen 2 is therefore statically unreachable = dormant, same class as the save-card FSM FUN_80025c00. It IS dynamically executable: poking 0x800b25c0=2 before the status task starts reaches it via the bounds-unchecked dispatch @0x800464b4-0x800464cc.

EVIDENCE: Raw disasm @0x80043194-0x800431c8; table dump @0x8007469c-0x800746d0; ghidra1_V2.txt XREF block lines 498925-498945; scan scripts run in-session over info/Re1.5/PSX.EXE and re15_port/shared_assets/PSX/BIN/{DEBUG,TITLE,STAGE1-6}.BIN.

### Screen 1 re-check (brief): map subscreen, no box content [PSX.EXE]
Screen 1 {0x8004c54c init, 0x8004cca4 run, 0x8004c830 draw} loads elements 0xc..0x12 (loop @0x8004c698, bound 0x13) — the 0x801bbXXX prim-buffer clones of status elements incl. equip glyphs. Draw 0x8004c830 reads ONLY the single inventory (0x800b10ac @0x8004c87c, equip byte 0x800b10ae @0x8004c958) plus map-view globals 0x800b25cf/0x800b25dc/0x800b25de. Run 0x8004cca4 has a sub-state jump table (jr v0 @0x8004cdc4) on byte 0x800b25c1; sub-states include the slide-out animator @0x8004d448-0x8004d4c0: panel y 0x800b25ea -=5 while >=167, then writes 0x80 to 0x800b25cf and zeros 0x800b25c1; slot-grid base 0x800b25e0 +=6 per frame while <340 (slides the item grid off-screen right). It also runs a mini script-VM @0x8004d4c4 (task block 0x800b2b4c+0xe60, opcode-handler table @0x800744a8, opcode dispatch @0x8004d518-0x8004d534). Nothing reads or writes a second item array — consistent with the prior 'standalone MAP/sce-6 close-up' identification; zero box-related content.

EVIDENCE: Raw disasm @0x8004c680-0x8004c6b4, @0x8004c830-0x8004ca08 (skim of data refs), @0x8004cca4-0x8004cdc8, @0x8004d420-0x8004d4c0, @0x8004d4c4-0x8004d5ac.

### Adversarial sweep: no two-array transfer code anywhere near the inventory array [PSX.EXE]
Every code reference to the inventory array 0x800b10ac in the EXE was enumerated (53 sites): status-screen UI (0x80046xxx-0x80049xxx), the two dormant screens (0x8004c884, 0x8004d9dc), and the known item-ops library cluster 0x8004db4c-0x8004ed34 (insert FUN_8004dc4c, combine/mix graph, classifiers — already covered by the inventory campaign). No site pairs 0x800b10ac with a second item-array base in a transfer loop. Combined with: RE2's box function names/addresses (FUN_80068f9c/FUN_80069714/FUN_80069668, DAT_800d4a68) having 0 counterparts in ghidra1_V2.txt (task brief), and all 16 safe rooms wiring the box object to a message-only AOT — the engine-side item-box verdict stands, now with the screen-2 gap closed: the closest thing to dormant box UI is the CHECK/COMBN/USE/TAKE/EXIT command palette of screen 2, which contains a TAKE verb label but no transfer mechanics.

EVIDENCE: grep enumeration of '800b10ac' code addresses in ghidra1_V2.txt (list in-session); FUN_8004dc4c identity from memory reai-v2-inventory-pickup (pure INSERT).

- OQ: What TAKE would have DONE in the dead palette cannot be determined statically — the command-dispatch code was stripped from the run FSM (0x8004d904 has no verb handling at all); only the TEX.TIM label and layout survive. If its handler ever existed, it was removed before this build, not gated.
- OQ: Phase-table states 12-19 (0x8004b568, 0x8004b8a0, 0x8004b9d4, 0x8004bc9c, 0x8004bdd4, 0x8004bf70, 0x8004a9dc, 0x8004aa64) were not disassembled in this pass — they are other subscreen phases (likely FILE viewer / pickup-modal phases) sharing the master table; worth a pass if the dormant-code census should be complete.
- OQ: Dynamic confirmation not yet run: poke 0x800b25c0=2 in a stage_saves savestate RAM (re15_ss_patch.py) before pressing START and screenshot the resulting screen — would visually confirm the decoded composition (chrome + ARMS + inventory grid at (207,26) + CHECK/COMBN/USE/TAKE/EXIT cross at (94..190,166..214)).
- OQ: Identity of the pad buttons behind exit masks 0x800 (held word 0x800ac762) and 0x8000 (press word 0x800ac76c) not resolved to physical buttons in this pass.
- OQ: Element 0x15 (template 0x800754b0, 2 sprites, uv(0x60,0xf0)) — which status-screen element 5 represents (some 32x16 button/label) was not pixel-identified; shared with status screen so not screen-2-specific.
- OQ: The CLUT actually used at runtime for element 0x16 (clut_idx 2 -> DAT_800b2610[2], VRAM (256,490) per the campaign's 0x7a10..0x7bd0 series) was approximated with TEX.TIM's own CLUT #2 for the pixel decode; label READING is unambiguous but exact colors unverified.


---
## Report: codeShape

ADVERSARIAL RE-VERIFY of "engine-side ABSENT" for RE1.5 item-box code — the negative HOLDS, now from raw bytes with stated coverage. Five independent shape hunts (RE2 cmd-0x0e/0x0f compare pair, capacity-forced-8 write, two-array 3-byte transfer loop, free-slot-scanner callers, box-array address analog) all come up empty across PSX.EXE, DEBUG.BIN, TITLE.BIN and all six STAGE overlays. The HOT LEAD is resolved: the unidentified screen-2 triplet {0x8004d7cc init, 0x8004d904 run, 0x8004d96c draw} dispatched via DAT_800b25c0=2 is a DORMANT standalone flat inventory-overview screen — it loads TIM pages 0x13-0x18 + two double-buffered prim sets (0x80198000/0x80199800, +0x14 per parity byte 0x800aca34), fades in over 0x1d steps, draws the 10 main-inventory slots (reads ONLY 0x800b10ac, icon table @0x80074db0 stride 12), and exits on one button (held 0x800 @0x800ac762 / press 0x8000 @0x800ac76c → phase byte 0x800b25bf++). It has NO second storage array, no deposit/withdraw, no capacity change — it is NOT an item box. DAT_800b25c0 is written =1 only by sce-command-6 handler LAB_80043194 (@0x800431a8, which also sets freeze bit 0x8000 on DAT_800aca3c and param→0x800b25cc) and =0 by the master close (@0x80046754); the value 2 is written NOWHERE in EXE, overlays, DEBUG.BIN or TITLE.BIN — screen 2 is compiled-in, dispatchable (no clamp @0x800464b4), designed-dead. The dormant dev save system serializes the inventory only as part of one monolithic 0x1230-byte game-state block (base 0x800b0fbc @0x800261c8) — no box block. DEBUG.BIN's dev status tool (5-state FSM, table @0x800c6864) only READS inventory[cursor] and never writes any item array. Unlike the earlier unreliable negatives, every claim here is instruction-cited and every scan states its method.

### VERDICT: no dormant item-box code in RE1.5 [PSX.EXE (RE1.5), cross-ref RE2_Quellcode_V2/FUN_80068f9c.c + FUN_80069714.c + FUN_80069668.c]
RE1.5 has no compiled-in item-box analog of RE2's FUN_80068f9c/FUN_80069714/FUN_80069668 — not in PSX.EXE, not in the dormant memcard/dev save family, not in DEBUG.BIN, not in STAGE1-6/TITLE overlays. All five RE2 instruction shapes are absent. This is unlike the dormant save system (which exists as real unused code): for the box, NOTHING exists — no second item array, no transfer code, no capacity swap, no box FSM.

EVIDENCE: Shape 1 (cmd-0x0e/0x0f pair, RE2 @0x80068f9c 'param_1-0xe<2'): exhaustive grep of ghidra1_V2.txt (full EXE disasm) for 'addiu ...,-0xe' finds exactly 4 sites — 0x8001df40, 0x8004a3f0, 0x8004a43c, 0x8004dc70 — ALL followed by sltiu 0x6 (weapons-id range 0x0e..0x13) or a debug item-id wrap writing DAT_800b25ee, none by sltiu 0x2. Shape 2 (capacity forced 8, RE2 'DAT_800d46ac=8'): RE1.5 capacity byte DAT_800b0fbc has 19 XREFs in ghidra1_V2.txt, ALL lbu reads except one addiu pointer-formation @0x800261c8 (save-serializer block base) — ZERO writers, so no box-mode capacity swap can exist. Shape 3 (two-array 3-byte transfer loop): custom scanner over every 18-instr window of PSX.EXE+DEBUG.BIN+TITLE.BIN+STAGE1-6.BIN for >=3 lbu at offsets K,K+1,K+2 plus >=3 sb at L,L+1,L+2 (K!=L): only hits are the known intra-inventory weapons 2-cell front-shift/swap @0x8004dc8c-0x8004de3c (src AND dst both inside 0x800b10ac..0x800b10d2), GPU-prim RGB copies (0x8006c390 ff., r0g0b0->r1g1b1), a pixel-blend (0x80024494, srl>>1 channel averaging), and a 32-byte-record field unpack (0x80059964, records via ptr 0x800b2b3c -> globals 0x800b53xx); overlays/DEBUG/TITLE: zero hits. Shape 4 (free-slot scanner, RE2 FUN_80069668 returns 0xffffffff): RE1.5 scanner FUN_8004df2c (bounds by DAT_800b0fbc) has exactly 3 callers per XREF — 0x8001df14 (item-get grant), 0x8004daec (remove/compact), 0x8004debc (insert) — no box enter/exit caller. Shape 5 (box array adjacency, RE2 box DAT_800d4a68 = inventory DAT_800d4a3c+0x2c): RE1.5 inventory 0x800b10ac+0x2c = 0x800b10d8; that region is owned by unrelated FUN_80037c1c (GPU prim-struct base s1=0x800b10ec, writes @0x80037c48/0x80037c74/0x80037c84, table 0x80074208).

### Inventory-array coverage (why the negative is exhaustive) [PSX.EXE via ghidra1_V2.txt]
Every code reference to the RE1.5 inventory array in the entire EXE lies inside the status/inventory module 0x8004641c..0x8004ed54; no other module (memcard family 0x80024000-0x80027000, room/AOT, sound) touches item storage. All inventory WRITES are the already-identified ops (insert, remove/compact, swap/reorder, combine-result, delete-zero). A working deposit/withdraw would have to appear in this XREF set — it does not.

EVIDENCE: ghidra1_V2.txt XREF enumeration: 160 label occurrences DAT_800b10ac/ad/ae at 157 distinct code addresses, min 0x8004641c max 0x8004ed54 (bucketed: 0x80046xxx:3, 47:3, 48:3, 49:14, 4a:15, 4b:4, 4c:4, 4d:23, 4e:88). Distinct 0x800b1xxx globals used by the whole module: only 0x800b10a8 (pre-slot/equip cell, 3 refs) + slots 0x800b10ac..0x800b10d2 — no second array label. Inventory sb writes: 0x800497b4, 0x8004a130, 0x8004a1f0/0x8004a204 (combine), 0x8004aef0/0x8004af0c/0x8004af28 (delete), 0x8004db60/84/a8 (compaction in FUN_8004dadc), 0x8004dc0c/1c/2c (zero last), 0x8004de6c-0x8004de88 (swap), 0x8004dee4/f4/04 (insert core); all byte ops — no lw/sw access to the array exists in the XREF list, so word-copy transfers are excluded too.

### HOT LEAD resolved: screen-2 triplet identity [PSX.EXE]
The never-identified screen-2 dispatch entry {LAB_8004d7cc init, 0x8004d904 run, 0x8004d96c draw} (tables @0x80074be8/0x80074bf4/0x80074c00, confirmed: [2]=0x8004d7cc, [5]=0x8004d904, [8]=0x8004d96c) is a DORMANT standalone flat inventory-overview screen. It displays the current 10-slot main inventory as an icon strip over a dedicated 2-prim-set background and exits on a single button press. It reads ONLY 0x800b10ac — no second array, no item mutation, no box semantics. inv_plan.md §5's 'unreachable/dead code' flag is CONFIRMED and the identity gap is now closed.

EVIDENCE: Init @0x8004d7cc: loads TIM pages 0x13..0x18 via jal 0x800467a8 (loop @0x8004d814, s0=0x13,s1=0x19), prim sets via jal 0x8004694c(a0=0x80199800) @0x8004d830 and jal 0x80046a1c(a0=0x80198000) @0x8004d83c, writes screen geometry 0xcf/0x1a/0x7e/0xa6 to 0x800b25e0/e2/e8/ea, then fade-in loop s0=0x1d down to 0 calling 0x80029ac8(1) per step (@0x8004d8bc-0x8004d8e0). Draw @0x8004d96c: loop i=0..9 (sltiu v0,v0,0xa @0x8004da10) reads slot id lbu [0x800b10ac+i*4] (@0x8004d9dc), icon cell lbu [0x80074db0+id*12] (@0x8004d9fc), draws via jal 0x80048f28; background pair 0x80199800/0x80199814 and 0x80198000/0x80198014 selected by frame-parity byte 0x800aca34 (NOT player — 0x14-byte double-buffer stride); links prims 0x800b2620/0x800b2638(+parity*12) into the OT via jal 0x8006b538 = libgpu AddPrim (verified: masked pointer-link code @0x8006b538-0x8006b570). Run @0x8004d904 (26 instr total): draw, then if (held @0x800ac762 & 0x800) or (press @0x800ac76c & 0x8000) increment phase byte 0x800b25bf (@0x8004d944-0x8004d958) — master task exits on phase advance.

### DAT_800b25c0 writers — screen 2 is designed-dead but dispatchable [PSX.EXE + STAGE1-6.BIN + TITLE.BIN + DEBUG.BIN]
The screen selector byte DAT_800b25c0 is only ever written =1 (opens the dormant sce-6 close-up/MAP screen) or =0 (close). The value 2 is written NOWHERE in the EXE, in any STAGE1-6 overlay, in TITLE.BIN, or in DEBUG.BIN. The master dispatch has NO bounds clamp, so the screen-2 code is genuinely compiled-in and callable — classic designed-dead code, same category as the dormant save FSM, but it is an inventory-display screen, not a box.

EVIDENCE: Writers per full DAT_800b25c0 XREF grep of ghidra1_V2.txt: @0x800431a8 'ori v1,zero,0x1; sb v1,9664(at)' inside handler LAB_80043194 (which also ORs 0x8000 into DAT_800aca3c freeze flag @0x800431ac-b0 and stores param byte a0->0x800b25cc @0x800431c0); @0x80046754 'sb zero' (master close); 0x8004605c/64/6c zero only bytes +1/+2/+3. LAB_80043194 is entry 6 of the event-command pointer table @0x8007469c (entry @0x800746b4), dispatched by the command executor @0x80042f74-0x80042f90 (cmd byte s0[0] indexes table, jalr). Dispatch no-clamp: @0x800464a8-0x800464cc 'lbu 0x800b25c0; sll 2; lw [0x80074be8+idx]; jalr' — no sltiu before use. Overlay/TITLE coverage: byte-level scanner over all STAGE1-6.BIN + TITLE.BIN + DEBUG.BIN for any store with base formed by lui 0x800b (window 9 instr) to offsets 0x25bf-0x25ff: STAGE/TITLE zero hits; DEBUG.BIN hits only 0x25c1,0x25c2,0x25c3,0x25c4,0x25cd,0x25d6,0x25d7 (cursor/phase bytes) plus pointer to 0x25bf @0x800c6588 — never +0 (0x25c0).

### Dormant dev save system: inventory saved only as monolithic block, no box block [PSX.EXE]
The dormant memcard/dev save family serializes the inventory only as an undifferentiated part of one 0x1230-byte game-state block starting at the capacity byte 0x800b0fbc — there is no separate box block in the save layout, and the region contains no box-like sub-FSM (the only transfer-shaped code there is a pixel blend).

EVIDENCE: @0x800261c4-0x800261d4: 'lui a1,0x800b; addiu a1,a1,4028 (=0x800b0fbc); ori a2,zero,0x1230; jal 0x8004ee38' — block base 0x800b0fbc, size 0x1230 (covers 0x800b0fbc..0x800b21ec incl. inventory 0x800b10ac). The EXE-wide transfer-loop scan's only hits in 0x80024000-0x80027000 are 0x80024494/0x8002456c = RGB channel averaging (addu+srl>>1 per byte), not item copies.

### DEBUG.BIN dev status tool: read-only on items, no box [DEBUG.BIN]
DEBUG.BIN (@0x800c0000) contains a dev status-screen driver with a 5-state FSM, but it only READS the single inventory (item id at cursor) and the capacity byte; it never writes any item array and hosts no second item storage — no dev item-box tool exists.

EVIDENCE: Full-BIN scan for lui-0x800b-based accesses to 0x0fbc..0x1100: exactly 2 hits — lbu capacity @0x800c63e0 and the getter @0x800c6600-0x800c6628 'lbu cursor 0x800b25bd; if !=10 return lbu [0x800b10ac+cursor*4]'. Dev FSM: dispatcher @0x800c6630-0x800c665c 'lbu 0x800b25d6; jr [0x800c6864+state*4]', table @0x800c6864 has 5 entries {0x800c6664, 0x800c6704, 0x800c6760, 0x800c67b8, 0x800c67ec} (entry 5+ is code, not pointers). It advances the shared phase byte 0x800b25bf on the same pad-held bit 0x800 (@0x800c6560-0x800c6598) the real screen uses. Transfer-loop scan of the whole BIN: zero hits.

### Runtime ground truth (savestate) [PSX.EXE + stage_saves/equip_test.sav]
Live RAM confirms the static picture: capacity byte = 0x0a (10 slots), the inventory at 0x800b10ac holds 4-byte entries (id,count,flag,pad), the would-be box region after slot 10 is zeros/unrelated, and screen-2's prim buffers contain live GPU packet links into the 0x80198xxx/0x80199xxx asset buffers. Note: capacity reads 10, not 11 — the campaign's '11-slot' count matches 10 pack slots + the separate pre-slot cell DAT_800b10a8 (3 XREFs) directly before the array.

EVIDENCE: stage_saves/equip_test.sav via re15_ss.py Ram: [0x800b0fbc]=0x0a; [0x800b10ac..]=01 00 00 00 / 03 0f 00 00 / 15 32 00 00, rest zero; [0x800b2620]=c4 89 19 02 ... b0 89 19 02 (prim link pointers into 0x801989xx = the buffer loaded by init @0x8004694c(0x80199800)); draw loop bound sltiu 0xa @0x8004da10 matches capacity 10.

### Method note for the campaign (negative-verdict reliability) [all]
This re-examination upgrades the earlier 'engine-side ABSENT' from a name/constant-grep negative to an instruction-shape negative with stated coverage. Caveats that remain open by construction: (a) the byte-window scanners catch lui-0x800b-based and 18-instr-window patterns only — a transfer routed entirely through computed pointers with no inventory XREF would evade them, but such code could not function as a box (deposit/withdraw must touch the inventory, whose XREF set is fully enumerated); (b) DEBUG.BIN function space was audited via targeted scans + the dev-FSM table, not a full manual disasm of all ~128KB.

EVIDENCE: Scan scripts run in-session (scratchpad, reproduced in the fact texts): (1) grep 'addiu ...,-0xe' whole ghidra1_V2.txt; (2) DAT_800b0fbc/DAT_800b10ac..ae/DAT_800b25c0/DAT_800b10d8-10fc full XREF pulls; (3) two-array 3-byte transfer window-scan over PSX.EXE + 8 BINs; (4) lui-0x800b store scans (offsets 0x25bf-0x25ff and 0x0fbc-0x1100) over all BINs; (5) manual disasm verification of every surviving hit (0x80024494, 0x80059964, 0x80115a90 STAGE4 false positive = entity-struct lw via ptr field +0x188, 0x8006c390 prim RGB).

- OQ: Screen-2 lineage: which historical UI is the flat inventory-overview screen a remnant of (an earlier standalone ITEM tab predating the composite status screen, or a game-over/result item recap)? The prim-set files loaded into 0x80198000/0x80199800 by 0x8004694c/0x80046a1c would name it — tracing which CD file fills those buffers (likely the same status-screen ITP package as TIM pages 0x13-0x18) was not completed.
- OQ: Who initializes the capacity byte 0x800b0fbc to 0x0a at runtime? No direct writer exists in the EXE (all 19 XREFs are reads); it must be set via an indexed/block write (new-game state template or per-character init). If capacity is per-character (Leon 10 vs Elza 8, as some RE1.5 preview material suggests), the initializer is worth finding for the port.
- OQ: The pre-slot cell DAT_800b10a8 (3 XREFs, sits 4 bytes before slot 0): confirm its role (equipped-weapon cell vs sentinel) and reconcile with the campaign's '11-slot' terminology.
- OQ: Event-command table @0x8007469c (10+ entries, executor @0x80042f74): entries other than cmd 6 (LAB_80043194 = open sce-6 close-up screen) were not audited in this pass; a full identification would close the last 'unknown dispatch' surface of the status subsystem.


---
## Report: dataHunt

TASK C verdict: RE1.5's dormant save system DOES reserve serialized storage far beyond the 11 inventory slots, and inside that reserve sit four init-zeroed 8-slot x 4-byte arrays (0x800b1444/1484/14a4/14c4) that no shipped code ever reads or writes — geometry exactly matching one RE2 box-view page (8 visible slots) times 4, in RE1.5's 4-byte inventory-slot format. The dormant card FSM (FUN_80025c00) serializes the whole 0x1230-byte game-state block @0x800b0fbc wholesale (raw MIPS @0x800261c4-d4), so these arrays — and a ~4KB otherwise-unreferenced hole 0x800b11d4-0x800b21cc — would persist to memory card. However, adversarial exhaustion (Ghidra xref extraction + custom constant-propagation pointer scan over PSX.EXE/DEBUG.BIN/TITLE.BIN/STAGE1-6.BIN + string scan + 10-savestate union) found ZERO readers, writers, box strings, or pre-initialized item data: the arrays are equally consistent with four spare 256-bit flag banks (same 0x20 geometry as flag zones 3/8/9). So: dormant box-sized, save-persisted STORAGE exists; dormant box CODE does not exist engine-side in the shipped binaries (method-backed negative). Also mapped: inventory arena is actually 16x4 bytes (slots 11-15 unreferenced), and the status-screen globals block 0x800b25xx lies OUTSIDE the save block — a functioning box could not have stored its contents there.

### Dormant save record layout (C.1) [PSX.EXE (info/Re1.5/PSX.EXE, file_off=0x800+addr-0x80010000)]
The dormant RE1.5 save writer serializes ONE contiguous record: 0x200 header workspace @0x800b0dbc + 0x1230 game-state block (GSB) @0x800b0fbc..0x800b21ec = 0x1430 bytes total. Save path (FUN_80025c00 case 8) copies the GSB with a single bulk memcpy; load path (case 9) copies 0x1430 back to 0x800b0dbc. Everything in the GSB persists — including ~0x1000 bytes of never-referenced BSS, i.e. the record DOES reserve space far beyond the 11 inventory slots.

EVIDENCE: Raw MIPS @0x800261c4: lui a1,0x800b; @0x800261c8: addiu a1,a1,4028 (=0x800b0fbc); @0x800261d0: ori a2,zero,0x1230; @0x800261d4: jal 0x8004ee38 (memcpy). Header base @0x800261e4-ec: t0=0x800b0fbc, s0=t0-0x200=0x800b0dbc. Load: FUN_80025c00 case 9 = FUN_80027368(buf,dir+slot*0x16,0x800,0) then FUN_8004ee38(&DAT_800b0dbc,buf,0x1430). Dir signature 'BISLPS-0022'+slot digit built in case 7 (words 0x4c534942/0x302d5350/0x32323230 @0x800260xx); save counter DAT_800b0fbd++ on success (case 8).

### Four dormant 8-slot arrays inside the save block (C.2 — the storage candidate) [PSX.EXE]
New-game init FUN_8003e4f4 zeros four 8-word (0x20-byte) arrays at 0x800b1444, 0x800b1484, 0x800b14a4, 0x800b14c4 — inside the serialized GSB (offsets +0x488/+0x4c8/+0x4e8/+0x508). This init is the ONLY code in any shipped binary that touches them: Ghidra never even created DAT_ symbols (raw '??' bytes, 0 xrefs). Geometry = 4 pages x 8 slots x 4 bytes: 8 matches RE2's box-screen forced visible count (DAT_800d46ac=8 in FUN_80068f9c) and 4 bytes matches RE1.5's inventory slot {id,qty,kind,pad}. BUT 0x20 also equals the standard flag-zone size (zones 3/8/9 are 0x20), so the arrays are equally consistent with four spare 256-bit flag banks — no reader exists to discriminate. Curiosity: the 0x20 gap 0x800b1464-0x800b1484 between arrays 1 and 2 is NOT zeroed by the init.

EVIDENCE: Pointer construction @0x8003e504-518: lui v0,0x800b; addiu v0,v0,-1100 (=0x800afbb4); addiu t0,v0,5316 (=0x800b14c4); addiu a3,v0,5284 (=0x800b14a4); addiu a2,v0,5252 (=0x800b1484); addiu a1,v0,5188 (=0x800b1444). Zero loop @0x8003e52c-550: sw zero to all four, 8 iterations (sltiu v0,a0,0x8 @0x8003e54c), +4/iter. Sole caller: jal 0x8003e4f4 @0x8001d570 (game bootstrap chain, also calls FUN_80043ae0/FUN_80045df8). ghidra1_V2.txt lines 494266/494330/494362/494394: addresses listed only as undefined '00h' bytes, no XREF. Same init also clears flag zones 4/5/6/10 (0x800b1018+0x80 loop @0x8003e55c-580, 0x800b1030 @0x8003e58c, 0x800b1028 @0x8003e5bc) and calls FUN_8003ebf4 (work_vars 0x800b0fd0-6 = 0xffff).

### GSB interior map around the inventory (C.2) [PSX.EXE]
GSB layout: +0x00 misc bytes (0x800b0fbc char?, 0fbd=save count), +0x14 SCD work_vars (0fd0-0fd6), +0x24 stage (0fe0), +0x26 room (0fe2, 137 xrefs), +0x3c..+0xef flag zones 3-10+13 per op_set pointer table, +0xf0 inventory. The inventory arena is 16 slots x 4 bytes (0x800b10ac-0x800b10ec): only 11 slots are ever addressed; the 0x14 bytes 0x800b10d8-0x800b10ec (slots 11-15) have ZERO references anywhere — a 5-slot reserve bounded exactly by the next struct. Then: 0x800b10ec-0x800b11cc = 8 x 0x1c display-primitive array (FUN_80037c1c — render state, serialized incidentally); 0x800b11cc-11d4 = light struct read by FUN_80039b2c (NPC light init, catalog-confirmed); 0x800b11d4-0x800b21cc = the 0xff9-byte dormant hole; tail 0x800b21cc-21ec = live fields (0x800b21cc is a scratch pointer in config table @0x80073e28 [entry 6, function ptrs 0x80050cb8..0x80051024 = shared NPC lib]; 21d4/d6/e8/ea written @0x8002e6a4-710).

EVIDENCE: Flag-zone table @0x80074664 decoded: [3]=0x800b0ff8 [4]=0x800b1018 [5]=0x800b1028 [6]=0x800b1030 [7]=0x800b1038 [8]=0x800b1058 [9]=0x800b1078 [10]=0x800b1098 [13]=0x800b10a8. Inventory slot walk @0x8004dca8-0x8004dea8 (lbu/sb DAT_800b10ac+slot*4, bytes +0/+1/+2). FUN_80037c1c: lui s1,0x800b; addiu s1,s1,4332 (=0x800b10ec) @0x80037c2c-30; loop bound sltiu v0,s2,0x8 @0x80037cf0; stride addiu s0,s0,28 @0x80037cf8 => ends 0x800b11cc. Ghidra symbol-table extraction over 0x800b0dbc-0x800b2700 (261 symbols): no symbol in 0x800b10d8-10eb and none in 0x800b11d4-0x800b21cb.

### Exhaustive negative: no reader/writer of the hole (method statement) [PSX.EXE + DEBUG.BIN + TITLE.BIN + STAGE1-6.BIN (all shipped code)]
No code in ANY shipped binary reads or writes 0x800b11d4-0x800b21cc (beyond the four-array init) or the inventory reserve 0x800b10d8-10ec. Every candidate hit was individually disassembled and refuted. One incidental access exists: the equip-snapshot path reads inv[0x80*4]=0x800b12ac via the 0x80 'no item' sentinel (an out-of-bounds READ into the hole, live value 0x00 — already pinned in the port, menu_common.c:490).

EVIDENCE: Methods: (1) Ghidra full-dump (ghidra1_V2.txt) symbol/xref extraction over the range — zero symbols in the hole. (2) Custom two-pass constant-propagation scanner (lui+addiu, lui+ori, lui+mem-offset, plus 32-bit literal pointer words) over every word of PSX.EXE, DEBUG.BIN, TITLE.BIN, STAGE1-6.BIN. All hits resolved: @0x80030cdc & @0x80030f30 'addiu a0,s0,0x45ce' = flag zone 0x800b1028 (Ghidra path-sensitive: s0=0x800aca5a on the executed path; my linear sweep took the s0=0x800acc10 path @0x80030b70-74; 0x800b11de would be word-misaligned for FUN_8004ef90's lw/sw — impossible); @0x8001e0c8 = room-buffer ptr lw DAT_800ac77c @0x8001e0bc + 0x21a0, not a BSS address; STAGE3 @0x8011d370 = data misdecoded as code (surrounded by invalid opcodes @0x8011d34c-36c). (3) FUN_8004ef90 confirmed as flag-bit-SET (srl a1,5; sll 2; lui 0x8000; srlv; or; sw @0x8004ef90-b4). Caveat stated: linear sweep is path-insensitive, but materializing these addresses requires lui-based construction or a literal pointer word — both scanned across all 9 binaries.

### RE2 comparison: what real box storage looks like (C.2 prerequisite) [RE2-Leon retail (ghidra_re2_Leon.txt)]
RE2-Leon's actual box storage is DAT_800a93d0 — a 3-byte-stride array, PRE-INITIALIZED in .data (first byte 03h = an item id, i.e. the box starts stocked), with exactly one xref (the box-screen page swap). The box screen swaps 11 slots at a time between inventory 0x800d4a3c and backup 0x800ce5e4 and forces visible capacity to 8. RE1.5 has NO analogous pre-initialized item array anywhere in its data segment — its four candidate arrays are BSS-zeroed. RE2's known DAT_800d4a68 (inv+0x2c) is box-screen state, not the storage. RE1.5's address equivalent of inv+0x2c (0x800b10ac+0x2c=0x800b10d8) is the unreferenced 5-slot inventory reserve.

EVIDENCE: ghidra_re2_Leon.txt line 336624-5: DAT_800a93d0 XREF[1]: FUN_80068f9c:800690a8(R), value '= 03h'. RE2_Quellcode_V2/FUN_80068f9c.c: puVar4=&DAT_800cc1e8, +0x8854=0x800d4a3c (inventory), +0x23fc=0x800ce5e4 (backup), loads page from (&DAT_800a93d0)[(iVar2+i)*3] with iVar2 in {0,0xb}, DAT_800d46ac=8 forced. DAT_800d4a68 xrefs: 23, all in 0x80070488-0x80072f70 (box screen code).

### Savestate check (C.4): empty-but-present storage [DuckStation savestates (PSX RAM)]
The entire hole 0x800b11d4-0x800b21ec — including the four arrays at 0x800b1444-14e4 — is ALL ZERO in every savestate tested (10 saves spanning boot, briefing, combat, death, inventory-open, load-game, and the 6 numbered HASH progress saves). Union of nonzero bytes across all 10 = empty set. This is exactly the 'empty-but-present' signature: allocated, save-serialized, init-cleared, never populated.

EVIDENCE: stage_saves read via .claude/skills/re15-savestate-ghidra/scripts/re15_ss.py Ram class: mzd_inv_open.sav, mzd_after_flow.sav, mzd_stage1_combat_death.sav, mzd_stage1_dog.sav, mzd_stage1_briefing_live.sav, mzd_death_cmd7_youdied.sav, equip_test.sav, nav_loadgame.sav, HASH-881C08B8082E53B6_1.sav, HASH-881C08B8082E53B6_6.sav — nonzero count 0 in all. Live inventory in mzd_inv_open.sav for contrast @0x800b10ac: slot0=01 (knife), slot1=03 0f (handgun,15), slot2=15 32 (ammo id 0x15, qty 50), slots 3-10 + reserve slots 11-15 all zero.

### Status-screen globals 0x800b25xx (C.3) — cannot be box storage [PSX.EXE]
The status-screen globals block 0x800b25bc-0x800b2696 lies OUTSIDE the serialized save block (GSB ends at 0x800b21ec) and is densely referenced; its unreferenced gaps are tiny (0x800b25a8-25b4, 0x800b25b5-25bc, and 0x40-byte strides 0x800b24b8-24f8/2500-2540/2544-2584 which are per-screen register banks). Nothing box-sized or save-persisted lives there — so a functioning item box could NOT have kept its contents in the status-screen block; the only save-persisted candidate storage is the GSB hole. DAT_800b25c0 (screen selector, the campaign hot lead) is NOT dormant: 101 references in ghidra1_V2.txt (my gap table missed its definition line due to its XREF-line format — corrected by direct grep).

EVIDENCE: Save block bound: 0x800b0fbc+0x1230=0x800b21ec (ori a2,0x1230 @0x800261d0). Symbol density from ghidra1_V2.txt extraction: 0x800b21ec-0x800b2696 continuous small fields, e.g. DAT_800b25bc-25bf (29-48 xrefs), DAT_800b260d (114 xrefs). grep -c DAT_800b25c0 ghidra1_V2.txt = 101.

### No box strings anywhere (adversarial cross-check) [PSX.EXE + DEBUG.BIN + TITLE.BIN]
Zero occurrences of 'box' (case-insensitive, ASCII) in PSX.EXE, DEBUG.BIN, or TITLE.BIN — unlike the dormant save system, no dev-menu text hints at a box screen. The known 'Itembox is not available in this preview' text lives only in room RDT message data (SCD Message_on), not in any engine binary.

EVIDENCE: Python re.finditer(rb'[\x20-\x7e]{3,}') + /(?i)box/ over info/Re1.5/PSX.EXE (full file), re15_port/shared_assets/PSX/BIN/DEBUG.BIN, re15_port/shared_assets/PSX/BIN/TITLE.BIN: 0 hits.

- OQ: What the four 8x4-byte arrays @0x800b1444/1484/14a4/14c4 were FOR is undecidable from the shipped bytes (item-box pages vs. four spare 256-bit flag banks vs. other cut state) — no reader exists. Next leads: diff against other RE1.5 dumps/builds if any surface, or check whether RE2 trial/beta EXEs place box storage at the structurally equivalent GSB offset (+0x488).
- OQ: Why the 0x20-byte gap 0x800b1464-0x800b1484 between arrays 1 and 2 is skipped by the init zero loop (a fifth array that lost its clear? an 0x40-sized array 0?).
- OQ: Identity of the light-ish struct 0x800b11cc-11d4 read by FUN_80039b2c inside the save block (a serialized room-light state?).
- OQ: Who writes DAT_800b25c0=2 (screen-2 dispatch {LAB_8004d7cc,0x8004d904,0x8004d96c}) — outside Task C scope, assigned to the screen-dispatch agent; note DAT_800b25c0 has 101 refs so the writer set is enumerable from the Ghidra dump.
- OQ: The GSB serializes live render state (the 8x0x1c prim array 0x800b10ec-11cc) — harmless on load, but worth noting if the port ever byte-maps the original save format.


---
## Report: stringsAssets

Task D (strings/assets/scripts hunt for a dormant RE1.5 item box) complete — all four probes negative, with method+coverage stated. (1) Glyph encoding (byte+0x24, 0x00=space) validated against the known preview message (exactly 16 RDT hits = the 8 safe-room pairs); with it, zero box vocabulary (Item Box/Box/Take/Put/Leave/Store/Deposit) in PSX.EXE, DEBUG.BIN, TITLE.BIN, STAGE1-6.BIN, DATA/*, or any of 240 RDTs (only flavor examine texts); the DEBUG.BIN sysmes bank @0x5f96 (34 entries fully decoded) is purely the dormant SAVE system's text incl. 8 save-room names — the save system left text traces, a box left none. (2) CRITICAL calibration: RE2-Leon decodes with the same encoding and its complete sysmes bank has NO box strings either — a working PSX-RE box screen is textless, so the string negative alone can't decide; decisive evidence is code-side. (3) Assets: DEBUG.BIN's full CD file table has no box-named file; the unmapped ST_00.TIM rows v120-149 are now identified as Leon's POLICE ID-card art + dev-menu label sprites (windows/tools/system/demo/fileing/command) — dev-menu art shipped, box-UI art did not. (4) Scripts: all 138 main/sub entries of the 16 safe-room RDTs walked opcode-exact with zero desync — no second inventory-touching sub (only normal floor-pickup Item_aot_set sce=9 in main init); game-wide sce-type histogram (1568 entries) shows no vestigial box/save AOT type. Remaining open lead is engine-only: the unidentified screen-2 dispatch {LAB_8004d7cc,...} and DAT_800b25c0's writers.

### Glyph encoding validated + safe-room census [re15_port/shared_assets/PSX/STAGE*/ROOM*.RDT]
RE1.5 message encoding confirmed as stored_byte = ascii_char - 0x24, 0x00 = space. The 'Itembox is not available in this preview' message exists in exactly 16 RDT files = the 8 safe-room pairs (ROOM1150/1151, 2010/2011, 30A0/30A1, 30B0/30B1, 4010/4011, 5010/5011, 6020/6021, 6030/6031) and nowhere else.

EVIDENCE: Encoded-pattern search: ROOM1150.RDT@0x1428, ROOM1151@0x1206, ROOM2010@0x949, ROOM2011@0x969, ROOM30A0/1@0x8a2, ROOM30B0/1@0x9a6, ROOM4010@0x1908, ROOM4011@0x7d9, ROOM5010@0x693, ROOM5011@0x174c, ROOM6020/1@0xbe0, ROOM6030@0x157c, ROOM6031@0x11d0. Search ran over all 240 STAGE*/​*.RDT files.

### NEGATIVE: no box UI strings anywhere in RE1.5 [info/Re1.5/PSX.EXE + BIN/*.BIN + DATA/* + all RDTs]
Zero glyph-encoded or ASCII occurrences of 'Item Box'/'ITEM BOX'/'Itembox'/'ItemBox'/'itembox'/'Box'/'BOX'/'Take'/'Put'/'Leave'/'Store'/'Deposit' in PSX.EXE, DEBUG.BIN, TITLE.BIN, STAGE1-6.BIN, or DATA/*. The RE1.5 EXE contains no English text runs at all (all text lives in DEBUG.BIN + RDTs). METHOD: (a) exact encoded-pattern search of each variant over the full byte range of every file; (b) exhaustive run-dump of ALL decodable glyph runs >=5 chars (EXE 1831 runs, DEBUG.BIN 707, TITLE.BIN 53) grepped case-insensitively for box/take/put/leave/store/deposit/item — only hits: pickup prompt 'Will you take the' @DEBUG.BIN:0x4fd5, ''t carry any more items' @0x4ffd, 'Joker item unused in this recreation' @0x5bff; (c) strings -a -n 3 | grep -i box on all 9 binaries = 0 hits.

EVIDENCE: DEBUG.BIN@0x4fd5/0x4ffd/0x5bff are the only item-adjacent strings; grep -c 'box' over decoded EXE runs = 0. All in-game RDT 'box' mentions are examine flavor text only: ROOM1070@0x1859 'These boxes contain nothing of use', ROOM1110@0xf20 'Ammunition boxes seized', ROOM11D0@0x1977 'Boxes and dog food', ROOM3040@0x1619 'The boxes are blocking the way', ROOM3070@0x3ad5 + ROOM4090@0xc71 'control box'.

### Sysmes bank @0x5f96 = dormant SAVE system text; box has no text counterpart [re15_port/shared_assets/PSX/BIN/DEBUG.BIN]
The DEBUG.BIN sysmes offset table @file 0x5f96 (= @0x800c5f96 in RAM) holds exactly 34 entries: all are memory-card FSM strings ('Do not save', 'Checking memory card...', 'Formatting is required', 'Fail in save/load', ...) plus Leon/Elza name tags [24]/[25] and 8 save-location names [26..33]: Irons' Office, Medical Room, West Staircase 1F, Sewer Maintainance, Sewer Control Room, Factory & Office, Security Room, Monitor Room — matching the 8 safe-room pairs 1:1. ZERO box-related entries. Asymmetry: RE1.5's dormant save system left a full text bank; an item box left none.

EVIDENCE: u16 offset table @DEBUG.BIN:0x5f96, 34 valid entries (0x44..0x27a rel), strings decoded @0x5fda..0x621d; entries [34+] are code bytes (garbage u16s 0x4b20...). Save-room names @0x6197..0x6210.

### RE2 calibration: a real box screen prints NO text [info/re2leon/PSX.EXE]
RE2-Leon retail decodes with the SAME +0x24 glyph encoding. Its complete system-message bank contains pickup/combine/mix/inventory-full messages plus literal ' UNUSED' filler slots — and NOT ONE box-screen string (no Take/Put/Leave/Item Box). RE2's shipped, functional item-box screen is textless graphics. CONSEQUENCE: string absence in RE1.5 is expected even IF a dormant box screen existed — the string-negative is necessary but NOT sufficient; decisive evidence must be code/asset-side (screen-2 handler identity, Task A/B).

EVIDENCE: ghidra-independent decode of info/re2leon/PSX.EXE: sysmes bank @file 0x090f5b ('Will you take the') through 0x09110b (' UNUSED'); box-adjacent hits are only item names ('Box Key' @0x08f014) and the room texture path 'd:/bio2/room/data/tetubox1.tim' (ASCII). No 'itembox'/'hako' dev-strings in the RE2 EXE.

### NEGATIVE: no box asset exists on the disc [DEBUG.BIN + DATA/ST_00.TIM + ITEM/]
No box-UI art or box-named file anywhere: (1) DEBUG.BIN's embedded CD file table (full ASCII dump: DUMMY, STAGE*.BIN, TIM/PIX/SCD/PLD/PLW/EDH/VB/BGM/BSS/RDT/DO2/EMS/ITP entries) contains no box-named file; (2) DATA/ (34 files) and ITEM/ (ITPS.ITP + STPIC_00-0F.TIM) contain none; (3) the previously unmapped ST_00.TIM atlas rows v120-149 are now IDENTIFIED: Leon's 'POLICE / Leon A. Kennedy' ID-card art (the wave-1 'crest/header art') + six dev-menu label sprites reading 'windows / tools / system / demo / fileing / com|mand'. Dev-menu art shipped in the retail atlas — box-UI art did not.

EVIDENCE: CD file table = ASCII strings in DEBUG.BIN @~0xbc3-0x1100+ (verified as raw ASCII, e.g. 'STAGE1.BIN', 'ITEMALL.PIX', 'MIXITEM.PIX'); ST_00 decode: scratchpad q3_st00_full.png (128x278), label sprites at atlas y~212-260, ID card at y~200-255.

### NEGATIVE: no unwired box sub in any safe-room SCD [16 safe-room RDTs]
All 16 safe-room RDTs scanned: every main+sub SCD entry (main=1 each; subs: 1150=9, 1151=8, 2010/2011=4, 30A0/1=3, 30B0/1=3, 4010=9, 4011=4, 5010=4, 5011=9, 6020/1=3, 6030=10, 6031=9) walked opcode-exact with ZERO desyncs (every walk ended flush at the next table offset or Evt_end). The ONLY item-manipulating opcodes (searched: 0x4E/0x50 Item_aot_set, 0x5E Keep_Item_ck, 0x62 Sce_item_lost, 0x69 Item_aot_set_4p, 0x76 Sce_item_get, 0x88 Sce_item_ck_lost) are ordinary main-init floor pickups: 2010/2011 main00+0x20/+0x36/+0x4c, 30A0/1 main00+0x20, 4010 main00+0xc0/+0xd6/+0xec, 4011 +0x98/+0xae, 5010 +0x84/+0x9a, 5011 +0x98/+0xae — all Item_aot_set(0x50) with sce=9. No sub touches inventory beyond the message; no second unwired box sub exists.

EVIDENCE: Opcode-size-table walk (RE1.5 sizes, e.g. 0x2C=20, 0x3B=32, 0x50=22) over sub tables at RDT header u32@0x40 (main) / u32@0x44 (sub); boundary-flush validation on all 138 entries of the 16 files.

### NEGATIVE: no vestigial box sce-type game-wide [all STAGE*/​*.RDT]
Game-wide AOT sce-type histogram over all 240 RDTs (1568 SCD entries walked in sync, 11 desynced = uncovered): Aot_set sce in {0:35, 1:399, 3:203, 4:18, 5:205, 8:32, 0xc:58, 0xd:54}, Door_aot_set {0:37, 2:593}, Item_aot_set {0:2, 9:148}. No room-singleton sce value that could be a dormant box/save trigger type (unlike RE2 where ITEMBOX/SAVE are dedicated sce types). Sole oddity: Item_aot_set with sce=0x00 in ROOM1190/1191 sub13 (dog kennel, both player variants — a disabled item, not box-related). The safe-room box objects use plain sce=3 event AOTs (e.g. ROOM1150 main00 'Aot_set 06 03 41' wiring the message sub).

EVIDENCE: sce byte = opcode+2 (validated: all doors=0x02 @0x3B+2, all live items=0x09 @0x50+2). Desynced/uncovered entries: ROOM1090 sub00, 4040/4041 main00, 40B0/40B1 main00, 5030/5031 main00+sub00, 5060/5061 main00 (9 non-safe rooms).

### Task-D verdict [all]
From the strings/assets/script side there is NO trace of a dormant item box: no UI strings (but RE2 proves a box screen needs none), no box asset or file-table entry, no unwired script sub, no vestigial AOT type. Contrast with the dormant save system, which left BOTH text (34-entry sysmes bank) and room-name data. If dormant box code exists it can only be pure engine code — the unidentified screen-2 dispatch entry {LAB_8004d7cc, 0x8004d904, 0x8004d96c} @tables 0x80074be8/f4/c00 remains the only open lead (engine-side task, not mine).

EVIDENCE: Aggregation of the six negatives above; each with method+coverage stated.

- OQ: Engine-side (Task A/B territory): identity of screen-2 handlers {LAB_8004d7cc, 0x8004d904, 0x8004d96c} in the 3-entry dispatch tables @0x80074be8/0x80074bf4/0x80074c00, and all writers of DAT_800b25c0 (can it ever become 2?) — the only remaining place dormant box code could hide, since a box screen provably needs no strings/assets of its own.
- OQ: The 11 SCD entries that desynced the offline walker (ROOM1090 sub00; 4040/4041, 40B0/40B1, 5030/5031(+sub00), 5060/5061 main00) are excluded from the game-wide sce-type histogram — all are non-safe rooms, but for a 100% negative they'd need a data-region-aware walk or live-VM trace.
- OQ: The ST_00 dev-menu label sprites ('windows/tools/system/demo/fileing/com|mand') prove a dev overlay menu with pre-rendered labels shipped in the retail atlas — which code draws them (DEBUG.BIN? the LAB_8004603c family?) and whether its 'fileing' or 'tools' pane had an item-box editor is undetermined (needs DEBUG.BIN disasm, not string search).
- OQ: RE1.5's sce-type numbering diverges from RE2 (door=2 not 1, item=9 not 2; sce=8 used 32x game-wide is unidentified) — the EXE's per-AOT sce dispatch (FUN_80042bac) should be checked for handlers of sce values that NO RDT uses (a compiled-in handler with zero data references would be exactly the dormant-box signature).
