# Wave-3 spec (RE workflow wf_cbdbc8a1) - USE classifier / anim step tables / MAP-FILE verdict

---
## TASK A complete — USE state 5 + classifier + all three weapon-anim FSM step bodies + heal handoff + cant-use message path fully decoded from raw MIPS (PSX.EXE via re15_disasm.py, DEBUG.BIN file bytes, mzd_inv_open.sav live values). Headline results: (1) the report-D/report-B contradiction on the classifier is settled — UNEQUIP triggers on inventory[equip].id == inventory[cursor].id (ID compare, no
(citations_complete=True)

### State-5 body + dispatcher FUN_8004aa24 [RE1.5-EXE (PSX.EXE)]
ITEM state 5 = per-frame jal FUN_8004aa24 (@0x8004a674, tail j 0x8004a700). FUN_8004aa24 @0x8004aa24 dispatches jalr PTR[0x80074c28 + DAT_800b25c3*4]. Table @0x80074c28 (8 entries): [0]=0x8004aa64 classifier, [1]=0x8004ab88 equip anim, [2]=0x8004ad10 unequip anim, [3]=0x8004adcc heal, [4]=0x8004b074 nop (jr ra only), [5]=0x8004b07c swap anim, [6]=0x8004b250 cant-use message, [7]=0x8004b37c (exchange sub0; no c3:=7 writer found — vestigial in this table; state 7 reaches it via its own table @0x80074c44). No entry resets c3 on dispatch: the invariant is that every flow's terminal resets c3=0 and c4=0 before returning to a state that can re-enter state 5 (verified in every terminal below), so the classifier (c3==0 handler) always runs first.

EVIDENCE: dispatcher @0x8004aa24-aa60 (lbu 0x800b25c3; sll 2; lw @0x80074c28+; jalr); table dump @0x80074c28-0x80074c44; state-5 call @0x8004a674 (jal 0x8004aa24); USE dispatch entry [0] @0x8004a550-554 (j 0x8004a6f8 with v0=5 → sb 25c2) writes ONLY 25c2

### Classifier @0x8004aa64 — complete branch decode (settles report contradiction) [RE1.5-EXE (PSX.EXE)]
id = inventory[DAT_800b25bd*4].id @0x800b10ac. WEAPON gate sltiu id,0x15 @0x8004aa90 (ids 1..0x14). Weapon path: DAT_800b25c4:=0 @0x8004aaa8 (c4 reset for ALL weapon outcomes), then: (a) if inventory[DAT_800b25c8*4].id == inventory[cursor*4].id (ID equality @0x8004aadc, NOT slot equality) → c3:=2 UNEQUIP + 25cd:=0x3e (@0x8004aae4-aaf8); (b) else if 25c8==0x80 (nothing equipped; the compare operand 0x80 comes from the branch delay slot `ori v0,zero,0x80` @0x8004aae0 — this is what the two prior reports misread) → 25cd:=0x80 + c3:=1 EQUIP (@0x8004ab04-ab20); (c) else → 25cd:=0x3e + c3:=5 SWAP (@0x8004ab2c-ab3c). NON-weapon: (id-0x22)<0xe @0x8004ab48 && id!=0x25 @0x8004ab54 → c3:=3 HEAL (ids 0x22..0x2f incl. 0x2f NUT, excl. Red 0x25; c4 NOT reset here — relies on the terminal invariant); ALL else (ammo 0x15-0x21, Red 0x25, keys/docs ≥0x30) → c3:=6 + c4:=0 (@0x8004ab6c-ab7c). Note the 25c8==0x80 case (a)-compare reads inventory[0x80*4]=0x800b12ac (out-of-inventory byte) before falling to (b) — byte-true as coded.

EVIDENCE: full disasm @0x8004aa64-0x8004ab84 quoted in-session: sltiu 0x15 @0x8004aa90; sb zero 25c4 @0x8004aaa8; equipped-id lbu @0x8004aac4 vs cursor-id lbu @0x8004aad4; bne @0x8004aadc + delay ori 0x80 @0x8004aae0; bne a0,v0 @0x8004ab04 (a0=25c8, v0=0x80); sltiu 0xe @0x8004ab48; beq 0x25 @0x8004ab54

### EQUIP anim FSM (c3=1) — all 12 steps [RE1.5-EXE (PSX.EXE) + stage_saves/mzd_inv_open.sav]
Guard c4<0xc else return. Jump table @0x80010f24: steps 0-4 and 6-10 → common tick 0x8004ac7c; step 5 → commit 0x8004abbc (falls into tick); step 11 → terminal 0x8004acec. COMMIT (step 5): 25c8:=cursor, 25c9:=old 25c8 (=0x80 on this path); then ammo-counter regs by kind byte 0x800b10ae[new slot*4]: kind0→(25dc,25de)=(0x96,0x35), kind1→(0x82,0x2f), kind2→(0xaa,0x2f). COMMON TICK (runs steps 0..10 = 11 ticks): IF 25c9==0x80: 25cd += 250 (= −6 mod 256); ALWAYS: 2608 += 5, 25de += 1, c4++. TERMINAL (step 11): 25c2:=6 (slide back to grid), c3:=0, c4:=0. Net over 11 ticks: cd 0x80→0x3e, 2608 −55→0, i.e. the anim lands exactly on the weapon-equipped resting values. Byte-true quirks: (1) terminal 25de = 0x35+6 = 0x3b (kind0), one more than the screen-init resting 0x3a — persists until next screen open; (2) the cd decrement in steps 0-4 is gated on the STALE 25c9 (written only by game-start init=0x80 @0x80045fe0, equip step5, swap step0): after a swap→unequip→equip sequence within one game session, steps 0-4 skip the decrement and cd terminals at 0x5c instead of 0x3e — port must model 25c9 to reproduce this.

EVIDENCE: table dump @0x80010f24 ([0..4,6..10]=0x8004ac7c, [5]=0x8004abbc, [11]=0x8004acec); commit @0x8004abbc-ac78 (sb cursor→25c8 @0x8004abd0, sb old→25c9 @0x8004abe0, kind branches @0x8004abfc/ac2c/ac60 with pairs 0x96/0x35, 0x82/0x2f, 0xaa/0x2f); tick @0x8004ac7c-ace8 (lbu 25c9 bne 0x80 @0x8004ac88; addiu 250 → 25cd @0x8004ac9c-aca4; 2608+5 @0x8004acc4/acd0; 25de+1 @0x8004acc8/acd8; c4++ @0x8004acc0/ace0); terminal @0x8004acec-ad04; 25c9 writers scan: 0x80045fe0 (init 0x80, with 25c8:=0 @0x80045fec), 0x8004abe0, 0x8004b0e8; savestate 25c9=0x80

### UNEQUIP anim FSM (c3=2) — all 12 steps [RE1.5-EXE (PSX.EXE)]
Guard c4<0xc. Table @0x80010f54: steps 0-4,6-10 → tick 0x8004ad50; step 5 → 0x8004ad44 (25c8:=0x80, falls into tick); step 11 → terminal 0x8004ada8 (25c2:=6, c3:=0, c4:=0). TICK (11×, unconditional, no 25c9 gate): 25cd += 6, c4++, 2608 −= 5, 25de −= 1. Net: cd 0x3e→0x80, 2608 0→−55 = exactly the nothing-equipped resting state. No 25dc/25de kind reset (unlike equip). Entered two ways, both with cd already 0x3e: (a) classifier c3=2 (sets cd=0x3e, c4=0 via weapon-path reset); (b) the vestigial grid equip-cell 0xA confirm (DEBUG.BIN @0x800c64d4-6534: 25c2=5, c3=2, c4=0, cd=0x3e — unreachable in shipped MZD, nav patched dead @0x800c637c).

EVIDENCE: table dump @0x80010f54; step5 @0x8004ad44-ad4c (ori 0x80 → sb 25c8); tick @0x8004ad50-ada4 (25cd+6 @0x8004ad64-ad68 unconditional; c4++ @0x8004ad7c-ad84; 2608−5 @0x8004ad88/ad94; 25de−1 @0x8004ad8c/ad9c); terminal @0x8004ada8-adc0

### SWAP anim FSM (c3=5) — 11-step inline [RE1.5-EXE (PSX.EXE)]
Inline FSM @0x8004b07c: slti c4,11 → c4==11 is terminal @0x8004b234 (25c2:=6, c3:=0, c4:=0 — sb zero,0(a0) with a0=&25c4); c4==0 → COMMIT @0x8004b0c4: 25c8:=cursor, 25c9:=old slot (≠0x80, swap requires equipped), then ammo regs by NEW slot kind: kind0→(25dc,25de)=(0xac,0x3a) — NOTE 0xac/0x3a here vs equip's 0x96/0x35 — kind1→(0x82,0x2f), kind2→(0xaa,0x2f); falls into tick. TICK @0x8004b184 (c4 0..10 = 11 ticks): reads kind of CURRENT 25c8: kind0 → 25dc −= 2 (0xac→0x96 after 11 ticks, landing exactly on resting); kind≠0 → 25de += 1 (0x2f→0x3a, resting); then IF 25c9==0x80: 25cd += 250 (dead in this flow — step 0 always writes 25c9≠0x80 first, so cd stays at the classifier's 0x3e); c4++. 2608 is NEVER touched by swap (no gun-slide; only the ammo-counter animates). Terminal registers land on the weapon-equipped resting state exactly (kind0 x=0x96/y=0x3a).

EVIDENCE: disasm @0x8004b07c-0x8004b24c quoted in-session: slti 11 @0x8004b08c; c4==0 branch @0x8004b0a0→0x8004b0c4; commit sb cursor→25c8 @0x8004b0d8, sb old→25c9 @0x8004b0e8, kind0 pair 0xac/0x3a @0x8004b108-b11c, kind1 @0x8004b13c-b150, kind2 @0x8004b16c-b180; tick kind read of 25c8 @0x8004b188-b1a0, kind0 25dc−2 @0x8004b1b4-b1c4, else 25de+1 @0x8004b1d4-b1e4, c9 gate @0x8004b1ec-b210, c4++ @0x8004b218-b228; terminal @0x8004b234-b244

### Anim visual registers — resting model + draw formula (closes the equip/unequip visual direction) [RE1.5-EXE (PSX.EXE) + stage_saves/mzd_inv_open.sav]
Screen init LAB_80049524 writes the resting pair from the equip state @0x800495e8: if 25c8==0x80 (nothing equipped) → 25cd:=0x80, 2608:=−55; else → 25cd:=0x3e, 2608:=0. The animated ARMS sprite is drawn in FUN_80049a5c @0x80049c70-9ca0: x = 25d8+0x18 (=150), y = 25da + (s16)2608 + 0x58; so 2608=−55 → y=59 = inside the equip box (58-87), 2608=0 → y=114 = the Standard-Arms box row. Therefore: EQUIP anim (2608 −55→0, cd 0x80→0x3e) slides the sprite DOWN from the equip box to the Standard-Arms row while dimming to 0x3e; UNEQUIP reverses (slides UP into the equip box, brightens to 0x80). Savestate (knife equipped, slot 0) confirms resting: 25cd=0x3e, 2608=0, 25dc=0x96, 25de=0x3a, 25c2/c3/c4=0.

EVIDENCE: init branch @0x800495e8 (bne v1,0x80) + writers @0x800495f0-9604 (cd=0x80; sh −55→2608) / @0x800495ec+0x80049610-961c (cd=0x3e via delay-slot ori 0x3e; sh zero→2608); draw @0x80049c70-9ca0 (lhu 25d8 +24 → sh 8(s1); lhu 25da + lhu 2608 + 88 → sh 10(s1); AddPrim jal 0x8006b538 @0x80049cb0); savestate reads via re15_ss.Ram listed in-session

### HEAL flow (c3=3) @0x8004adcc — NO prompt, direct wipe→consume; full sub-FSM [RE1.5-EXE (PSX.EXE)]
s0:=100 (@0x8004ae14), s1:=id−0x22. Sub-FSM on c4: c4==0: if (id−0x22)<0xd → wipe-select jump table @0x80010f84[(id−0x22)]: [1]=0x23,[4]=0x26 → DAT_800b25d4:=2 (blue vertical wipe); [12]=0x2e → no wipe; all others → 25d4:=1 (green sweep); if (id−0x22)≥0xd (only 0x2f NUT reachable) → player.hp(0x800acaee):=0x4d=77 immediately (@0x8004ae98-aea0), no wipe; then c4++ (@0x8004aea4-aebc). c4==1: wait until 25d4==0 (renderer FUN_80048a44 self-clears after the 32/36-frame wipe) → c4:=2. c4==2 (@0x8004aee4): consume slot (id/qty/kind bytes :=0 @0x800b10ac/ad/ae + cursor*4), jal FUN_8004dadc (compaction), then apply table @0x80010fbc[(id−0x22)] with s0=100: [0]0x22→hp:=100; [1]0x23→hp+=25 AND cure poison(0x800acaec&=~2); [2]0x24 GREEN→hp+=25 (srl s0,2 — NOT +50); [3]0x25→nothing (dead, classifier blocks Red); [4]0x26→cure only; [5]0x27,[6]0x28→hp+=50; [7]0x29→hp+=25+cure; [8]0x2a→hp:=100; [9]0x2b→hp+=75 (50+25); [10]0x2c→hp+=50+cure; [11]0x2d→hp:=100; [12]0x2e→hp+=50+cure; (id−0x22)≥0xd (0x2f)→hp:=77 AGAIN (@0x8004b028 — double write, harmless). No clamp anywhere; consume happens even at full HP. Tail: ECG sweep reset 0x800b2600:=0x20, c3:=0, c4:=0, 25c2:=6 (slide back to grid). THERE IS NO 'Will you use it?' PROMPT AND NO 'You have used it' MESSAGE anywhere in this flow — the ECG wipe is the only feedback.

EVIDENCE: disasm @0x8004adcc-0x8004b070 quoted in-session; wipe table dump @0x80010f84 (13 entries → 0x8004ae70/0x8004ae84/0x8004aea4); apply table dump @0x80010fbc (13 entries → 0x8004afe4/af60/af68/b034/af7c/af98/af98/afb4/afe4/afbc/aff4/afe4/aff4); Green +25 re-verified: entry[2]→0x8004af68 with delay `srl v0,s0,2` @0x8004af78 → addu+sh @0x8004afa8-afb0; s0=0x64 @0x8004ae14; consume @0x8004aee4-af2c; sweep+exit @0x8004b038-b058

### PORT DIVERGENCE 1 — heal value: Green Medicine [port (re15_port/engine/src/item_use_common.c) vs RE1.5-EXE]
re15_port/engine/src/item_use_common.c:20 ships {0x24 Green Medicine: add=+50}; raw MIPS is +25 (100>>2). The memory note reai-v2-item-use-heal ('Green=+50') is likewise wrong against the raw bytes. The rest of the port's 13-entry table matches the raw decode exactly (0x23=+25+cure, 0x27/0x28=+50, 0x29=+25+cure, 0x2b=+75, 0x2c/0x2e=+50+cure, 0x22/0x2a/0x2d=set 100, 0x26 cure-only, 0x25 nothing, 0x2f=77 special-cased). Wave-3 fix: one constant, citation @0x8004af78 (srl v0,s0,2).

EVIDENCE: port file item_use_common.c:20 vs disasm @0x8004af68-af78 + @0x8004afa8-afb0 (delay-slot srl s0,2 = 25); contrast 0x27/0x28 handler @0x8004af98-afa4 (srl s0,1 = 50)

### PORT DIVERGENCE 2 — heal handoff model (prompt vs direct) [port vs RE1.5-EXE]
The port's item_use_common.c runs a 5-state Yes/No 'Will you use the X?' prompt + 'You have used the X' message FSM inside menu state 5 (use_bridge menu_common.c:317-325). The original status-screen heal flow has NO prompt and NO text: classifier c3:=3 → wipe(c4=0) → wait(c4=1) → consume+apply(c4=2) → 25c2:=6. Timing: original = 1 classify frame + 1 wipe-arm frame + 32 (green) / 36 (blue) / 0 (0x2e,0x2f) wipe frames + 1 consume frame → state 6. Also the original wipes BEFORE consuming (wipe c4=0/1, consume c4=2); the port arms the wipe at consume. Wave-3 rewrite: the status-screen path must drop the prompt/typewriter entirely and implement the c4 sub-FSM (item_use_common.c's prompt machinery, if kept, belongs only to a non-menu context — no RE1.5 status-screen citation exists for it). Comment mislabel confirmed: item_use_common.c:4 calls @0x8004b250 a Yes/No prompt — @0x8004b250 is the cant-use plain message with DAT_800b8522=0 (no Yes/No), and it is not part of the heal flow at all.

EVIDENCE: original: @0x8004adcc-0x8004b070 (no jal 0x80027e68, no pad reads anywhere in the heal flow — only 25d4 wait); wipe durations per FUN_80048a44 (+3/frame 0x20→0x80 = 32f; −2/frame 0x6a→0x22 = 36f, prior report wf_e8e48d36); port: item_use_common.c:27-126, menu_common.c:312-342

### PORT DIVERGENCE 3 — weapon USE semantics in use_bridge [port vs RE1.5-EXE]
menu_common.c:328-335 (a) unequips on eq==cur (SLOT compare) — original unequips on ID compare @0x8004aadc (duplicate-weapon corner case diverges: USE on a second copy of the equipped weapon id unequips in the original, equips/swaps in the port); (b) merges equip and swap into an instant re15_inv_set_equipped_slot with no anim — original runs the 13-frame (1 classify + 11 ticks + 1 terminal) equip/unequip/swap FSMs decoded above, and the visible commit (25c8 write) happens at anim step 5 (equip/unequip) or step 0 (swap), not at confirm; (c) port lacks 25c9, 25cd, 2608, 25dc/25de modeling (needed for the g5/ARMS render and the cd-history quirk). Wave-3: replace the whole weapon branch of use_bridge with the three FSMs.

EVIDENCE: port menu_common.c:312-342 read in-session; original citations as in the classifier/equip/unequip/swap facts

### Cant-use message path (c3=6) @0x8004b250 — full mechanics [RE1.5-EXE (PSX.EXE) + DEBUG.BIN (shared_assets/PSX/BIN/DEBUG.BIN, maps 1:1 @0x800c0000)]
Sub-FSM on c4: c4==0 @0x8004b2a0: 25ee += 14 while <251 (command cluster slides OUT bottom, 166→264, 7 add-frames + 1 transition frame) → c4:=1. c4==1 @0x8004b2d8: jal FUN_80027e68(a0=0x00a80018 [y=0xa8<<16 | x=0x18], a1=0x8400, a2=0, a3=0) → c4:=2. c4==2 @0x8004b2fc: wait (DAT_800b8520 & 0x80)==0 → 25c2:=1 (GRID — the command stage does NOT return), c3:=0, c4:=0. FUN_80027e68 arg decode (a1&0x300==0 generic path): DAT_800b8534:=x=0x18, 8536:=y=0xa8, 8522:=a1&0x80=0 (NO Yes/No), 8538:=a1&7=0 (text depth class 0), a1&0xc00==0x400 → string ptr 8528 := 0x800c50de + u16[0x800c50de + 0*2] = description-bank entry 0; 8520:=0x80 (active), 8540:=a3=0, pauseflags |= a3 (no-op); a1 bit 0x8000 is NOT consumed by the opener (dead bit, confirmed). Entry-0 bytes (DEBUG.BIN file offset 0x50de+0x90, file==RAM): '35 4b 51 00 3f 3d 4a 3a 50 00 51 4f 41 00 45 50 00 44 41 4e 41 57 01 00' = "You can’t use it here." (chr=code+0x24; 0x3a=apostrophe glyph, 0x57=period glyph) terminated by control 0x01 0x00 → msg-VM end-mode: code 1 with operand 0 → VM state DAT_800b8521:=5 = PRESS-WAIT. DISMISS: virtual edge word DAT_800ac76c & 0xc000 (CROSS confirm OR SQUARE cancel) → DAT_800ac768:=0xffff (input eat), 8520&=0x7f, pauseflags restored from 853c; NO auto-dismiss, NO SE on dismiss (no jal 0x80045024 in the state-5 block). Typewriter cadence on the status screen: DAT_800b5456==0 → first glyph after 2 frames, then 4 frames/glyph (8525=1<<1, 8524=2<<1); fast-forward is structurally impossible for this message (the held-CROSS FF path @0x8002820c-28214 is gated on 8522!=0). DISPLAY: plain glyph text only (no window box — VM state 5 draws nothing; boxes/arrows only in states 2/4 via FUN_800279c8), emitted by FUN_80028868 into the shared text ring, OT depth class 0 chain @0x800b829c; at (24,168) the text sits in the bottom strip left empty by the slid-out tab (25ea=264) and command (25ee=264) clusters. TICKING while task 0 is suspended: the msg VM FUN_80028134 has exactly ONE caller — the present-chain fn FUN_80010000 (@0x80010044, gated on 8520&0x80 @0x800280c8-d4 inside helper FUN_800280b4; second site 0x80053150) which runs from the frame flush (caller chain 0x80020f3c ← FUN_80020bb0 ← 0x800544e8), i.e. per rendered frame independent of the suspended gameplay task — so the message types out and accepts dismissal while only the menu task runs.

EVIDENCE: disasm @0x8004b250-0x8004b338 quoted in-session (slide @0x8004b2a0-b2d4; call args @0x8004b2d8-b2ec; wait+exit @0x8004b2fc-b324); FUN_80027e68.c generic path lines 49-56 + raw cross-checks; DEBUG.BIN bytes read from re15_port/shared_assets/PSX/BIN/DEBUG.BIN @0x50de (entry0 off 0x90, entry1 0xa8='A combat knife…'); msg-VM raw: init cadence @0x80028194-281c4 (8525=1<<s1, 8524=2<<s1; s1=(5456<1) @0x80028148/0x80028168), FF gate @0x800281ec-f4 (beq 8522,zero,0x8002823c skips FF), state-5 dismiss @0x8002868c-86d0 (lw 0x800ac76c; andi 0xc000; sw 0xffff→ac768; 8520&=0x7f; pauseflags:=853c); caller scan jal-word 0x80028134 → single hit @0x800280dc; FUN_800280b4 callers 0x80010044/0x80053150/0x8008ffd0; FUN_80010000 caller @0x80020f3c

### All 7 SE call sites in FUN_8004a0cc — bank/id + trigger [RE1.5-EXE (PSX.EXE)]
SE fn FUN_80045024(a0=bank<<24|id<<16, a1=0). (1) @0x8004a158: a0=0x04090000 = SE(4,9) — debug item-spawner OPEN (SELECT edge 0x100, after sh 1→DAT_800b2668 @0x8004a150). (2) @0x8004a47c: SE(4,4) — state-4 D-pad RIGHT edge 0x2000 (@0x8004a464) → 25d6:=3. (3) @0x8004a4a4: SE(4,4) — LEFT edge 0x8000 (@0x8004a48c) → 25d6:=1. (4) @0x8004a4cc: SE(4,4) — DOWN edge 0x4000 (@0x8004a4b4) → 25d6:=2. (5) @0x8004a4f4: SE(4,4) — UP edge 0x1000 (@0x8004a4dc) → 25d6:=0. (6) @0x8004a520: SE(4,6) — state-4 virtual confirm edge DAT_800ac76c&0x4000 (@0x8004a510), before the jr through @0x8004a73c. (7) @0x8004a664: SE(4,5) — cancel: virtual SQUARE edge 0x8000 (@0x8004a654) at the cancel-wait 0x8004a648 (reached when confirm bit clear @0x8004a514 beq→0x8004a648, AND as dispatch entry [4] for the d6==4 CHECK-reuse guard) → 25c2:=6 (v0=6 @0x8004a670 → sb @0x8004a6f8). Each D-pad hit consumes the frame (j 0x8004a700 skips confirm/cancel). Bank 4 = system-SE table @0x801fbd00 (FUN_80045024 case 4, prior report).

EVIDENCE: disasm of all 7 sites with lui a0 constants quoted in-session: 0x8004a154 (lui 0x409), 0x8004a478/0x8004a4a0/0x8004a4c8/0x8004a4f0 (lui 0x404), 0x8004a51c (lui 0x406), 0x8004a660 (lui 0x405); bit tests @0x8004a464/a48c/a4b4/a4dc/a510/a654; d6 stores @0x8004a474/a49c/a4c4/a4ec

### USE on the EQUIPPED weapon + g7 command-label swap rule [RE1.5-EXE (PSX.EXE)]
USE while the cursor item's ID equals the equipped item's ID → the classifier's branch (a): c3:=2 = the 12-step UNEQUIP anim (25c8:=0x80 at step 5) — so USE toggles the equipped weapon off; the weapon-model commit to the player still only happens at screen close (@0x80046688, already ported). The g7 command-cluster label (element 0, s4==0) in FUN_80047648 @0x80047d08-dcc: skipped (uv frozen) while 25c2 ∈ {5,6} (addiu −5; sltiu 2 @0x80047d20-28); otherwise: id=inventory[25bd].id, id<0x15 → uv:=(0x10,0x90) 'Equip' face; else uv:=(0x68,0xc0) 'Use' face; THEN overridden if 25bd == 25c8 (SLOT equality @0x80047d90) → uv:=(0x10,0xa0) (the unequip-label face). MISMATCH corner case (byte-true): with two copies of the equipped weapon id in inventory, the copy in the non-equipped slot shows the plain EQUIP label (slot compare) but USE on it triggers UNEQUIP (id compare) — do not 'fix' this in the port. Position: x=tmpl+25ec, y=tmpl+25ee @0x80047da8-dcc.

EVIDENCE: disasm @0x80047d08-0x80047dcc quoted in-session (gate @0x80047d14-28; sltiu 0x15 @0x80047d54; uv writes sb 2(s0)/3(s0) @0x80047d5c-d78; equip-slot compare @0x80047d7c-d90; override @0x80047d98-da4); classifier branch (a) citations above

### State-5 flow cadence (port build sheet) [RE1.5-EXE (PSX.EXE) + DEBUG.BIN]
Frame-exact sequence for a weapon USE: frame 0 = state-4 confirm (SE(4,6), 25c2:=5); frame 1 = classifier frame (c3 set, registers cd/c4 primed); frames 2-12 = 11 anim ticks (commit inside tick-frame 5 for equip/unequip, tick-frame 0 for swap); frame 13 = terminal (25c2:=6, c3=c4=0); then 7 slide frames (25ee 166→264 +14/f) + 1 transition frame → state 1 grid. For a heal: frame 1 classify, frame 2 wipe-arm (c4 0→1), 32 (green) / 36 (blue) wipe frames polled at c4==1, +1 consume frame (c4==2 → state 6). For cant-use: frame 1 classify (c3=6,c4=0), 7+1 slide-out frames, 1 message-open frame, then typewriter (2f + 4f/glyph × 21 glyphs of entry 0) + press-wait; dismissal frame clears 8520, next state-5 frame returns 25c2:=1 (grid directly — no slide-in, the cluster is already out).

EVIDENCE: synthesis of the per-state citations above; slide arithmetic @0x8004b2a0-b2c8 (slti 251/addiu 14) and state-6 @0x8004a684-6a8 (prior report); glyph count of entry 0 = 21 printable codes from the DEBUG.BIN byte dump

**Open questions:**
- Visual identity of the animated ARMS sprite's uv (which icon it shows — the equipped weapon's 40x30 cache cell vs the Standard-Arms knife face): the position/rgb registers and draw site @0x80049c70-9ca0 are decoded, but the uv-fixup block that follows (@0x80049cb8+ reads 25c8 kind) was not fully walked — needed for pixel-true wave-3 rendering, not for the FSM.
- The 25de terminal quirk (equip anim rests ammo-y at 0x3b vs screen-init 0x3a; equip kind1 rests 0x35 vs swap kind1 0x3a) is proven from the step arithmetic but not yet live-verified with a DuckStation capture of an in-menu equip — recommend one re15-room-capture run before declaring wave-3 pixel parity.
- The 25c9-history cd quirk (equip after swap→unequip in the same session terminals cd=0x5c instead of 0x3e) is byte-derived; a live capture would confirm the visible brightness difference.
- g7 elements 1..n (the CHECK/EXCHANGE/EXIT label faces and their uv rows) were not decoded in this pass — only element 0's swap rule; wave-3 drawing of the command cluster needs the remaining template rows (they are plain template+base positions per the wave-1 build sheet, but the uv constants should be dumped from @0x80075390-family templates).
- Reachability of the duplicated d-pad handler block @0x8004a75c-0x8004a7f4 after the tables (carried over from the wave-2 spec) — still unestablished; assume dead until a pointer/branch scan says otherwise.
- Whether the world-context (non-menu) item use shares the heal-apply table — the port's item_use prompt machinery may have a legitimate home elsewhere (item-get modal precedent), but no RE1.5 citation for a status-screen prompt exists; if the prompt FSM is retained for another flow it needs its own @0x-evidence.

---
## TASK B COMPLETE. The three "anim step tables" are per-step FUNCTION-POINTER tables indexed by the frame counter DAT_800b25c4 (c4), one call = one frame. Consumers: USE state 5 = FUN_8004aa24 -> jr PTR_80074c28[c3] where c3=1 EQUIP walker @0x8004ab88 (table @0x80010f24), c3=2 UNEQUIP walker @0x8004ad10 (table @0x80010f54), c3=5 SWAP inline walker @0x8004b07c (no table, c4 compare chain); EXCHANGE s
(citations_complete=True)

### Step-table FORMAT (all four tables) [RE1.5-EXE (PSX.EXE)]
Each 'step table' is an array of 32-bit function pointers indexed by step counter DAT_800b25c4 (c4); the walker runs once per frame (FUN_8004a0cc state 5/7 -> per-frame call) and does `jr table[c4]`; each step function ends by incrementing c4 itself, so 1 step = 1 frame. There are no duration/field records in the tables — durations come from repeating the same fn pointer N times. Equip walker @0x8004ab88: `lbu c4; sltiu v0,c4,0xc; beq->ret; jr [0x80010f24+c4*4]` (@0x8004ab94-abb4). Unequip walker @0x8004ad10 same shape with 0x80010f54 (@0x8004ad1c-ad3c). Combine-result walker @0x8004b408: `sltiu 0x11; jr [0x80010ff4+c4*4]` (@0x8004b414-b434). Swap @0x8004b07c is inline (no table): `slti v0,c4,11`, c4==0 -> 0x8004b0c4, c4 1-10 -> 0x8004b184, c4==11 -> 0x8004b234 (@0x8004b08c-b0b8).

EVIDENCE: RE1.5 PSX.EXE (info/Re1.5/PSX.EXE, file_off=0x800+addr-0x80010000). Table bytes: @0x80010f24 = '7c ac 04 80'x5, 'bc ab 04 80', '7c ac 04 80'x5, 'ec ac 04 80'; @0x80010f54 = '50 ad 04 80'x5, '44 ad 04 80', '50 ad 04 80'x5, 'a8 ad 04 80'; @0x80010ff4 = '3c b4 04 80'x8, 'a8 b4 04 80'x8, '24 b5 04 80'. Dispatch table @0x80074c28 (9 words '64 aa..08 b4'): [0]=0x8004aa64 classifier,[1]=0x8004ab88 equip,[2]=0x8004ad10 unequip,[3]=0x8004adcc heal,[4]=0x8004b074 (pure jr ra),[5]=0x8004b07c swap,[6]=0x8004b250 message,[7]=0x8004b37c,[8]=0x8004b408 (entries 7/8 = the 2-entry state-7 table @0x80074c44 overlapping its tail). FUN_8004aa24 @0x8004aa24 does jalr PTR_80074c28[25c3] (RE_15_Quellcode_V2/FUN_8004aa24.c + table dump).

### EQUIP per-step table (c3=1, table @0x80010f24, 12 entries) [RE1.5-EXE (PSX.EXE)]
Steps 0-4 and 6-10 -> 0x8004ac7c 'generic' (1 frame each): IF DAT_800b25c9==0x80 THEN 25cd(gun rgb) -= 6 (lbu; addiu +250; sb, u8 wrap); THEN unconditional tail: c4++; DAT_800b2608(y-slide) += 5; DAT_800b25de(digit y) += 1. Step 5 -> 0x8004abbc 'commit': 25c8 := 25bd (equip slot := grid cursor), 25c9 := old 25c8; kind := inv[25c8*4+2]@0x800b10ae: kind0 -> 25dc=0x96,25de=0x35; kind1 -> 25dc=0x82,25de=0x2f; kind2 -> 25dc=0xaa,25de=0x2f; then FALLS THROUGH into 0x8004ac7c (fade check + tail). Step 11 -> 0x8004acec TERMINAL: 25c2=6 (slide-out state), 25c3=0, 25c4=0. Totals: 12 frames; 2608 -55 -> 0 (+5 x11); 25cd 0x80 -> 0x3e (-6 x11) iff 25c9==0x80 throughout; 25dc jumps to final at step 5; 25de animates +1/frame. No calls of any kind in the walker (no jal instructions).

EVIDENCE: @0x8004ac7c-aca4 (lbu 25c9; ori v0,0x80; bne skip; lbu 25cd; addiu v0,250; sb 25cd); tail @0x8004aca8-ace0 (lbu 25c4; lhu 2608; lhu 25de; addiu +1/+5/+1; sh 2608 @0x8004acd0; sh 25de @0x8004acd8; sb 25c4 @0x8004acdc). Step5 @0x8004abbc-ac78 (25c8 swap @0x8004abcc-abe0 incl. sb v1->25c9 @0x8004abe0; kind reads @0x8004abe4-ac60 on 0x800b10ae; 0x96->25dc @0x8004ac04-08, 0x35->25de @0x8004ac0c-14; 0x82/0x2f @0x8004ac34-48; 0xaa/0x2f @0x8004ac64-78). Terminal @0x8004acec-ad04 (ori 6; sb->25c2; sb zero->25c3; sb zero->25c4).

### UNEQUIP per-step table (c3=2, table @0x80010f54, 12 entries) [RE1.5-EXE (PSX.EXE) + DEBUG.BIN]
Steps 0-4 and 6-10 -> 0x8004ad50 'generic' (1 frame each): 25cd += 6; c4++; 2608 -= 5; 25de -= 1 (exact mirror of equip, no conditional — the fade always runs). Step 5 -> 0x8004ad44: 25c8 := 0x80 (equipped slot cleared mid-anim), falls through into 0x8004ad50. Step 11 -> 0x8004ada8 TERMINAL: 25c2=6, 25c3=0, 25c4=0. Totals: 12 frames; 25cd 0x3e -> 0x80; 2608 0 -> -55; 25de -= 11 (0x3a -> 0x2f, matching equip step-5's kind1/2 start value); 25dc untouched. Entry sites (all set 25cd=0x3e, c4=0, c3=2): (a) classifier same-item branch @0x8004aae4-aaf8; (b) DEBUG.BIN grid equip-cell(0xA) confirm @0x800c64d4-6534: if 25c8!=0x80 -> SE(4,0x0a) (lui a0,0x40a @0x800c64e8, jal 0x80045024), 25c2=5, 25c3=2 @0x800c650c, 25c4=0 @0x800c6518, 25cd=0x3e @0x800c6520, and cursor restore 25bd := DAT_800b25d7 (saved cursor) + 25be := 25d7 @0x800c64f8/6524/652c — confirms saved-cursor global = 25d7 (plan contradiction #3 resolved).

EVIDENCE: Generic @0x8004ad50-ad9c (lbu 25cd; addiu +6; sb; lbu 25c4; lhu 2608; lhu 25de; addiu +1/-5/-1; sb 25c4 @0x8004ad84; sh 2608 @0x8004ad94; sh 25de @0x8004ad9c). Step5 @0x8004ad44-ad4c (ori v0,0x80; sb->25c8). Terminal @0x8004ada8-adc0. DEBUG.BIN (shared_assets/PSX/BIN/DEBUG.BIN, 1:1 @0x800c0000): manual word-decode via re15_disasm.dis_one of file bytes @0x800c64d4-6534 (lbu 25c8; beq 0x80 skip; lui a0,0x40a; jal 0x80045024; lbu v1,9687=25d7; sb 5->25c2 @0x800c6500; sb 2->25c3 @0x800c650c; sb zero->25c4 @0x800c6518; sb 0x3e->25cd @0x800c6520; sb v1->25bd/25be @0x800c6524-652c).

### SWAP inline walker (c3=5 @0x8004b07c, 11 movement steps + terminal) [RE1.5-EXE (PSX.EXE)]
Step 0 -> 0x8004b0c4: 25c8 := 25bd, 25c9 := old 25c8 (@0x8004b0e8); kind := inv[25c8*4+2]: kind0 -> 25dc=0xac, 25de=0x3a; kind1 -> 25dc=0x82, 25de=0x2f; kind2 -> 25dc=0xaa, 25de=0x2f; falls into the tail. Steps 0-10 tail @0x8004b184 (1 frame each): kind(new 25c8)==0 -> 25dc -= 2, ELSE -> 25de += 1; then IF 25c9==0x80 -> 25cd -= 6 (shared guard; effectively never fires in a legal swap since 25c9 = the old occupied slot); c4++. Step 11 (c4==0xb) -> 0x8004b234 TERMINAL: 25c2=6, 25c3=0, 25c4=0. NO 2608 write anywhere in swap (the gun does not slide). Totals converge exactly on the rest state: kind0 25dc 0xac-2x11=0x96, 25de stays 0x3a; kind1/2 25de 0x2f+11=0x3a, 25dc stays 0x82/0xaa. Classifier entry sets 25cd=0x3e, c4=0 (@0x8004ab2c-38 + @0x8004aaa8).

EVIDENCE: Dispatcher @0x8004b084-b0b8 (slti v0,v1,11; c4==0 beq -> 0x8004b0c4; c4!=0 bne -> 0x8004b184; ori v0,0xb; beq -> 0x8004b234). Step0 @0x8004b0c4-b180 (25c8/25c9 swap @0x8004b0d4-b0e8; kind0 0xac->25dc @0x8004b108-110, 0x3a->25de @0x8004b114-11c; kind1 0x82/0x2f @0x8004b13c-150; kind2 0xaa/0x2f @0x8004b16c-180). Tail @0x8004b184-b228 (lbu kind of 25c8; beq 0 -> lhu 25dc; addiu -2; sh @0x8004b1c4 | else lhu 25de; addiu +1; sh @0x8004b1e4; lbu 25c9 @0x8004b1ec; bne 0x80 skip; lbu 25cd; addiu 250; sb @0x8004b20c; lbu c4; +1; sb @0x8004b228). Terminal @0x8004b234-b244 (sb 6->25c2; sb zero->25c3; sb zero,0(a0)=25c4).

### COMBINE-RESULT walker (state 7 c3=1, table @0x80010ff4, 17 entries) — format for wave 5 [RE1.5-EXE (PSX.EXE)]
Consumer: FUN_8004b33c (ITEM state 7) does jalr PTR_80074c44[25c3] where @0x80074c44={[0]=0x8004b37c 2nd-cursor+confirm, [1]=0x8004b408 result-anim walker}. Walker @0x8004b408: c4<0x11 -> jr [0x80010ff4+c4*4]. Steps 0-7 -> 0x8004b43c GROW (1 frame each): 25d0+=1, 25d1+=1, 25d2-=1, 25d3-=1, c4++. Steps 8-15 -> 0x8004b4a8 SHRINK: 25d0-=1, 25d1-=1, 25d2+=1, 25d3+=1, AND 25be := 25bd (2nd cursor snapped onto cursor) each frame, c4++. Step 16 -> 0x8004b524 TERMINAL: 25c2=6, 25c4=0, 25c3=0, 25d0=25d1=25d2=25d3=0. DAT_800b25d0-d3 are a 4-byte cursor-frame pulse (peak +-8 at frame 8), consumed by the cursor-group prim builder FUN_80047648 (reads @0x80047e98/0x80047f00/0x80047f18/0x80047f80); zeroed at menu init @0x8004646c-6484. Anim entered from 0x8004b37c confirm: jal 0x8004e900 (matcher) -> jal 0x8004e054(result) -> if result!=0: jal 0x8004dadc (compaction), 25c4=0, 25c3=1; if 0 -> 25c2=6; cancel(ac76c&0x8000) -> 25c2=6 (@0x8004b384-b3f4).

EVIDENCE: FUN_8004b33c @0x8004b33c-b364 (lbu 25c3; lw [0x80074c44+v0*4]; jalr); table bytes @0x80074c44='7c b3 04 80 08 b4 04 80'. Grow @0x8004b43c-b49c (lbu d0/d1/d2/d3/c4; addiu +1/+1/-1/-1/+1; sb x5). Shrink @0x8004b4a8-b518 (addiu -1/-1/+1/+1/+1; sb v1(25bd)->25be @0x8004b4f0; sb x5). Terminal @0x8004b524-b55c (sb 6->25c2; sb zero->25c4/25c3/25d0/25d1/25d2/25d3). Readers: ghidra1_V2.txt lines 160815-160874 (lbu DAT_800b25d0..d3 @0x80047e98/f00/f18/f80 inside FUN_80047648); init zeroing ghidra1_V2.txt 158923-158929 (@0x8004646c-84).

### 'You cant use it here.' path (c3=6 @0x8004b250) — 3-substate mini-FSM on c4 [RE1.5-EXE (PSX.EXE)]
c4==0: slide command cluster out: if 25ee < 251 -> 25ee += 14 (stay); else c4=1. c4==1: jal FUN_80027e68(a0=0x00a80018, a1=0x8400, a2=0, a3=0) (message 'You cant use it here.', bank 0x00a8 entry 0x18 encoding per prior wave-3 anchor), then c4=2. c4==2: while (DAT_800b8520 & 0x80) stay (message system busy); when clear -> 25c2=1 (back to GRID state, NOT the command stage), 25c3=0, 25c4=0. Entered by classifier @0x8004ab6c (id >= 0x15 and not a heal: c3=6, c4=0 @0x8004ab78). Duration: slide from 25ee=166 needs 7 add-frames (166+14x7=264>=251) + 1 detect frame, then message frames until dismissed.

EVIDENCE: @0x8004b264-b298 c4 switch; c4==0 @0x8004b2a0-b2d4 (lh 25ee; slti v0,251; beq -> ori v0,1; sb->25c4 | addiu v0,v1,14; sh->25ee); c4==1 @0x8004b2d8-b2f8 (lui a0,0xa8; ori a0,0x18; ori a1,0x8400; addu a2,zero; jal 0x80027e68; addu a3,zero; ori v0,2; sb->25c4); c4==2 @0x8004b2fc-b324 (lbu 0x800b8520; andi 0x80; bne stay; ori v0,1; sb->25c2; sb zero->25c3; sb zero->25c4). Classifier entry @0x8004ab6c-ab7c (ori 6; sb->25c3; sb zero->25c4).

### Classifier c3=0 @0x8004aa64 (confirmed + start-value writes) [RE1.5-EXE (PSX.EXE)]
id = inv[25bd*4]@0x800b10ac. id < 0x15 (weapon): c4=0 (@0x8004aaa8); compare inv[25c8*4].id vs inv[25bd*4].id: EQUAL -> c3=2 UNEQUIP + 25cd=0x3e; else if 25c8==0x80 -> c3=1 EQUIP + 25cd=0x80; else -> c3=5 SWAP + 25cd=0x3e. Else if (id-0x22)<0xe && id!=0x25 -> c3=3 HEAL (no c4 write — relies on the every-exit-restores-c4=0 invariant). Else -> c3=6 message + c4=0. Command-stage USE entry ([0] of PTR @0x8004a73c -> 0x8004a550) sets only 25c2=5; the block @0x8004a578-a648 that duplicates this logic is the known dead prototype (plan section 4).

EVIDENCE: @0x8004aa64-ab84 full disasm: sltiu v0,a0,0x15 @0x8004aa90; sb zero->25c4 @0x8004aaa8; equal-id branch @0x8004aadc-aaf8 (c3=2 @0x8004aaec, 0x3e->25cd @0x8004aaf8); 0x80 check @0x8004ab04-ab20 (0x80->25cd @0x8004ab14, c3=1 @0x8004ab20); swap @0x8004ab2c-ab3c (0x3e->25cd, c3=5); heal gate @0x8004ab48-ab60 (sltiu (id-0x22),0xe; beq a0,0x25 skip; c3=3); message @0x8004ab6c-ab7c. Jump table @0x8004a73c bytes '50 a5 04 80 58 a5 04 80 68 a5 04 80 70 a5 04 80' -> targets @0x8004a550/558/568/570 = 25c2 5/9/8/7 (CHECK also 25d6=0 @0x8004a55c).

### Rest/init values of the visual registers (screen init LAB_80049524) [RE1.5-EXE (PSX.EXE)]
@0x80049584-800496e0: reads 25c8; IF 25c8==0x80 (nothing equipped): 25cd=0x80, 2608=-55 (addiu v0,zero,-55); ELSE: 25cd=0x3e, 2608=0. Then kind := inv[25c8*4+2]: kind0 -> 25dc=0x96, 25de=0x3a; kind1 -> 25dc=0x82, 25de=0x3a; kind2 -> 25dc=0xaa, 25de=0x3a. So equip's 2608 (-55 -> 0) and 25cd (0x80 -> 0x3e) land exactly on the equipped rest state, and swap lands exactly, but the equip anim's final 25de is 0x3b (kind0: 0x35+6) or 0x35 (kind1/2: 0x2f+6) vs reopen rest 0x3a — a genuine original end-state inconsistency the port must replicate, not 'fix'. 25d4 (condition-wipe flag) zeroed @0x8004645c; 25d0-d3 zeroed @0x8004646c-84 (FUN_800460b8).

EVIDENCE: @0x80049584-88 (lbu v1,9672=25c8); @0x800495e8-f8 (bne v1,0x80 -> 0x80049610; delay ori v0,0x3e; taken: sb 0x3e->25cd @0x80049614, sh zero->2608 @0x8004961c; not-taken: ori v0,0x80; sb->25cd @0x800495f8; addiu v0,zero,-55 @0x800495fc; sh->2608 @0x80049604); kind blocks @0x80049640-660 (0x96/0x3a), @0x8004967c-9c (0x82/0x3a), @0x800496b8-dc (0xaa/0x3a). Writer scan ghidra1_V2.txt: 2608 stores only @0x80049604/0x8004961c/0x8004acd0/0x8004ad94/0x8004c7a0; 2608 readers @0x80047abc (FUN_80047648 prim y) + @0x80049c90 (draw FUN_80049a5c).

### 25c9 quirk — no init writer, gates the equip fade [RE1.5-EXE (PSX.EXE) + DEBUG.BIN]
DAT_800b25c9 (previous equip slot) is written ONLY by equip step 5 (@0x8004abe0) and swap step 0 (@0x8004b0e8) — no menu-init or DEBUG.BIN writer exists (EXE-wide ghidra xref list + DEBUG.BIN byte scan for imm 0x25c9 store ops: zero hits). The equip/swap fade guard 'if 25c9==0x80 then 25cd-=6' therefore behaves history-dependently: on the FIRST equip after boot (25c9 BSS=0) only steps 5-10 fade (25cd ends 0x80-36=0x5c, not 0x3e) — the screen-init reset (0x3e on reopen) later hides it; on later equips 25c9 is stale from the previous anim. Byte-true port must model 25c9 as a persistent global with initial 0, NOT derive the fade from 'was empty before'.

EVIDENCE: ghidra1_V2.txt xrefs of DAT_800b25c9: only 8004abe0 (sb), 8004ac80 (lbu), 8004b0e8 (sb), 8004b1ec (lbu). DEBUG.BIN scan (python over shared_assets/PSX/BIN/DEBUG.BIN, all load/store opcodes with imm16==0x25c9): no hits. FUN_800460b8 init zeroing list @0x8004645c-6484 covers 25d4/25d0-d3 but not 25c9; master task zeroing @0x80046050-6c covers only 25bf/25c1/25c2/25c3.

### HEAL (c3=3, FUN_8004adcc) — @0x80010f84 is a per-ITEM wipe-type table, NOT a step table [RE1.5-EXE (PSX.EXE)]
3-substate FSM on c4: c4==0 -> IF (id-0x22)<0xd: jr [0x80010f84+(id-0x22)*4] — [1](0x23) and [4](0x26) -> 0x8004ae70: 25d4=2 (horizontal wipe); [12](0x2e) -> 0x8004aea4: NO wipe; all others -> 0x8004ae84: 25d4=1 (vertical wipe); ELSE (id 0x2f, idx 13): hp=0x4d @0x8004ae98; then common c4++ @0x8004aea4. c4==1 -> wait until 25d4==0 (wipe driver self-clears @0x80048eec; port-verified durations mode1=32f, mode2=36f), then c4=2. c4==2 -> consume slot (id/qty/kind bytes = 0 @0x8004aee4-af28), jal 0x8004dadc (compaction), apply heal via SECOND per-item table @0x80010fbc[(id-0x22)] (idx>=0xd -> hp=0x4d @0x8004b028), then ECG sweep DAT_800b2600=0x20 @0x8004b038, c3=0, c4=0, 25c2=6. So the wipe runs BEFORE the HP change and consumption; the port's re15_inv_screen_heal_wipe mapping ([1]/[4]->mode2, [12]->none, rest->mode1 + sweep 0x20) matches the bytes; the ordering note in item_use_common.c:103 (wipe currently armed AT consume) remains the open wave-3 alignment.

EVIDENCE: c4 switch @0x8004ae0c-ae44; dispatch @0x8004ae48-ae68 (andi v1,s1,0xff; sltiu v0,v1,0xd; jr [0x80010f84+v1*4]); 0x8004ae70 (ori 2; sb->25d4); 0x8004ae84 (ori 1; sb->25d4); fallthrough hp=0x4d @0x8004ae98-aea0 (sh v0,-13586(at)=0x800acaee=player+0x9a); tail c4++ @0x8004aea4-aebc; wait @0x8004aec0-aedc (lbu 25d4; bne stay; sb 2->25c4); consume @0x8004aee4-af28 (sb zero on 0x800b10ac/ad/ae + cursor*4); jal 0x8004dadc @0x8004af2c; applier dispatch @0x8004af34-af58 (sltiu 0xd; jr [0x80010fbc+idx*4]); exit @0x8004b034-b058 (ori 0x20; sh->2600; sb zero->25c3/25c4; sb 6->25c2). Table bytes @0x80010f84: '84 ae..' x1, '70 ae' at [1], '84 ae' [2,3], '70 ae' [4], '84 ae' [5..11], 'a4 ae' [12], 00000000 [13].

### Heal appliers @0x80010fbc decoded + PORT BUG (id 0x24) [RE1.5-EXE (PSX.EXE) vs re15_port/engine/src/item_use_common.c]
13 pointers indexed (id-0x22), s0=0x64=100 (@0x8004ae14): [0]0x22 hp=100 abs (0x8004afe4); [1]0x23 hp+=25 & cure poison (0x8004af60 -> 0x8004aff8, srl s0,2); [2]0x24 hp+=25 ONLY (0x8004af68: delay-slot `srl v0,s0,2` -> jump to the addu@0x8004afa8) — THE PORT HAS +50 HERE (item_use_common.c:20 s_heal[2]={0,50,0}) = confirmed divergence, must become {0,25,0}; [3]0x25 nothing (0x8004b034, unreachable via classifier); [4]0x26 cure-poison only (0x8004af7c, 0x800acaec &= 0xfffd); [5]0x27 hp+=50 (0x8004af98, srl s0,1); [6]0x28 hp+=50; [7]0x29 hp+=25 & cure; [8]0x2a hp=100 abs; [9]0x2b hp+=75 (0x8004afbc: hp + (100>>1) + (100>>2)); [10]0x2c hp+=50 & cure; [11]0x2d hp=100 abs; [12]0x2e hp+=50 & cure; idx>=0xd (0x2f) hp=0x4d abs. No clamp anywhere; poison-clear = u16@0x800acaec &= ~2.

EVIDENCE: Table bytes @0x80010fbc: 'e4 af 60 af 68 af 34 b0 7c af 98 af 98 af b4 af e4 af bc af f4 af e4 af f4 af' (+0x8004 halves) + 00000000 [13]. Raw bytes disproving the port's +50 for 0x24: @0x8004af68-af7b = '0b 80 04 3c | ee ca 84 24 | 00 00 83 94 | ea 2b 01 08 | 82 10 10 00' = lui a0,0x800b; addiu a0,-0x3512 (0x800acaee hp); lhu v1,0(a0); j 0x8004afa8; srl v0,s0,2 (0x00101082, shamt=2 -> 100>>2=25); @0x8004afa8 = '21 10 43 00'=addu v0,v0,v1; j 0x8004b034; 'a4 82 00 00' wait: @0x8004afb0='00 00 82 a4'=sh v0,0(a0). By contrast the +50 entry @0x8004af98 has '42 10 10 00' @0x8004afa4 = srl v0,s0,1 (shamt=1 -> 50). Port file: re15_port/engine/src/item_use_common.c line 20.

### No SE / no icon-cache calls in any anim walker + post-anim flow [RE1.5-EXE (PSX.EXE) + DEBUG.BIN]
Full disassembly 0x8004aa64-0x8004b564 contains exactly five jal targets: 0x8004dadc (compaction; heal c4==2 and combine-confirm), 0x80027e68 (message), 0x80048904 (2nd cursor draw, combine c3=0 only), 0x8004e900/0x8004e054 (combine matcher/executor). ZERO calls to SE FUN_80045024 and ZERO calls to icon-cache FUN_800492b8/FUN_8004947c — equip/unequip/swap redraw purely from the mutated globals (25c8/25c9/25cd/2608/25dc/25de) read per-frame by FUN_80047648/FUN_80049a5c. Confirm/cancel SEs fire in state 4 BEFORE dispatch (SE(4,6) confirm / SE(4,5) cancel per FUN_8004a0cc case 4) and SE(4,0x0a) on the DEBUG.BIN equip-cell confirm — bank/id attribution of the remaining state-4 sites = Task C. All four terminals route to ITEM state 6 (25c2=6): slide 25ee += 14/frame until >0xfa then 25c2=1 (grid) per FUN_8004a0cc @0x8004a6bc-a6e4/@0x8004a684-a6a8; message instead returns directly to 25c2=1.

EVIDENCE: jal scan of the disassembled range (all outputs above): jal 0x8004dadc @0x8004af2c + @0x8004b3cc; jal 0x80027e68 @0x8004b2e8; jal 0x80048904 @0x8004b384; jal 0x8004e900 @0x8004b3b0; jal 0x8004e054 @0x8004b3b8. No 'jal 0x80045024' and no 'jal 0x800492b8'/'jal 0x8004947c' between 0x8004aa64 and 0x8004b564. State-6 slide @0x8004a684-a6a8 (lh 25ee; slti 251; -> ori v0,1; sb->25c2 | addiu +14). SE(4,6)/SE(4,5) in state 4 per FUN_8004a0cc decompile (RE_15_Quellcode_V2/FUN_8004a0cc.c:105-135) + SE(4,0xa) @0x800c64e8 (DEBUG.BIN).

**Open questions:**
- c3=4 (PTR_80074c28[4] = 0x8004b074, a pure `jr ra` no-op that would freeze the USE state forever) has no discovered setter — presumably dead/reserved; if a setter exists it was not found in the EXE (grep for sb 4->25c3 not exhaustively done across DEBUG.BIN).
- The equip anim's final 25de (0x3b kind0 / 0x35 kind1-2) differs from the reopen rest value 0x3a — bytes are unambiguous, but a live PSX capture of the digit y right after an equip would visually confirm the original really leaves the digits 1-5px off until reopen (recommended before someone 'fixes' the port to converge).
- DAT_800b25cd's draw-side reader was not found by direct xref (only the walkers read it at 0x8004ac94/0x8004b200); the draw code must access it via a pointer/struct base — wave-1 pixel verification already established it as the equip-box gun rgb, but the exact consuming instruction in FUN_80047648/FUN_80049a5c remains uncited.
- SE bank/id decode of the remaining FUN_8004a0cc state-4 sites (0x8004a158/a47c/a4a4/a4cc/a4f4/a520/a664) = Task C scope; this task only pinned SE(4,6) confirm, SE(4,5) cancel, SE(4,4) d-pad, SE(4,0x0a) equip-cell @0x800c64e8.
- Port fix needed (not applied — RE-only task): item_use_common.c s_heal[2] (id 0x24) must change {0,50,0} -> {0,25,0} per srl v0,s0,2 @0x8004af78; also the port's heal Yes/No-prompt FSM shape differs from the original prompt-less wipe->apply flow (known wave-3 alignment, gap already flagged in the file's own comments).

---
## TASK C VERDICT: BOTH screens are FULLY FUNCTIONAL in the shipped MZD build — verdict (a) for each; the wave-2 immediate-back stubs are NOT byte-true and each screen should become its own small implementation wave. (1) MAP tab (25c1=1, FUN_8004c058, screen-0-internal): a real floor-map viewer — 25-frame panel slide, VRAM rect (448,256,64,256) saved to 0x801a0000, MAP0x.PIX (CD id table @0x80074c4c 
(citations_complete=True)

### MAP tab verdict (in-status MAP, FUN_8004c058) [RE1.5 PSX.EXE (info/Re1.5/PSX.EXE) + shared_assets/PSX/DATA/MAP01..MAP0D.PIX]
Verdict (a) FULLY FUNCTIONAL: floor-map viewer. Slide-out 25 frames, save VRAM rect (448,256,64,256) to 0x801a0000, CD-load MAP graphic to 0x801a8000 (task 0x8004c328: file id = u16 @0x80074c4c[DAT_800b260e]) and upload into the same rect; interactive until cancel/L1; reverse slide; VRAM restore. The u16 table @0x80074c4c = {0xc,0xd,...,0x18} (13 ids) = MAP01.PIX..MAP0D.PIX; all 13 files present in re15_port/shared_assets/PSX/DATA/, each 32768 B = 64hw x 256 raw PIX (4bpp page at VRAM (448,256), DR_MODE tpage 0x17 @0x800b2650). Wave-2 immediate-back stub is NOT byte-true; MAP deserves its own wave.

EVIDENCE: Table dump @0x80074c4c via re15_disasm read (u16 pairs 0x000C..0x0018); runner FUN_8004c058 contract per shots/inv_wave2_spec.md (slide deltas @0x8004c0c8-14c, VRAM save @0x8004c158-78, upload @0x8004c198-b0 a1=0x801a8000); loader 0x8004c328 lbu DAT_800b260e + lhu @0x80074c4c (wave2 spec @0x8004c328-50); ls shared_assets/PSX/DATA MAP01..MAP0D.PIX = 13x 32768 B

### MAP content: what is drawn [RE1.5 PSX.EXE]
Map content (drawn only while 25c1==1 && 25c2==1, gate word 0x00010100 @0x80049bb4-cc) = FUN_800473f8 per frame: (1) blinking player-position marker — 8x8 semi-trans textured quad (code 0x2e), uv (224,128)-(232,136), clut DAT_800b261c (0x7b90), rgb = pulse DAT_800b2602, screen pos = player world X (0x800aca88) / Z (0x800aca90) scaled by per-room table @0x800768b0 (stride 8 {x_off,y_off,x_div,y_div}) indexed by DAT_800b260d; (2) two fixed sprites (30,30) 88x32 uv(0,0) and (270,40) 32x48 uv(96,0) sampling the MAP PIX page; (3) N room-rectangle SPRTs (code 0x64|2 semi-trans, clut GetClut(0x100,0x1f5)=0x7d50 = TEX.TIM CLUT row 21) from the per-page list table @0x80076840: 14 {u16 count, u32 ptr} pairs, counts {7,10,11,10,7,2,11,14,15,15,4,4,8,1}, list entries {x,y,w,h,u,v} stride 12. Prim arena @0x8019a800 built by FUN_80046fd8; AddPrim to OT 0x800aa6d8+parity*0x4000.

EVIDENCE: RE_15_Quellcode_V2/FUN_80046fd8.c (marker quad uv 0xe0/0x80..0xe8/0x88, clut DAT_800b261c, GetClut(0x100,0x1f5); list walk stride 6 u16) + FUN_800473f8.c (marker rgb=DAT_800b2602, AddPrim loop count+2); raw-MIPS spot-verify @0x80047420-44 (lbu DAT_800b260d, *8, lhu @0x800768b4, *10 >>20 scaling — matches decompile); table dump @0x80076840 (counts/ptrs 0x800768a0/0x80076914/...)

### MAP page/room selection mechanism [RE1.5 PSX.EXE]
On MAP entry (tab confirm or L1), LAB_8004974c calls per-stage init PTR @0x80074c0c[stage lh DAT_800b0fe0] = {0x8004b568, 0x8004b8a0, 0x8004b9d4, 0x8004bc9c, 0x8004bdd4, 0x8004bf70}. Stage-1 init 0x8004b568: switch on room index lh @0x800b0fe2 (bound 0x26) via jump table @0x8001103c (38 entries); each case writes DAT_800b260d = room byte and DAT_800b260e = map page (observed pages 1/2/3 for different room blocks; join @0x8004b888-894 sb v0->260e, sb v1->260d). Room >= 0x26 returns without writing (stale-value edge case). No other writer of 260e exists in the EXE (scan clean).

EVIDENCE: re15_disasm dis 0x8004b568 (lh 0x800b0fe2, sltiu 0x26, jr via @0x8001103c; case stubs sb ->0x800b260d; tails ori v0,1/2/3 + j 0x8004b888); dis 0x8004b880-89c (sb v0,9742(at)=0x800b260e, sb v1,9741(at)=0x800b260d, jr ra); table dump @0x8001103c -> 0x8004b59c..0x8004b87c; table @0x80074c0c dumped

### FILE tab verdict (FUN_800c6ca0, DEBUG.BIN) [DEBUG.BIN (shared_assets/PSX/BIN/DEBUG.BIN == info/Re1.5/PSX/BIN/DEBUG.BIN, cmp identical; maps 1:1 @0x800c0000)]
Verdict (a) FULLY FUNCTIONAL: 3-page document browser with a working document reader. Interactive state draws: 10 name rows (0x800c72b8), page title (FUN_800c727c(0x2c,0x1f,page)), selection highlight (0x800c742c), then inner dispatch @0x800c7008 {0=page-level input 0x800c6dbc, 1=row-select 0x800c7010 via 0x800c6ec8}. Page titles from ptr table @0x800c78e4 decode (glyph=code+0x24, 0x07=end) to: page0="Files", page1="S.T.A.R.S.", page2="Umbrella". Wave-2 immediate-back stub is NOT byte-true; FILE deserves its own wave.

EVIDENCE: DEBUG.BIN disasm @0x800c6d70-6db8 (jal 0x800c72b8; a2=lbu 0x800c6c95, a0=0x2c a1=0x1f jal 0x800c727c; jal 0x800c742c; dispatch lw @0x800c7008[0x800c6c94]); title ptrs @0x800c78e4 = {0x800c78f0,0x800c78f6,0x800c7907}; bytes decode 22 45 48 41 4f='Files', 2f57 3057 1d57 2e57 2f57='S.T.A.R.S.', 31 49 3e 4e 41 48 48 3d='Umbrella'

### FILE row names + visibility masks [DEBUG.BIN + RE1.5 PSX.EXE]
Row drawer 0x800c72b8: 10 rows at x=0x2c, y=0x35+row*16; per-page row-name base byte @0x800c7370 = {0x48,0x52,0x5c}; visibility bitmask u16 @0x800c6c98[page] — bit set: print name FUN_80028840(base+row) (returns 0x800c4a28 + u16[0x800c495c + idx*2], the DEBUG.BIN name bank) with a3=0; bit clear: print fixed 21-underscore string @0x800c7916 with a3=0x30. Shipped masks (file==RAM) = {0x0001, 0xffff, 0xffff}: page 0 shows ONLY row 0 "Chris' Diary"; pages 1/2 show all 10. Masks are STATIC: exhaustive immediate scan of DEBUG.BIN + EXE finds exactly ONE reference (lhu @0x800c72f0 via addiu 0x6c98 @0x800c72e8) and ZERO writers — visibility is not gameplay-driven in this build. Decoded names: 0x48-0x51 = Chris' Diary, Operation Report, File 3..File 10; 0x52-0x5b = Albert Wesker, Jill Valentine, Chris Redfield, Barry Burton, Rebecca Chambers, Brad Vickers, Enrico Marini, E.Dewey&K.Dooley, J.Frost&F.Speyer, K.Sullivan&R.Aiken; 0x5c-0x65 = Cerberus, Mutated Baboon, Mutated Alligator, Gargantuagator, Arachnophobia, Giant Spiders, Umbrella File 7/8/9, blank.

EVIDENCE: DEBUG.BIN disasm @0x800c72b8-736c (lbu 0x800c6c95; lhu s3=@0x800c6c98[page*2]; lbu s1=@0x800c7370[page]; loop 10, y 0x35+=16, andi s3,1 -> jal 0x80028840 / else str 0x800c7916 a3=0x30; jal 0x80028ec4); EXE disasm @0x80028840-64 (lhu @0x800c495c+idx*2; v0=0x800c4a28+off); file bytes @0x6c98={01 00 ff ff ff ff}, @0x7370={48 52 5c}; imm-scan: only hit for 0x6c98 = 0x800c72e8 (addiu), EXE zero hits; name bank decoded from file offsets 0x495c/0x4a28

### FILE row-select sub-state (0x800c7010) [DEBUG.BIN]
Entered from page level via raw SQUARE (sets [0x800c6c94]=1, cursor [0x800c6c96]=0). Row-select input (raw edges DAT_800ac762): DOWN 0x4000 -> cursor=(cur+1)%10 + SE(4,4); UP 0x1000 -> cursor-1 wrap 9 + SE(4,4); SQUARE 0x80 -> SE(4,5) + back to page level ([6c94]=0); CROSS 0x40 -> SE(4,6) + OPEN READER: DAT_800b25c2=3, reader page [0x800c6c97]=0, arrow-anim word @0x800c75fc=0. The mask is NOT checked — CROSS on any row (visible or underscore) opens the reader. Highlight drawer 0x800c742c: 4-word GPU packet {len 0x04000000, DR_MODE 0xe1000440 (ABR=2 subtractive), TILE 0x62 rgb (0x20,0x20,0x20)}: page level = box (17,25) 140x26 over the title; row level = bar (43, 52+row*16) 154x16; prim buf 0x800adc74+parity*0x20, AddPrim 0x8006b538.

EVIDENCE: DEBUG.BIN disasm @0x800c7010-710c (andi 0x40->0x800c704c SE 0x406 + sb 3->0x800b25c2 + sb 0->0x800c6c97 + sw 0->0x800c75fc; 0x80->SE 0x405 + sb 0->0x800c6c94; 0x4000/0x1000 cursor arithmetic sltiu 0xa wrap); @0x800c742c-74ec (words 0x04000000/0xe1000440/0x62202020; page branch xy 0x00190011 wh 0x001a008c; row branch xy 0x0034002b + row<<20, wh 0x0010009a)

### FILE reader: single embedded document, selection-independent [DEBUG.BIN]
Reader state (25c2=3 @0x800c6f90) = jal 0x800c7600(a0=0x28 text x) + 0x800c7528 (corner arrows) + 0x800c7110 (input). Text drawer 0x800c7600: doc base = 0x800ccd34; page count = u16[base]>>1 = 7; cur page [0x800c6c97] clamped to 6; text ptr = base + u16[base+page*2] (offsets {0xe,0x27,0x12d,0x1ff,0x2fc,0x402,0x51a}); printed via EXE glyph fn 0x80028ec4(x,0x20,text,0). The document is the RE1.5 "Operation Report" (page0 = title card; pages 1-6 = 'September 26th, The Raccoon Police Dept. was unexpectedly attacked by zombies...' ... 'Recorder: David Ford'). CRITICAL: content does NOT depend on selected page (6c95) or row (6c96) — every confirm shows this one document. Exhaustive immediate scan: only 4 refs to 0xcd34 in DEBUG.BIN (lhu @0x800c7128, lhu @0x800c7544, addiu @0x800c7614, lhu @0x800c7768) and ZERO in the EXE, all reads; no CD-loader targets the region — the doc ships statically inside DEBUG.BIN @file offset 0xcd34.

EVIDENCE: DEBUG.BIN disasm @0x800c6f90-6fa8 (jal chain), @0x800c7600-766c (lhu @0x800ccd34 srl 1; clamp; lhu base+page*2; jal 0x80028ec4 a1=0x20); file bytes @0xcd34: 0e 00 27 00 2d 01 ff 01 fc 02 02 04 1a 05 + text; glyph decode (code+0x24) of all 7 pages; imm-scan results for 0xcd34 (4 reads, 0 writes, EXE 0 hits)

### FILE reader input + page-turn animation [DEBUG.BIN]
Reader input 0x800c7110 (raw edges; s0 = u16[0x800ccd34]>>1 = 7 = end position): CROSS 0x40 -> SE(4,5) + 25c2=1 (close reader, back to list); SQUARE 0x80 -> close only when page==end; RIGHT 0x2000 -> if page+1==end: page=end + SE(4,4) (no anim); if page+1==end+1: close-check; else 25c2=6 + anim regs @0x800c78a4={0,0xc} + SE(4,8) (forward page-turn); LEFT 0x8000 -> if page==end: page-- + SE(4,4); if page-1<0: page=0; else 25c2=4 + @0x800c78a4={0,0x44} + SE(4,8) (backward turn). States 4+/6 run 0x800c6fb0: 25c2 = 0x800c77bc(25c2) (anim driver advances/returns next state) and draw text at x = s16 @0x800c78a6 (page slides horizontally). Corner-arrow drawer 0x800c7528: left arrow at (0x14-off, 0x70) when page>0, right arrow at (0x11c+off, 0x70) with type 2/3/1 by distance to end, bob cycle @0x800c75fc (offset 4 at counter 0x1e, 0 at 0x3c, period 60).

EVIDENCE: DEBUG.BIN disasm @0x800c7110-7278 (branch chain andi 0x40/0x80/0x8000/0x2000; sb 6/4 -> 0x800b25c2; sh 0/0xc and 0/0x44 -> 0x800c78a4; lui a0,0x408/0x404/0x405 jal 0x80045024); @0x800c6fb0-6fd4 (jal 0x800c77bc -> sb v0->25c2; lh 0x800c78a6 -> jal 0x800c7600); @0x800c7528-75e8 (lhu 0x800ccd34, jal 0x800c7670 x=0x14-off / 0x11c+off y=0x70, counter cmp 0x1e/0x3c)

### Exit contracts of the wave-2 stubs — VERIFIED CORRECT [RE1.5 PSX.EXE + DEBUG.BIN]
MAP exit @0x8004c2c4-304: LoadImage(rect(0x1c0,0x100,0x40,0x100) <- 0x801a0000) restore + DrawSync(0), then sb zero -> 0x800b25ca, 0x800b25c1, 0(s1)=0x800b25c2, 1(s1)=0x800b25c3, with s1=0x800b25c2 proven in the FUN_8004c058 prologue @0x8004c060-64. FILE exit @0x800c6f74-8c: sb zero -> 0x800b25ca, 0x800b25c1, 0(a1)=25c2, 1(a1)=25c3, with a1=0x800b25c2 proven in the FUN_800c6ca0 prologue @0x800c6ca8-cac. So the stubs' register effects (25ca=0, 25c1=0, 25c2=0, 25c3=0, tab kept) are byte-true; only the immediate-return behavior itself diverges (both screens are functional and must run their slide/interactive states). Port stub sites: re15_port/engine/src/menu_common.c:511-527.

EVIDENCE: re15_disasm dis 0x8004c2c4 (sh 0x1c0/0x100/0x40 rect, jal 0x80068c88 a1=0x801a0000, jal 0x80068a60, sb zero 9674/9665(at), sb zero 0/1(s1)); dis 0x8004c058 (lui s1,0x800b; addiu s1,9666 = 0x800b25c2); DEBUG.BIN dis 0x800c6f74 (sb zero 9674/9665(at), sb zero 0/1(a1), j 0x800c6fd8); dis 0x800c6ca0 (addiu a1,9666 = 0x800b25c2)

### LAB_8004c54c/LAB_8004cca4/FUN_8004c830 family is NOT the MAP tab — dormant sce-6 close-up screen [RE1.5 PSX.EXE + shared_assets/PSX/STAGE*/ROOM*.RDT]
The task prompt's 'MAP (LAB_8004cca4 family)' label is incorrect. That triplet is status-screen index DAT_800b25c0==1 (init/run/draw tables @0x80074be8/bf4/c00), reachable ONLY via the sce-6 AOT handler @0x80043194 which writes sb 1 -> 0x800b25c0 (@0x800431a8, raw-verified; the only nonzero 25c0 writer per prior write scan), sets open request aca3c|=0x8000 and payload byte -> DAT_800b25cc. It is the 3D item/drawer close-up screen (room506u.scd aux scripts). Shipped MZD RDTs contain ZERO sce-6 AOTs (full 6-stage SCD sweep, prior report) -> the family is dormant/unreachable in-game; no port work needed and no stub required. The reachable MAP is FUN_8004c058 inside screen 0 (sub-state 25c1==1).

EVIDENCE: re15_disasm dis 0x80043194 (ori v1,1; sb v1,9664(at)=0x800b25c0; ori v0,0x8000 -> 0x800aca3c; lbu (a0) -> sb 9676(at)=0x800b25cc); shots/inv_re_reports.md lines 189-194 (25c0 write scan: only 0x800431a8 nonzero; RDT sweep sce-6 count = 0; screen-1 identity room506u.scd @0x8004c5fc-c68c)

### Recommendation [RE1.5 PSX.EXE + DEBUG.BIN + shared_assets]
Replace both wave-2 stubs with real implementations, each as its own wave: MAP wave = extract tables (@0x80074c0c per-stage inits incl. room jump tables, @0x80074c4c file ids, @0x80076840 room-rect lists, @0x800768b0 marker scale rows, marker uv/clut) + MAP0x.PIX upload path + FUN_8004c058 4-state FSM (slide/interactive/reverse/restore) — all assets present. FILE wave = port FUN_800c6ca0's 5-state FSM + row list (names/masks/underscores from DEBUG.BIN data, shippable as extracted constants with @0x800c… citations) + row-select highlight + reader with the embedded 7-page Operation Report text + page-turn anim (needs 0x800c77bc disasm) + SEs (4,4)/(4,5)/(4,6)/(4,8). Until then the stubs must at minimum keep the verified exit-contract register effects (they do).

EVIDENCE: All citations in the preceding facts; port stub sites re15_port/engine/src/menu_common.c:511-527

**Open questions:**
- Per-stage room->map-page mapping: only the stage-1 init (0x8004b568, jump table @0x8001103c) was decoded; stages 2-6 inits (0x8004b8a0/0x8004b9d4/0x8004bc9c/0x8004bdd4/0x8004bf70) and the full room->{260d,260e} tables need disassembly for the MAP implementation wave.
- Player-marker position formula discrepancy: FUN_80046fd8 decompile shows (world+25000)/div while FUN_800473f8 shows ((world+32000)*10*scale>>20 +5)/10 rounding — the per-frame path @0x80047420-44 is raw-verified in form, but both formulas need full raw-MIPS verification (decompiles have lied) before porting the marker math.
- Page-turn animation driver 0x800c77bc (state sequencing 4/6 -> back to 3, x-offset curve in @0x800c78a4/78a6) and arrow-glyph drawer 0x800c7670 internals not yet disassembled — needed for the FILE wave, not for the verdict.
- SE(4,8) identity (played on page turns) not audio-verified; likewise text-printer FUN_80028ec4's a3 flag values (0=default, 0x10 title, 0x30 dimmed underscores) are inferred from usage, glyph/color semantics not decoded.
- Reader 'end position' semantics: page index can reach s0=7 (one past the last text page; drawer clamps to 6) where SQUARE/CROSS close — decoded from disasm but not live-verified with a savestate in the reader state.
- MAP entry with room index >= 0x26 (stage 1) leaves DAT_800b260d/260e stale (init returns without writing) — original behavior is 'use previous values'; harmless but should be modeled verbatim.
- Glyph 0x78 at the start of each document title line (before 'Operation Report') and codes 0xf0/0x88 are unmapped control/ligature glyphs of the TEX.TIM font — decode when rendering the reader text pixel-true.
