# Global effect textures — byte-true VRAM extraction (RE1.5 CORE00.ESP bank)

The global effect bank `DATA/CORE00.ESP` holds effect ids **{3,8,0,2,4}** (loaded once at
game-init by the boot installer, param_4=0). Their **anim/coord records** are in CORE00.ESP;
their **textures** are NOT in any file — they live only in VRAM (uploaded at boot). We
recovered them byte-true from a live ROOM1140 frame via DuckStation `[Debug] ShowVRAM` (the
1024×512 view = VRAM 1:1), reconstructing the raw 15-bit halfwords from the PNG and decoding
the tpage/clut. Tool: `re15_port/tools/vram_png_to_tim.py` (or `.claude/skills/re15-room-capture/
scripts/re15_vram_extract.py`). Ground truth: `vram_view_ground_truth.png`.

## The map — 2 sprite sheets, recoloured per CLUT

The 5 global effects are only **TWO texture pages**, recoloured via different CLUTs:

| effect-id | word1 (runtime==file) | tpage → VRAM page | CLUT → VRAM | what it is |
|-----------|-----------------------|-------------------|-------------|------------|
| **0** | 0x001f**7951** | 0x001f → (960,256) 4bpp | 0x7951 → (272,485) | **dark blood** (hit splatter — the one wired in) |
| 2 | 0x001f**7a51** | 0x001f → (960,256) 4bpp | 0x7a51 → (272,489) | bright/fresh blood (same splatter sheet) |
| 4 | 0x001f**7ad1** | 0x001f → (960,256) 4bpp | 0x7ad1 → (272,491) | brown/dried blood (same sheet) |
| **8** | 0x001e**7911** | 0x001e → (896,256) 4bpp | 0x7911 → (272,484) | **fire / explosion / muzzle** (~340 spawns game-wide) |
| 3 | 0x001e**7811** | 0x001e → (896,256) 4bpp | 0x7811 → (272,480) | grey smoke / dust (same cloud sheet) |

- **Blood sheet @VRAM(960,256):** ~20 splatter frames + round pools/drops + streaks + a hit-marker.
- **Cloud sheet @VRAM(896,256):** puffy burst clouds + flickering flames (fire when orange, smoke when grey).

## Files here

- `effect0_blood.tim` — effect-0 as a byte-true 4bpp PSX TIM (CLUT[0]=0x0000). **This is the one
  wired into the port** (loaded at game-init → `RE15_TIM_SLOT_EFFECT_GLOBAL` slot 20; the effect
  slots treat index-0 as transparent, see render_pc.c). Renders on every zombie hit.
- `effect0_blood.png` … `effect8_fire.png`, `effect2_blood_bright.png`, `effect3_smoke.png`,
  `effect4_blood_brown.png` — human-readable REFERENCE decodes of each variant.
- `vram_view_ground_truth.png` — the full 1024×512 VRAM (source for all of the above; also holds
  the room/model textures, framebuffers, etc.).

- `effect8_fire.tim` — effect-8 sliced from **`DATA/TEX.TIM`** (see below), 4bpp 256×256. Wired
  into the port at `RE15_TIM_SLOT_FX_FIRE` slot 44 with `RE15_TIM_KEY_PSX` (its CLUT has 0x0000 at
  index 0 **and** index 15; index 0 is 68 % of every flame cell, so the default key would paint
  solid blocks). Generated by `tools/tex_tim_effect_slice.py --word1 0x001e7911`.

## Status / TODO

- effect-0 (blood): extracted + integrated + rendered. **Done** — but see the correction below:
  this file is the *lossy* VRAM stand-in (1269 texels, 1.94 %, differ from the TEX.TIM truth).
  Re-slicing it from TEX.TIM would make it byte-true; deliberately not done in the same change
  that added effect-8, to keep an already-working effect untouched.
- effect-8 (fire): extracted **from the file** + integrated + PINned
  (`tests/unit/test_1090_fire_pin.c` §G–J). ROOM1090's burning-debris variants 0 and 3 draw from
  it (`re15_esp_type26_flame`, jump table @0x80100364). **Done.**
- effects 2/3/4: extracted + mapped + uploaded to slots 21/22/23. Still the 16bpp VRAM stand-ins.
## ORIGINAL SOURCE FOUND (2026-07-02): `DATA/TEX.TIM`

The authoritative source of these effect sheets is **`DATA/TEX.TIM`** — a 1280×256 4bpp common
atlas (status/inventory UI + font + the effect sprites in its right portion). Proof (byte-true):
TEX.TIM's CLUT block is at VRAM(256,480) 32×24; the effect CLUTs live in its **right half
(columns 16–31)** and are **byte-identical** to the VRAM-extracted palettes **except bit 15 (the
STP / semi-transparency bit), which TEX.TIM preserves** and the ShowVRAM PNG lost:

    effect-0 blood @TEX clut(272,485): 0000 884c 842b 842a 8429 …   (VRAM extract: 0000 084c 042b …)
    effect-8 fire  @TEX clut(272,484): 0000 ffff efde e3de d7de …   (VRAM extract: 0000 7fff 6fde …)

The effect sprites (blood/cloud/flames/hit-marker) are visible in TEX.TIM's right portion
(x≳768). => the flag "original file unknown" is RESOLVED: it is TEX.TIM.

### CORRECTION 2026-08-21 — the upload is NOT "repacked"; slice the file, not the screenshot

This section previously claimed the VRAM effect pages were a *repacked* upload that "does NOT match
TEX.TIM row-for-row", and concluded that slicing the page from the file was "not worth it". **Both
claims are wrong**, and the second one hid a real data loss. Measured:

- TEX.TIM's image block (@file `0x620`, 320 halfwords × 256 rows, 4bpp, uploads to VRAM(0,0)) has
  its halfword **columns 192…319** blitted **verbatim** to **VRAM(896,256)**. All **32768/32768**
  halfwords of that rect are identical to `vram_view_ground_truth.png` (offset search over every
  8-aligned source column: only srcx=192 matches, and it matches 100 %). So the two effect pages
  are a plain rect blit: VRAM x −704, y −256.
- The **VRAM route is lossy.** The ShowVRAM PNG has **no alpha channel** (all 255), so halfword
  **bit 15 is dropped**. In a 4bpp page bit 15 is the *high bit of every 4th texel index* — so
  indices 8…15 silently collapse to 0…7 in every fourth texel column. That is not an STP-only
  cosmetic loss as claimed; it corrupts pixel data. Counted: **1269 halfwords** carry bit 15 in the
  blood page, i.e. `effect0_blood.tim` has **1269 wrong texels (1.94 %)**; the fire page has 4162.

=> **Source new effect sheets from `DATA/TEX.TIM` via `tools/tex_tim_effect_slice.py`.** It takes
the CORE00.ESP `word1` directly, resolves tpage/clut → file offsets, and has a `--verify-vram` mode
that re-asserts the slice against the ShowVRAM ground truth (0 mismatches for effect-8).
